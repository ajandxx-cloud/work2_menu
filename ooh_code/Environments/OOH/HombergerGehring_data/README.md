# Bundled Synthetic Instances

This directory stores the bundled synthetic benchmark instances used by the internal work2 simulator.

The files contain depot, demand-location, and meeting-point coordinates. Euclidean travel times are derived directly from coordinates for these instances, so no stored distance matrix is required.

- The last 10 locations in each instance act as the bundled meeting-point set.
