---
phase: 02-demand-calibration-and-outside-option-sensitivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ooh_code/Environments/OOH/customerchoice.py
  - ooh_code/Environments/OOH/Parcelpoint_py.py
  - ooh_code/Src/parser.py
  - ooh_code/Src/config.py
  - ooh_code/experiments/studies/phase34_outside_option_scan_m10.yaml
  - ooh_code/experiments/studies/phase34_outside_option_scan_m05.yaml
  - ooh_code/experiments/studies/phase34_outside_option_scan_00.yaml
  - ooh_code/experiments/studies/phase34_outside_option_scan_p05.yaml
  - ooh_code/experiments/studies/phase34_outside_option_scan_p10.yaml
  - ooh_code/experiments/suites/phase34_outside_option_scan.yaml
autonomous: true
requirements: [CAL-01]

must_haves:
  truths:
    - "customerchoice.py accepts an outside_option_util constructor parameter and uses it instead of the hardcoded 0.0"
    - "The parameter defaults to 0.0 so all existing studies continue to run without modification"
    - "A CLI argument --outside_option_util reaches customerchoicemodel via the parser -> config -> Parcelpoint_py chain"
    - "Five YAML study files exist, each setting a different outside_option_util value"
    - "A suite YAML groups the five studies for one-command execution"
  artifacts:
    - path: "ooh_code/Environments/OOH/customerchoice.py"
      provides: "Parameterized outside-option utility in MNL choice model"
      contains: "outside_option_util"
    - path: "ooh_code/Src/parser.py"
      provides: "CLI argument --outside_option_util"
      contains: "outside_option_util"
    - path: "ooh_code/experiments/studies/phase34_outside_option_scan_00.yaml"
      provides: "Baseline study at u_0=0.0"
      contains: "outside_option_util"
    - path: "ooh_code/experiments/suites/phase34_outside_option_scan.yaml"
      provides: "Suite grouping five outside-option scan studies"
      contains: "phase34_outside_option_scan"
  key_links:
    - from: "ooh_code/Src/parser.py"
      to: "ooh_code/Environments/OOH/customerchoice.py"
      via: "config.py -> Parcelpoint_py.py -> customerchoicemodel constructor"
      pattern: "outside_option_util"
    - from: "ooh_code/experiments/studies/phase34_outside_option_scan_00.yaml"
      to: "ooh_code/Src/parser.py"
      via: "base_args.outside_option_util parsed by research_pipeline"
      pattern: "outside_option_util"
---

<objective>
Parameterize the outside-option utility in the MNL choice model and create five YAML study manifests scanning u_0 = -1.0, -0.5, 0.0, +0.5, +1.0.

Purpose: The outside-option utility is currently hardcoded at 0.0 in customerchoice.py (line 45: `utils[n_offers] = 0.0`). Parameterizing it enables concrete sensitivity analysis of how outside-option strength affects menu-optimization outcomes, directly addressing the Round 2 reviewer's concern that MNL parameters are uncalibrated design parameters (per CONTEXT.md Outside-Option Parameterization decision).

Output: Modified customerchoice.py, Parcelpoint_py.py, parser.py, config.py with backward-compatible outside_option_util parameter. Five study YAML files and one suite YAML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-demand-calibration-and-outside-option-sensitivity/02-CONTEXT.md

<interfaces>
<!-- Threading path for outside_option_util: parser.py -> config.py -> Parcelpoint_py.py -> customerchoicemodel -->

From ooh_code/Environments/OOH/customerchoice.py:
```python
class customerchoicemodel:
    def __init__(self, base_util, dist_scaler, travel_time_weight=0.0, pickup_time_weight=0.0):
        # Line 45: utils[n_offers] = 0.0  <-- this must use self.outside_option_util
```

From ooh_code/Environments/OOH/Parcelpoint_py.py:
```python
# Constructor accepts: incentive_sens, base_util, home_util, ...
# Line 131: self.customerchoice = customerchoicemodel(
#     base_util, self.dist_scaler,
#     travel_time_weight=menu_travel_time_weight,
#     pickup_time_weight=menu_pickup_time_weight,
# )
```

From ooh_code/Src/config.py:
```python
# Line 128: return Parcelpoint_py(
#     incentive_sens=self.incentive_sens,
#     base_util=self.base_util,
#     home_util=self.home_util,
#     ...
# )
```

From ooh_code/Src/parser.py:
```python
# Lines 326-342: parser.add_argument("--incentive_sens", ...)
# parser.add_argument("--base_util", ...)
# parser.add_argument("--home_pickup_utility", ...)
```

From ooh_code/experiments/studies/phase27_mnl_sensitivity_low.yaml:
```yaml
# base_args contains: base_util, home_pickup_utility, incentive_sens,
#   menu_travel_time_weight, menu_pickup_time_weight
# splits: list of {id, train_split, test_split, args_overrides}
# ablations: list of variant definitions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize outside-option utility in customerchoice.py, Parcelpoint_py.py, parser.py, and config.py</name>
  <files>ooh_code/Environments/OOH/customerchoice.py, ooh_code/Environments/OOH/Parcelpoint_py.py, ooh_code/Src/parser.py, ooh_code/Src/config.py</files>
  <action>
read_first:
  - ooh_code/Environments/OOH/customerchoice.py (constructor and line 45)
  - ooh_code/Environments/OOH/Parcelpoint_py.py (constructor lines 20-46 and line 131-136)
  - ooh_code/Src/parser.py (lines 325-342 for adjacent arg definitions)
  - ooh_code/Src/config.py (lines 128-155 for build_environment Parcelpoint_py call)

Implementation changes (backward-compatible, default 0.0 preserves all existing studies):

1. **customerchoice.py**: Add `outside_option_util=0.0` parameter to `__init__`. Store as `self.outside_option_util`. Replace line 45 `utils[n_offers] = 0.0` with `utils[n_offers] = self.outside_option_util`.

2. **Parcelpoint_py.py**: Add `outside_option_util=0.0` to `__init__` parameter list (after `menu_pickup_time_weight=-0.0015`). Store as `self.outside_option_util`. Pass `outside_option_util=self.outside_option_util` to `customerchoicemodel()` constructor call at line 131.

3. **parser.py**: Add a new `parser.add_argument` for `--outside_option_util` after the `--home_pickup_utility` block (after line 342). Default `0.0`, type `float`, help `"Outside-option utility level in the MNL choice model. Default 0.0 normalizes the outside option; positive values make opting out more attractive."`.

4. **config.py**: Add `outside_option_util=self.outside_option_util` to the `Parcelpoint_py(...)` call in `build_environment()` (after line ~138 where `home_util=self.home_util` appears).
  </action>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/2.paper_2_menu optimization-6分（需降ai）/ooh_code" && python -c "from Environments.OOH.customerchoice import customerchoicemodel; m = customerchoicemodel(0.0, 10.0, outside_option_util=0.5); assert m.outside_option_util == 0.5, 'param not stored'; m0 = customerchoicemodel(0.0, 10.0); assert m0.outside_option_util == 0.0, 'default not 0.0'; print('PASS: outside_option_util parameterized correctly')"</automated>
  </verify>
  <done>
    - customerchoicemodel accepts outside_option_util parameter (default 0.0)
    - The hardcoded `utils[n_offers] = 0.0` line uses self.outside_option_util
    - CLI argument --outside_option_util flows through parser.py -> config.py -> Parcelpoint_py.py -> customerchoicemodel
    - All existing YAML studies work unchanged (default 0.0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create five outside-option scan YAML study files and a suite manifest</name>
  <files>ooh_code/experiments/studies/phase34_outside_option_scan_m10.yaml, ooh_code/experiments/studies/phase34_outside_option_scan_m05.yaml, ooh_code/experiments/studies/phase34_outside_option_scan_00.yaml, ooh_code/experiments/studies/phase34_outside_option_scan_p05.yaml, ooh_code/experiments/studies/phase34_outside_option_scan_p10.yaml, ooh_code/experiments/suites/phase34_outside_option_scan.yaml</files>
  <action>
read_first:
  - ooh_code/experiments/studies/phase27_mnl_sensitivity_low.yaml (full file for structure template)
  - ooh_code/experiments/suites/phase27_mnl_sensitivity.yaml (suite manifest pattern)

Create five study YAML files and one suite YAML. Each study follows the phase27_mnl_sensitivity_low.yaml structure exactly but adapted for outside-option scanning.

**Study naming convention:**
- `phase34_outside_option_scan_m10` (u_0 = -1.0)
- `phase34_outside_option_scan_m05` (u_0 = -0.5)
- `phase34_outside_option_scan_00` (u_0 = 0.0, baseline matching current default)
- `phase34_outside_option_scan_p05` (u_0 = +0.5)
- `phase34_outside_option_scan_p10` (u_0 = +1.0)

**Template for each study YAML (substitute NAME and U0_VALUE):**

```yaml
name: phase34_outside_option_scan_XX
title: Outside-Option Utility Scan - u_0 = VALUE
type: ablation
study_role: support
description: >
  Outside-option utility sensitivity scan at u_0 = VALUE. Tests how varying
  the outside-option attractiveness affects menu-optimization outcomes under
  the primary RC calibration. Three policies: strict-filter, no-filter, flat-markdown.
reference_tag: full_display
reference_label: full display
reference_policy: offer_all_feasible_bundles
baseline_variant:
  tag: strict_filter
  label: strict-filter heuristic
  policy: menu_optimization
  menu_k: 3
  args_overrides:
    menu_time_filtering: true
    menu_eta_filter_mode: hard
    menu_eta_variant: deployed
    menu_time_prediction_blend: 0.35
    menu_pricing_mode: lambertw
ablations:
  - tag: no_filter
    label: no-filter heuristic
    policy: menu_optimization
    menu_k: 3
    args_overrides:
      menu_time_filtering: false
      menu_pricing_mode: lambertw
  - tag: flat_markdown
    label: flat markdown pricing
    policy: menu_optimization
    menu_k: 3
    args_overrides:
      menu_time_filtering: true
      menu_eta_filter_mode: hard
      menu_eta_variant: deployed
      menu_time_prediction_blend: 0.35
      menu_pricing_mode: flat_markdown
      menu_pricing_constant: -3.0
behavior_gate:
  min_acceptance_rate: 0.02
  max_acceptance_rate: 0.60
  max_opt_out_rate: 0.98
base_args:
  experiment_name: phase34_outside_option_scan_XX
  instance: RC
  load_data: true
  save_model: true
  log_output: file
  debug: true
  seed: 1234
  max_episodes: 12
  eval_episodes: 6
  menu_k: 3
  base_util: -2.0
  home_pickup_utility: 3.2
  pref_window_half_width: 600
  incentive_sens: -0.25
  menu_travel_time_weight: -0.002
  menu_pickup_time_weight: -0.0015
  outside_option_util: VALUE
splits:
  - id: rc_0_to_1
    train_split: 0
    test_split: 1
  - id: rc_1_to_0
    train_split: 1
    test_split: 0
```

Use only 2 splits (rc_0_to_1, rc_1_to_0) to keep scan runtime manageable (per CONTEXT.md "primary RC only, focused scan").

**Suite YAML** at `ooh_code/experiments/suites/phase34_outside_option_scan.yaml`:

```yaml
name: phase34_outside_option_scan
title: Outside-Option Utility Scan Suite
type: suite
description: >
  Five-point outside-option utility scan (u_0 = -1.0, -0.5, 0.0, +0.5, +1.0)
  on the primary RC calibration with three pricing/filtering policies each.
members:
  - phase34_outside_option_scan_m10
  - phase34_outside_option_scan_m05
  - phase34_outside_option_scan_00
  - phase34_outside_option_scan_p05
  - phase34_outside_option_scan_p10
```
  </action>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/2.paper_2_menu optimization-6分（需降ai）/ooh_code" && python -c "
from pathlib import Path
studies_dir = Path('experiments/studies')
suites_dir = Path('experiments/suites')
expected_studies = [
    ('phase34_outside_option_scan_m10.yaml', -1.0),
    ('phase34_outside_option_scan_m05.yaml', -0.5),
    ('phase34_outside_option_scan_00.yaml', 0.0),
    ('phase34_outside_option_scan_p05.yaml', 0.5),
    ('phase34_outside_option_scan_p10.yaml', 1.0),
]
for fname, u0 in expected_studies:
    path = studies_dir / fname
    assert path.exists(), f'Missing study: {fname}'
    import yaml
    data = yaml.safe_load(path.read_text())
    assert data['base_args']['outside_option_util'] == u0, f'{fname}: outside_option_util={data[\"base_args\"][\"outside_option_util\"]}, expected {u0}'
    assert 'outside_option_util' not in [k for k in data.get('ablations', [{}])[0].get('args_overrides', {})], 'ablations should NOT override outside_option_util'
suite_path = suites_dir / 'phase34_outside_option_scan.yaml'
assert suite_path.exists(), 'Missing suite YAML'
suite = yaml.safe_load(suite_path.read_text())
assert len(suite['members']) == 5, f'Suite has {len(suite[\"members\"])} members, expected 5'
print('PASS: all 5 studies + suite validated')
"
    </automated>
  </verify>
  <done>
    - Five study YAML files exist with correct outside_option_util values (-1.0, -0.5, 0.0, +0.5, +1.0)
    - Each study uses the same 3-policy ablation structure (strict-filter, no-filter, flat-markdown)
    - Each study uses 2 RC splits for focused scan
    - Suite YAML groups all five studies
    - All YAML files parse cleanly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML args -> parser | Study YAML args_overrides flow into argparse namespace; unknown keys raise ValueError |
| MNL choice | Outside-option utility shift affects choice probabilities; extreme values may cause degenerate demand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | S | customerchoice.py | accept | No user input; simulator parameter only |
| T-02-02 | D | YAML studies | accept | Study manifests are internal config files, not user-facing |
</threat_model>

<verification>
1. customerchoicemodel constructor accepts outside_option_util parameter with default 0.0
2. The hardcoded outside-option line in customerchoice.py uses self.outside_option_util
3. CLI --outside_option_util flows through the full parser -> config -> env -> choice model chain
4. Five study YAML files exist with correct u_0 values
5. Suite YAML exists and lists all five studies
6. Python import test passes without errors
</verification>

<success_criteria>
- customerchoice.py uses self.outside_option_util instead of hardcoded 0.0
- All existing YAML studies work unchanged (default 0.0)
- Five new study YAMLs + one suite YAML ready for execution
- Unit-level verification passes (parameterization + YAML validation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-demand-calibration-and-outside-option-sensitivity/02-01-SUMMARY.md`
</output>
