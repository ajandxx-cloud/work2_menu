---
phase: 4
phase_name: Mainline Artifact Pipeline And Claim Guard
plan: 04-03-manuscript-frame-and-end-to-end-verification
type: implementation
wave: 3
depends_on:
  - phase: 4
    plan: 04-02-mainline-artifact-bundle-and-ranking
files_modified:
  - work2_coding/Src/manuscript_claims.py
  - work2_coding/scripts/build_manuscript_frame.py
  - work2_coding/scripts/test_artifact_gates.py
autonomous: true
requirements:
  - ART-02
  - ART-03
  - ART-04
  - RUN-04
must_haves:
  truths:
    - "D-02: Phase 4 generates manuscript frame and machine-readable claim guard from ARTIFACT_STATUS.json."
    - "D-03: Manuscript-frame outputs are generated artifacts only; do not edit manuscript source."
    - "D-04: Smoke outputs are diagnostic/status only."
    - "D-06: Formal claim-ready outputs require dependency snapshot and loaded checkpoint provenance."
    - "D-07: Claim guards block unsafe evidence categories."
    - "D-08: Checkpoint and dependency provenance are visible in status JSON and metadata."
---

<objective>
Make manuscript-frame and claim-guard generation mainline-aware, then verify the
Phase 4 path end to end from a smoke run through artifact bundle generation and
claim guard output.
</objective>

<tasks>

## Task 1 - Update Mainline Claim Frame Text

**Type:** code
**Files:**
- `work2_coding/Src/manuscript_claims.py`

**Action:**
1. Update generated method, experiment, result, and checklist language so it
   describes the seven-tag V1 mainline family instead of old robust-policy
   families where appropriate.
2. Keep always-allowed claims limited to implementation, auditability, paired
   replay contracts, and artifact transparency.
3. Keep empirical superiority, formal ranking, and completed formal evidence
   claims blocked unless artifact status is claim-ready.
4. Ensure diagnostic/no-filter operational recommendation remains blocked.

**Verify:**
```powershell
cd work2_coding
python scripts/test_artifact_gates.py
```

**Acceptance Criteria:**
- `CLAIM_GUARD.json` reflects current artifact status and source run metadata.
- Generated manuscript frame does not assert unsupported empirical superiority.
- No manuscript source file is modified.

## Task 2 - Test Manuscript Frame Generation From Artifact Status

**Type:** tests
**Files:**
- `work2_coding/scripts/test_artifact_gates.py`

**Action:**
1. Add or extend tests that build artifacts from synthetic mainline rows and then
   generate manuscript frame outputs.
2. Assert frame files include `CLAIM_GUARD.json`, `claim_checklist.md`,
   `method_outline.md`, `experiment_outline.md`, and `result_outline.md`.
3. Assert smoke frame is not claim-ready.
4. Assert pilot claim-ready frame allows pilot/formal effect-size conditional
   claims only when artifact status passes.
5. Assert blocked claims include unsupported empirical superiority when status
   is not claim-ready.

**Verify:**
```powershell
cd work2_coding
python scripts/test_artifact_gates.py
```

**Acceptance Criteria:**
- Tests prove manuscript frame generation consumes `ARTIFACT_STATUS.json`.
- Tests do not require formal replay.

## Task 3 - Run Phase 4 End-To-End Verification

**Type:** validation
**Files:**
- generated local output under `work2_coding/outputs/phase4_verification/`
- generated artifact output under `work2_coding/outputs/phase4_artifacts/`

**Action:**
1. Run import smoke.
2. Run Phase 4-relevant script tests.
3. Run or reuse a smoke actual replay.
4. Build artifacts with `--allow-incomplete` from the smoke run directory.
5. Generate manuscript frame from the built artifact root.
6. Inspect `ARTIFACT_STATUS.json` and `CLAIM_GUARD.json`.
7. Record Phase 4 verification in `04-VERIFICATION.md` and close with
   `04-SUMMARY.md`.

**Verify:**
```powershell
cd work2_coding
python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"
python scripts/test_artifact_gates.py
python scripts/test_smoke_study_rows.py
python scripts/test_experiment_contracts.py
python scripts/run_study.py --study smoke_robust_menu --execute --output-root outputs/phase4_verification
python scripts/build_artifacts.py --run-dir <phase4 smoke run dir> --output-root outputs/phase4_artifacts --allow-incomplete
python scripts/build_manuscript_frame.py --artifact-root outputs/phase4_artifacts
```

**Acceptance Criteria:**
- End-to-end smoke artifact build completes.
- Smoke artifact status is diagnostic/status, not claim-ready.
- `recommended_policy_ranking.json` excludes `mainline_no_menu`.
- Baseline/boundary output includes `mainline_no_menu`.
- `CLAIM_GUARD.json` exists and blocks unsupported empirical/formal claims.
- No manuscript source, formal rows, or generated paper result rows are hand-edited.

</tasks>

<verification>

Run from `work2_coding/`:

```powershell
python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"
python scripts/test_artifact_gates.py
python scripts/test_smoke_study_rows.py
python scripts/test_experiment_contracts.py
python scripts/run_study.py --study smoke_robust_menu --execute --output-root outputs/phase4_verification
python scripts/build_artifacts.py --run-dir <phase4 smoke run dir> --output-root outputs/phase4_artifacts --allow-incomplete
python scripts/build_manuscript_frame.py --artifact-root outputs/phase4_artifacts
```

</verification>

<success_criteria>

- Manuscript-frame outputs are generated from artifact status.
- Claim guard language is mainline-aware and conservative.
- End-to-end Phase 4 smoke artifact verification passes.
- Phase 4 verification and summary docs are written.
- No manuscript source or generated result row is hand-edited.

</success_criteria>

## PLANNING COMPLETE
