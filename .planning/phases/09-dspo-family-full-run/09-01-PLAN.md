---
phase: 09-dspo-family-full-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - work2_coding/Src/policy_adapters.py
  - work2_coding/Experiments/studies/phase9_dspo_family_validation.yaml
  - work2_coding/scripts/test_experiment_contracts.py
  - work2_coding/scripts/test_policy_fairness_contract.py
  - work2_coding/scripts/test_method_family_contract.py
autonomous: true
requirements:
  - EXP-04
  - GATE-01
  - GATE-02
must_haves:
  truths:
    - "Phase 9 exposes exactly two DSPO policy tags: dspo_clip and dspo_wide."
    - "Both Phase 9 tags resolve to method_family=DSPO and comparison_role=dspo_family."
    - "Phase 9 manifest reuses the five Phase 8 formal-equivalent split settings and contains no baseline or DSPO_PLUS policies."
    - "D-01: Add explicit Phase 9 policy tags dspo_clip and dspo_wide."
    - "D-02: Both Phase 9 tags emit method_family=DSPO."
    - "D-03: Use policy_tag plus comparison_role=dspo_family for DSPO clip/wide identity."
    - "D-04: Define clip/wide by DSPO service-risk thresholds, not time-window, candidate scope, or DSPO_PLUS semantics."
    - "D-05: dspo_clip uses threshold 0.35."
    - "D-06: dspo_wide uses threshold 0.45."
    - "D-07: Phase 9 tags do not inherit, compare to, or mention DSPO_PLUS parameters or penalties."
    - "D-08: Phase 9 primary manifest contains only dspo_clip and dspo_wide."
    - "D-09: Phase 9 primary manifest does not rerun Phase 8 baselines."
    - "D-10: Phase 9 reuses the five Phase 8 split settings exactly."
    - "D-18: Phase 9 uses the Phase 8 lightweight formal-equivalent runtime budget."
    - "D-19: Phase 9 keeps Phase 8 menu and candidate parameters."
    - "D-20: Phase 9 keeps Phase 8 HGS and replay runtime settings unless incompatibility is documented."
    - "D-21: Phase 9 requires loaded checkpoint path/hash/status provenance."
    - "D-27: DSPO_PLUS is stale residue and excluded from Phase 9 adapter/manifest scope."
  artifacts:
    - path: "work2_coding/Src/policy_adapters.py"
      provides: "DSPO-only dspo_clip and dspo_wide adapter tags"
      contains: "dspo_clip"
    - path: "work2_coding/Experiments/studies/phase9_dspo_family_validation.yaml"
      provides: "Phase 9 paired replay manifest"
      contains: "required_policy_tags"
    - path: "work2_coding/scripts/test_experiment_contracts.py"
      provides: "Manifest contract checks"
      contains: "phase9_dspo_family_validation"
  key_links:
    - from: "work2_coding/Experiments/studies/phase9_dspo_family_validation.yaml"
      to: "work2_coding/Src/policy_adapters.py"
      via: "policy tag resolution"
      pattern: "dspo_(clip|wide)"
    - from: "work2_coding/scripts/test_policy_fairness_contract.py"
      to: "work2_coding/Src/paired_replay.py"
      via: "resolve_paired_settings"
      pattern: "resolve_paired_settings"
---

<objective>
Create the executable Phase 9 DSPO clip/wide contract.

Purpose: Phase 9 cannot run until the paper-facing DSPO tags and manifest encode the locked DSPO-only semantics from D-01 through D-23.
Output: Adapter tags, Phase 9 study manifest, and focused contract tests that prove the manifest is paired, DSPO-only, and Phase 8-compatible.
</objective>

<execution_context>
@C:/Users/39583/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/39583/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/repository_audit.md
@.planning/phases/09-dspo-family-full-run/09-CONTEXT.md
@.planning/phases/09-dspo-family-full-run/09-RESEARCH.md
@.planning/phases/08-baseline-validation/08-SUMMARY.md
@.planning/phases/08-baseline-validation/08-VERIFICATION.md
@work2_coding/Src/policy_adapters.py
@work2_coding/Src/paired_replay.py
@work2_coding/Experiments/studies/phase8_baseline_validation.yaml
@work2_coding/scripts/test_experiment_contracts.py
@work2_coding/scripts/test_policy_fairness_contract.py
@work2_coding/scripts/test_method_family_contract.py

<interfaces>
Existing adapter API:
- `OPTIONAL_POLICY_TAGS` lists optional policy tags accepted by manifests.
- `POLICY_ADAPTERS` maps tag strings to metadata and parser overrides.
- `POLICY_ONLY_FIELDS` restricts policy-level overrides; add no non-policy fields.
- `adapter_overrides(tag)` and `adapter_metadata(tag)` are the public test/runtime readers.
- `METHOD_FAMILIES` currently permits `DSPO`, `DSPO_PLUS`, and `diagnostic`; Phase 9 tags must use `DSPO`.

Existing paired replay API:
- `resolve_paired_settings(manifest, manifest_hash_value=None)` merges split and policy settings and validates paired fields against varied fields.
- `build_normalized_row(...)` emits `normalized-row-v2` rows with `policy_tag`, `method_family`, `comparison_role`, checkpoint fields, status fields, and opt-out/home/meeting-point accounting fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 9 manifest and adapter contract tests</name>
  <files>work2_coding/scripts/test_experiment_contracts.py, work2_coding/scripts/test_policy_fairness_contract.py, work2_coding/scripts/test_method_family_contract.py</files>
  <behavior>
    - Test: `load_manifest("phase9_dspo_family_validation")` succeeds and has `required_policy_tags == ["dspo_clip", "dspo_wide"]`.
    - Test: the manifest has exactly five splits whose `seed`, `data_seed`, `data_seed_test`, `uptake_regime`, `home_util`, `base_util`, and `incentive_sens` values match `phase8_baseline_validation.yaml` per D-10.
    - Test: the manifest policy tags are exactly `dspo_clip` and `dspo_wide`; no Phase 8 baseline tag and no `dspo_plus_*` tag appears per D-08, D-09, and D-27.
    - Test: Phase 9 method-family coverage validates only DSPO clip/wide semantics; DSPO_PLUS may appear only as explicitly excluded stale residue per D-27.
    - Test: `dspo_clip` resolves to `service_quit_rate_guardrail=0.35`, `menu_optout_guardrail=0.35`, `method_family=DSPO`, `comparison_role=dspo_family`, and `attention_enabled=False` per D-02 through D-07.
    - Test: `dspo_wide` resolves to `service_quit_rate_guardrail=0.45`, `menu_optout_guardrail=0.45`, `method_family=DSPO`, `comparison_role=dspo_family`, and `attention_enabled=False` per D-02 through D-07.
    - Test: the Phase 9 manifest declares `shared_checkpoint.required=true`, `shared_checkpoint.expected_status=loaded`, `base_args.require_checkpoint=true`, `base_args.allow_checkpoint_mismatch=false`, and the same Phase 8 checkpoint path `outputs/shared_training/work2_robust_menu/formal/supervised_ml.pt` per D-21.
    - Test: `resolve_paired_settings` returns ten settings, with only the DSPO threshold fields and policy identity fields differing within each split per D-10, D-18, D-19, and D-20.
  </behavior>
  <action>Add focused assertions to the existing script-style tests before adding production adapters or the manifest. Revise `test_method_family_contract.py` so the Phase 9 command path validates DSPO clip/wide semantics and DSPO_PLUS exclusion only; it must not positively validate DSPO_PLUS tags, inherit DSPO_PLUS contracts, or compare against DSPO_PLUS per D-07 and D-27. Do not perform broad documentation cleanup of older DSPO_PLUS planning residue in this plan.</action>
  <verify>
    <automated>cd work2_coding; python scripts/test_experiment_contracts.py; python scripts/test_policy_fairness_contract.py; python scripts/test_method_family_contract.py</automated>
  </verify>
  <done>The new tests fail before Task 2 because `dspo_clip`, `dspo_wide`, and the Phase 9 manifest do not exist, and they express all locked Phase 9 contract decisions listed in the behavior block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DSPO clip/wide adapters and manifest</name>
  <files>work2_coding/Src/policy_adapters.py, work2_coding/Experiments/studies/phase9_dspo_family_validation.yaml</files>
  <behavior>
    - `dspo_clip` and `dspo_wide` are optional tags and known policy tags.
    - Both tags use `method_family=DSPO`, `method_variant=DSPO_original`, `comparison_role=dspo_family`, `menu_contract_mode=optimized_menu`, `product_mode=m+w+p`, `time_window_mode=adaptive_window`, `menu_pricing_mode=lambertw`, and `attention_enabled=False`.
    - `dspo_clip` uses service-risk and opt-out guardrail threshold `0.35`; `dspo_wide` uses `0.45`.
    - The Phase 9 manifest keeps the Phase 8 checkpoint contract concrete: `shared_checkpoint.required=true`, `shared_checkpoint.expected_status=loaded`, `base_args.checkpoint_path=outputs/shared_training/work2_robust_menu/formal/supervised_ml.pt`, `base_args.require_checkpoint=true`, and `base_args.allow_checkpoint_mismatch=false`.
    - The Phase 9 manifest contains only the two DSPO policies and copies Phase 8's five split settings and lightweight runtime budget: `menu_k=3`, `max_candidates=8`, `menu_exact_threshold=8`, `menu_exact_gap_threshold=8`, `hgs_reopt_time=0.1`, `hgs_final_time=0.1`, `max_episodes=1`, `max_steps_r=20`, `max_steps_p=0.7`, `n_vehicles=2`, and `veh_capacity=3`.
  </behavior>
  <action>Add `dspo_clip` and `dspo_wide` to `OPTIONAL_POLICY_TAGS` and `POLICY_ADAPTERS` per D-01 through D-07. Do not copy `dspo_plus_contract`, `method_family=DSPO_PLUS`, `service_quit_penalty`, or `menu_outside_penalty_lambda` from DSPO_PLUS tags per D-07 and D-27. Create `phase9_dspo_family_validation.yaml` from the Phase 8 manifest shape, preserving the five split values per D-10 and keeping baselines out per D-08 and D-09. Preserve the Phase 8 checkpoint contract explicitly with shared checkpoint required/loaded status and `base_args.require_checkpoint=true`; do not weaken it to a path-only reference. Include `service_quit_rate_guardrail` and `menu_optout_guardrail` in `varied_fields` because those define clip versus wide per D-04 through D-06.</action>
  <verify>
    <automated>cd work2_coding; python scripts/test_experiment_contracts.py; python scripts/test_policy_fairness_contract.py; python scripts/test_method_family_contract.py</automated>
  </verify>
  <done>All three contract scripts pass; the Phase 9 manifest resolves to exactly ten paired settings and no Phase 9 manifest, adapter metadata, or test assertion validates DSPO_PLUS as part of Phase 9.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 9 | Run and gate DSPO clip/wide under Phase 8 paired replay contract | 09-01, 09-02, 09-03 | COVERED | This plan creates the runnable DSPO manifest and tags.
REQ | EXP-04 | DSPO clip/wide executable under paired replay | 09-01, 09-03 | COVERED | Adapter and manifest prerequisites.
REQ | GATE-01 | Explicit checkpoint status in rows/metadata | 09-01, 09-02, 09-03 | COVERED | Manifest requires the Phase 8 checkpoint contract.
REQ | GATE-02 | Exclude failed/blocked/diagnostic/no-filter rows from claims | 09-02, 09-03 | COVERED | This plan avoids diagnostic/no-filter/baseline rows in the Phase 9 manifest.
REQ | GATE-04 | Failures report reason, minimal fix, rerun instruction | 09-02, 09-03 | COVERED | Validator/report plans provide failure records.
RESEARCH | Adapter-driven policy identity | Add DSPO tags in `policy_adapters.py` | 09-01 | COVERED | Mirrors research recommendation.
RESEARCH | Manifest copies splits, not baselines | Add Phase 9 manifest from Phase 8 split/budget source | 09-01 | COVERED | Baselines excluded per locked decisions.
CONTEXT | D-01..D-10, D-18..D-21, D-27 | DSPO-only tags, thresholds, split reuse, runtime/checkpoint contract, DSPO_PLUS exclusion | 09-01 | COVERED | Task actions reference specific decisions.
CONTEXT | Deferred Ideas | DSPO_PLUS cleanup and manuscript claims | NONE | EXCLUDED | Deferred by context; do not implement here.
</source_audit>

<decision_coverage>
- D-01: Add explicit `dspo_clip` and `dspo_wide` tags in `policy_adapters.py`.
- D-02: Require both Phase 9 tags to emit `method_family=DSPO`.
- D-03: Require `policy_tag` plus `comparison_role=dspo_family` for clip/wide identity.
- D-04: Define clip/wide by DSPO service-risk thresholds, not time-window mode, candidate scope, or DSPO_PLUS semantics.
- D-05: Set `dspo_clip` threshold to `0.35`.
- D-06: Set `dspo_wide` threshold to `0.45`.
- D-07: Exclude DSPO_PLUS parameters, penalties, inheritance, and comparisons from Phase 9 adapters and tests.
- D-08: Keep the Phase 9 primary manifest to exactly `dspo_clip` and `dspo_wide`.
- D-09: Do not rerun Phase 8 no-pricing or static-pricing baselines inside the Phase 9 primary manifest.
- D-10: Copy the five Phase 8 split settings exactly, including seeds, data seeds, uptake regime, and utility parameters.
- D-18: Use the Phase 8 lightweight formal-equivalent runtime budget.
- D-19: Keep Phase 8 menu and candidate parameters, including `menu_k=3`, `max_candidates=8`, `menu_exact_threshold=8`, and `menu_exact_gap_threshold=8`.
- D-20: Keep Phase 8 HGS and replay runtime settings unless a documented incompatibility is discovered.
- D-21: Preserve the Phase 8 loaded-checkpoint contract with required checkpoint path/hash/status metadata.
- D-27: Treat DSPO_PLUS references as stale residue and exclude them from Phase 9 adapter/manifest scope.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML manifest -> parser/runtime | Manifest values become executable parser settings. |
| Policy adapter metadata -> row/report gates | Adapter metadata controls method family and comparison role used by gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `phase9_dspo_family_validation.yaml` | mitigate | Contract tests require exact two-policy manifest, five Phase 8 split values, and shared checkpoint settings. |
| T-09-02 | Spoofing | `policy_adapters.py` method metadata | mitigate | Method-family tests require `method_family=DSPO` and no DSPO_PLUS contract for Phase 9 tags. |
| T-09-SC | Tampering | package installs | accept | No package-manager installs are planned in this phase. |
</threat_model>

<verification>
From `work2_coding/` run:
- `python scripts/test_experiment_contracts.py`
- `python scripts/test_policy_fairness_contract.py`
- `python scripts/test_method_family_contract.py`
</verification>

<success_criteria>
- `dspo_clip` and `dspo_wide` are available policy tags with DSPO-only metadata.
- `phase9_dspo_family_validation.yaml` contains only those two tags and five Phase 8-equivalent splits.
- Contract tests prove Phase 9 has no baseline rerun, no DSPO_PLUS validation, and no paired replay drift beyond declared DSPO threshold fields.
</success_criteria>

<output>
Create `.planning/phases/09-dspo-family-full-run/09-01-SUMMARY.md` when done.
</output>
