---
phase: 09-dspo-family-full-run
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - work2_coding/Src/dspo_validation.py
  - work2_coding/scripts/build_phase9_dspo_family_validation_report.py
  - work2_coding/scripts/test_phase9_dspo_family_validation.py
autonomous: true
requirements:
  - EXP-04
  - GATE-01
  - GATE-02
  - GATE-04
must_haves:
  truths:
    - "Phase 9 validation blocks any failed, blocked, incomplete, placeholder-only, contract-only, bad-checkpoint, drifted, or bad-accounting DSPO row."
    - "Every Phase 9 failure includes reason, minimal fix, rerun command, and evidence location."
    - "Phase 9 report separates DSPO validation status from claim-ready status and ranking claims."
    - "D-11: Phase 9 report records latest passed Phase 8 report/run ID as cross-run status context."
    - "D-12: Phase 8 sanity comparison is status-only, not same-run formal ranking evidence."
    - "D-13: Phase 9 blocks failed, blocked, incomplete, placeholder-only, or contract-only DSPO rows."
    - "D-14: Phase 9 blocks bad checkpoint status, paired drift, missing row-v2 fields, provenance anomalies, and accounting errors."
    - "D-15: Phase 9 failures include reason, minimal_fix, and rerun_command."
    - "D-16: A passed Phase 9 unlocks DSPO result organization/status language only, not ranking claims."
    - "D-17: A blocked Phase 9 report provides debug handoff and does not auto-repair/rerun."
    - "D-22: Phase 9 keeps claim_ready separate from DSPO validation."
    - "D-23: Phase 9 writes only JSON and Markdown validation reports, no artifact bundle."
    - "D-24: Phase 9 uses gate-first report fields including dspo_validation_status, phase9_gate, claim_ready, Phase 8 status, and sanity summary."
    - "D-25: Phase 9 report says sanity comparison is status-only and not a manuscript ranking conclusion."
    - "D-26: Phase 9 may pass even when sanity status does not support a DSPO advantage conclusion."
    - "D-27: Phase 9 report says DSPO_PLUS is unrelated/stale and not inherited, compared, or validated."
    - "D-28: Phase 9 report includes one next step: debug handoff or Phase 11 status/risk language only."
  artifacts:
    - path: "work2_coding/Src/dspo_validation.py"
      provides: "Phase 9 DSPO row validator and report formatter"
      exports: ["validate_phase9_dspo_rows", "write_phase9_dspo_family_validation_report"]
    - path: "work2_coding/scripts/build_phase9_dspo_family_validation_report.py"
      provides: "CLI wrapper for JSON/Markdown Phase 9 reports"
    - path: "work2_coding/scripts/test_phase9_dspo_family_validation.py"
      provides: "Focused Phase 9 gate tests"
  key_links:
    - from: "work2_coding/Src/dspo_validation.py"
      to: "work2_coding/Src/artifact_status.py"
      via: "classify_artifact"
      pattern: "classify_artifact"
    - from: "work2_coding/scripts/build_phase9_dspo_family_validation_report.py"
      to: "work2_coding/Src/dspo_validation.py"
      via: "report CLI"
      pattern: "write_phase9_dspo_family_validation_report"
---

<objective>
Create the Phase 9 DSPO validation gate and report writer.

Purpose: Running DSPO rows is not sufficient; Phase 9 must fail closed on row, checkpoint, pairing, accounting, provenance, and report-language violations while keeping ranking claims locked.
Output: A Phase 8-style DSPO validator, report CLI, and script tests for the Phase 9 gate.
</objective>

<execution_context>
@C:/Users/39583/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/39583/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dspo-family-full-run/09-CONTEXT.md
@.planning/phases/09-dspo-family-full-run/09-RESEARCH.md
@.planning/phases/08-baseline-validation/08-SUMMARY.md
@.planning/phases/08-baseline-validation/08-VERIFICATION.md
@work2_coding/Src/baseline_validation.py
@work2_coding/Src/artifact_status.py
@work2_coding/Src/paired_replay.py
@work2_coding/Src/study_execution.py
@work2_coding/scripts/test_phase8_baseline_validation.py

<interfaces>
Phase 8 validation pattern:
- `baseline_validation._blocker(reason, minimal_fix, evidence_location, code, rerun_command=...)` returns failure dictionaries with the required repair fields.
- `validate_phase8_baseline_rows(rows, manifest=None, study_summary=None, dependency_snapshot=None)` checks status, checkpoint, row fields, pairing, accounting, and artifact classification.
- `write_phase8_baseline_validation_report(output_root=None, run_dir=None, studies_root=None)` discovers the latest run, writes JSON and Markdown, and returns the report dictionary.

Artifact status API:
- `classify_artifact(rows, summary=None, claim_ready_requested=False, dependency_snapshot=None)` returns `status`, `claim_ready`, `reasons`, row statuses, checkpoint statuses, method families, and blockers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 9 gate tests</name>
  <files>work2_coding/scripts/test_phase9_dspo_family_validation.py</files>
  <behavior>
    - Test: synthetic completed rows for `dspo_clip` and `dspo_wide` across five splits pass `dspo_validation_status=passed`, set `phase9_gate=open`, and keep `claim_ready` separate per D-13, D-16, and D-22.
    - Test: missing clip or wide row in any split blocks with `missing_dspo_pair`.
    - Test: failed, blocked, incomplete, placeholder-only, or contract-only rows block per D-13.
    - Test: checkpoint status other than `loaded`, missing checkpoint path/hash, missing row-v2 provenance fields, paired drift, and opt-out/home/meeting accounting errors block per D-14.
    - Test: every failure has `reason`, `minimal_fix`, `rerun_command`, and `evidence_location` per D-15 and GATE-04.
    - Test: report output includes `dspo_validation_status`, `phase9_gate`, `claim_ready`, `phase8_reference_run_id`, `phase8_reference_status`, `sanity_status`, `next_step`, and explicit DSPO_PLUS exclusion per D-24 through D-28.
    - Test: if DSPO execution gates pass but sanity comparison does not support DSPO advantage over Phase 8 baselines, `dspo_validation_status` remains allowed to pass while `sanity_status` states no advantage conclusion and `next_step` forbids ranking-claim language per D-26 and D-28.
    - Test: Markdown report states sanity comparison is status-only and not a manuscript ranking conclusion per D-12 and D-25.
  </behavior>
  <action>Create a new script-style test file following `test_phase8_baseline_validation.py`. Build synthetic rows through the Phase 9 manifest and `build_normalized_row` rather than hand-writing partial row dictionaries, so row-v2 validation remains active. The tests should fail until Task 2 creates `Src.dspo_validation` and the CLI/report implementation.</action>
  <verify>
    <automated>cd work2_coding; python scripts/test_phase9_dspo_family_validation.py</automated>
  </verify>
  <done>The new test script encodes EXP-04, GATE-01, GATE-02, and GATE-04 behavior and initially fails only because the Phase 9 validation module is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DSPO validator and report CLI</name>
  <files>work2_coding/Src/dspo_validation.py, work2_coding/scripts/build_phase9_dspo_family_validation_report.py</files>
  <behavior>
    - `validate_phase9_dspo_rows` filters main rows to `dspo_clip` and `dspo_wide`, requires five paired splits, and blocks extra Phase 9 policies including baselines and DSPO_PLUS per D-08, D-09, and D-27.
    - It requires `status=completed`, `execution_status=completed`, `placeholder_only=False`, `method_family=DSPO`, `comparison_role=dspo_family`, loaded checkpoint path/hash/status, `manifest_hash`, `settings_hash`, `trace_id`, `trace_hash`, and row-v2 accounting fields per D-13 and D-14.
    - It checks paired fields between clip and wide within each split: run ID, seed, data seeds, trace ID/hash, manifest hash, checkpoint metadata, HGS settings, menu/candidate budget, and Phase 8 uptake utility fields must match; DSPO threshold fields may differ per D-04 through D-06.
    - It calls `classify_artifact` for claim-ready separation but does not invoke artifact building or claim-ready bundle generation per D-22 and D-23.
    - It loads the latest Phase 8 report when available and records it as status context only per D-11 and D-12.
  </behavior>
  <action>Create `Src/dspo_validation.py` as a sibling of `baseline_validation.py`, reusing Phase 8 helper shape but renaming report fields to `dspo_validation_status`, `phase9_gate`, `claim_ready`, `claim_ready_status`, `claim_ready_reasons`, `phase8_reference_run_id`, `phase8_reference_status`, `sanity_status`, and `next_step` per D-24. Implement D-26 so sanity comparison can report no DSPO advantage without blocking an otherwise valid Phase 9 run, and implement D-28 so the next step is debug handoff when blocked or Phase 11 status/risk language only when passed without advantage. Use report constants `DEFAULT_STUDY_NAME="phase9_dspo_family_validation"`, `REPORT_JSON="PHASE9_DSPO_FAMILY_VALIDATION.json"`, and `REPORT_MD="PHASE9_DSPO_FAMILY_VALIDATION.md"`. Create the CLI wrapper that accepts `--output-root`, optional `--run-dir`, optional `--studies-root`, and optional `--phase8-report`, then writes only the JSON and Markdown Phase 9 reports per D-23.</action>
  <verify>
    <automated>cd work2_coding; python scripts/test_phase9_dspo_family_validation.py</automated>
  </verify>
  <done>The Phase 9 focused test script passes, generated report dictionaries contain all gate-first fields, and blocked reports include debug handoff details without attempting automatic repair or rerun.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 9 | Gate DSPO clip/wide under Phase 8 paired replay contract | 09-02, 09-03 | COVERED | This plan creates the hard validation gate.
REQ | EXP-04 | DSPO clip/wide executable under paired replay | 09-01, 09-03 | COVERED | Validator consumes actual paired rows.
REQ | GATE-01 | Checkpoint load status explicit | 09-02 | COVERED | Tests and validator require loaded checkpoint status/path/hash.
REQ | GATE-02 | Placeholder/blocked/diagnostic/no-filter rows excluded from claims | 09-02 | COVERED | Validator blocks bad rows and uses artifact classification for claim separation.
REQ | GATE-04 | Failure reason, minimal fix, rerun instruction | 09-02 | COVERED | Every blocker has required repair fields.
RESEARCH | Validator mirrors Phase 8 but renames fields | Create `dspo_validation.py` and report CLI | 09-02 | COVERED | Mirrors `baseline_validation.py` without mixing semantics.
CONTEXT | D-11..D-17, D-22..D-28 | Phase 8 reference, failure gate, claim separation, JSON/Markdown reports, DSPO_PLUS exclusion, next step | 09-02 | COVERED | Task behavior names each decision group.
CONTEXT | Deferred Ideas | Manuscript claims and DSPO_PLUS cleanup | NONE | EXCLUDED | Reports may mention exclusion only; no cleanup or claims.
</source_audit>

<decision_coverage>
- D-11: Load and record the latest passed Phase 8 validation report/run ID as cross-run status context when available.
- D-12: Label every Phase 8 sanity comparison as status-only, not same-run formal ranking evidence.
- D-13: Block failed, blocked, incomplete, placeholder-only, or contract-only DSPO rows.
- D-14: Block bad checkpoint status, paired drift, missing row-v2 fields, provenance anomalies, and opt-out/home/meeting accounting errors.
- D-15: Require every failed row or split to include `reason`, `minimal_fix`, and `rerun_command`.
- D-16: State that a passed Phase 9 unlocks DSPO result organization/status language only, not DSPO-over-baseline claims.
- D-17: Produce a debug-ready blocked report and do not automatically repair/rerun failed Phase 9 outputs.
- D-22: Keep `claim_ready` separate from DSPO validation status.
- D-23: Write only Phase 9 JSON and Markdown validation reports; do not generate an artifact bundle.
- D-24: Use gate-first fields including `dspo_validation_status`, `phase9_gate`, `claim_ready`, Phase 8 reference status, and sanity summary.
- D-25: Explicitly state sanity comparison is status-only and not a manuscript ranking conclusion.
- D-26: Allow Phase 9 validation to pass even if sanity status does not support a DSPO advantage conclusion.
- D-27: State that DSPO_PLUS is unrelated/stale for Phase 9 and is not inherited, compared, or validated.
- D-28: Include one next step: debug handoff if blocked, or Phase 11 status/risk language only if passed without advantage.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated rows -> Phase 9 report | Local generated normalized rows determine gate status. |
| Phase 8 report -> Phase 9 status context | Prior baseline report is referenced but not treated as same-run ranking evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | `normalized_rows.json` | mitigate | Validator blocks missing row-v2 fields, bad status, placeholder rows, checkpoint anomalies, paired drift, and accounting errors. |
| T-09-04 | Repudiation | Phase 9 failure reports | mitigate | Every blocker includes reason, minimal fix, rerun command, and evidence location. |
| T-09-05 | Information Disclosure | Markdown/JSON reports | accept | Reports contain local experiment provenance only; no secrets or user data are introduced. |
| T-09-SC | Tampering | package installs | accept | No package-manager installs are planned in this phase. |
</threat_model>

<verification>
From `work2_coding/` run:
- `python scripts/test_phase9_dspo_family_validation.py`
- `python scripts/test_artifact_gates.py`
- `python scripts/test_checkpoint_provenance.py`
- `python scripts/test_optout_accounting.py`
</verification>

<success_criteria>
- Phase 9 validation passes only for ten completed DSPO rows across five paired splits.
- Bad rows, bad checkpoint provenance, paired drift, missing row-v2 fields, and accounting errors all block with actionable failure records.
- Reports are JSON plus Markdown only and explicitly avoid artifact-bundle generation and ranking-claim language.
</success_criteria>

<output>
Create `.planning/phases/09-dspo-family-full-run/09-02-SUMMARY.md` when done.
</output>
