# Phase 12 Plan: Formal Actual Replay Enablement

**Status:** Planned
**Created:** 2026-06-11
**Mode:** MVP

## Goal

Enable strict-gated formal actual replay without permitting formal placeholder rows or random-weight evidence.

## Requirements

- FORM-01: old unconditional formal actual replay block is replaced by strict gated execution.
- FORM-02: formal execution requires loaded checkpoint provenance and non-placeholder rows.
- FORM-03: missing checkpoint still fails closed with blocker metadata.
- FORM-04: formal placeholder rows are impossible.
- FORM-05: formal contract, missing-checkpoint, loaded-checkpoint, and placeholder-impossibility tests exist.

## Work Items

1. Update formal execution control.
   - Remove unconditional formal raise from actual row execution.
   - Include formal studies in suite execution when actual execution is requested.

2. Add focused tests.
   - Test formal contract-only refusal.
   - Test missing formal checkpoint writes blockers.
   - Test tiny formal actual run completes with loaded checkpoint.
   - Test formal placeholder row validation still rejects.

3. Verify.
   - Run new formal replay tests.
   - Run existing study execution, checkpoint provenance, attention manifest, and attention smoke tests.

## Acceptance Criteria

- Formal actual replay can complete only when strict gates are satisfied.
- Formal placeholder rows remain impossible.
- Missing checkpoint behavior remains fail-closed.
- This phase does not override Phase 11's claim stop.

