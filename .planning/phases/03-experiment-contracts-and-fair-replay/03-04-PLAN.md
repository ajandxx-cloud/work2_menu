---
phase: 03-experiment-contracts-and-fair-replay
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02-PLAN.md
  - 03-03-PLAN.md
files_modified:
  - work2_coding/scripts/run_study.py
  - work2_coding/Src/paired_replay.py
  - work2_coding/Src/experiment_contracts.py
  - work2_coding/scripts/test_smoke_study_rows.py
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
user_setup: []
must_haves:
  truths:
    - "D-02: Contract-first, small runnable smoke before formal evidence."
    - "D-04: Smoke must be cheap and runnable."
    - "D-08/D-09: Smoke rows preserve paired replay and trace provenance."
    - "D-12: Rows carry checkpoint/filter/solver/opt-out/acceptance/provenance fields."
    - "D-14: Smoke rows may be diagnostic or incomplete only when explicit; formal rows cannot be placeholder-only."
  artifacts:
    - path: "work2_coding/scripts/test_smoke_study_rows.py"
      provides: "End-to-end smoke runner and normalized row output tests"
      min_lines: 100
      contains: "normalized_rows"
  key_links:
    - from: "work2_coding/scripts/run_study.py"
      to: "work2_coding/outputs/studies"
      via: "Public smoke study runner emits normalized rows"
      pattern: "normalized_rows"
---

<objective>
Add the public Phase 3 smoke study runner and verify it emits normalized rows through the same manifest/pairing/schema path intended for later pilot/formal studies.

Purpose: prove the experiment contract is runnable end-to-end at smoke scale while keeping formal evidence generation deferred to Phase 4.

Output: `scripts/run_study.py`, smoke row outputs, and end-to-end script tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-experiment-contracts-and-fair-replay/03-CONTEXT.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-RESEARCH.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-01-PLAN.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-02-PLAN.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-03-PLAN.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-02-SUMMARY.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-03-SUMMARY.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-04-SUMMARY.md

@work2_coding/Src/experiment_contracts.py
@work2_coding/Src/paired_replay.py
@work2_coding/Src/policy_adapters.py
@work2_coding/run.py
@work2_coding/Src/config.py
@work2_coding/Environments/OOH/Parcelpoint_py.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public study runner CLI</name>
  <files>work2_coding/scripts/run_study.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/Src/paired_replay.py, work2_coding/Src/policy_adapters.py, work2_coding/run.py</read_first>
  <action>Create a CLI that accepts `--study`, `--suite`, `--output-root`, `--contract-only`, and `--max-policies` or equivalent smoke controls. It should load and validate manifests, resolve paired settings, compute manifest/settings hashes, create a run directory under `work2_coding/outputs/studies/<study>/<run_id>/`, write `manifest_snapshot.yaml`, `study_summary.json`, `normalized_rows.json`, and `normalized_rows.csv`, and print the run directory. The default smoke path should exercise row construction and public runner logic even if expensive training/evaluation is skipped or shortened.</action>
  <verify>`cd work2_coding; python scripts/run_study.py --study smoke_robust_menu --contract-only` exits 0 and writes normalized rows.</verify>
  <acceptance_criteria>
    - Runner uses manifest validation before writing rows.
    - Runner writes manifest snapshot, summary, JSON rows, and CSV rows.
    - Runner marks contract-only or diagnostic smoke status explicitly.
    - Runner does not write paper artifacts or manuscript files.
  </acceptance_criteria>
  <done>Smoke studies have a public command that emits normalized rows through Phase 3 helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate actual smoke execution where safe</name>
  <files>work2_coding/scripts/run_study.py, work2_coding/Src/paired_replay.py</files>
  <read_first>work2_coding/run.py, work2_coding/Src/config.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>If runtime cost permits, add a tiny execution path that constructs `Config`/`Solver` for the smoke manifest with very short episodes and extracts stats/menu metadata into normalized rows. If this is too expensive or unstable, keep the runner in explicit `contract_only` mode by default and document the exact blocker in `study_summary.json` as `execution_status: contract_only`. Do not hide skipped runtime behind successful formal status.</action>
  <verify>Run the cheapest safe command determined by implementation. At minimum, `--contract-only` must pass; if actual smoke execution is enabled, add `--execute-smoke` and prove it writes non-placeholder smoke rows.</verify>
  <acceptance_criteria>
    - Smoke row emission always uses the same normalized schema.
    - Runtime failures do not create formal-looking successful evidence.
    - Any skipped execution is explicit in summary and rows.
  </acceptance_criteria>
  <done>Phase 3 has the nearest runnable smoke equivalent available in the current package, with honest status metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add smoke row end-to-end tests</name>
  <files>work2_coding/scripts/test_smoke_study_rows.py</files>
  <read_first>work2_coding/scripts/run_study.py, work2_coding/Src/paired_replay.py, work2_coding/experiments/studies/smoke_robust_menu.yaml</read_first>
  <action>Create script-style tests that run the public study runner into a temporary directory, load JSON/CSV rows, assert required fields, required policy tags, trace ID sharing within split, manifest hash presence, no-filter diagnostic status, checkpoint status, uptake regime, row status, and absence of artifact/manuscript writes. Include a negative check that formal placeholder rows are rejected by schema validation.</action>
  <verify>`cd work2_coding; python scripts/test_smoke_study_rows.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Test uses a temporary output root.
    - Test exercises the public `run_study.py` path, not only private helpers.
    - Test confirms normalized JSON and CSV agree on row count.
  </acceptance_criteria>
  <done>End-to-end smoke row emission has deterministic regression coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Run Phase 3 verification suite</name>
  <files>work2_coding/scripts/test_experiment_contracts.py, work2_coding/scripts/test_paired_replay_contract.py, work2_coding/scripts/test_policy_fairness_contract.py, work2_coding/scripts/test_smoke_study_rows.py</files>
  <read_first>All Phase 3 modified files</read_first>
  <action>Run the Phase 3 script tests and the Phase 2 baseline smoke import. Fix any contract/schema regressions. Keep generated smoke outputs under ignored/local output roots unless the test uses a temporary directory.</action>
  <verify>Run all commands in the verification block.</verify>
  <acceptance_criteria>
    - All Phase 3 tests pass.
    - `Src.config` import still passes.
    - No generated paper artifacts or manuscript files are modified.
  </acceptance_criteria>
  <done>Phase 3 is ready for Phase 4 evidence generation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_experiment_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_paired_replay_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_policy_fairness_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_smoke_study_rows.py` passes.
- [ ] `cd work2_coding; python scripts/run_study.py --study smoke_robust_menu --contract-only` writes `normalized_rows.json`, `normalized_rows.csv`, and `study_summary.json`.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] `rg -n "placeholder_only|formal|no_filter_diagnostic|manifest_hash|trace_id|checkpoint_load_status|uptake_regime" work2_coding/Src work2_coding/scripts work2_coding/experiments` finds implementation/test coverage.
</verification>

<success_criteria>
- EXP-01 is covered by smoke/pilot/formal contracts and normalized row outputs.
- EXP-02 is covered by required baseline policies.
- EXP-03 is covered by paired replay validation and trace/checkpoint provenance.
- EXP-04 is covered by uptake regime declarations and row fields.
- Phase 3 smoke row emission is available without generating paper artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experiment-contracts-and-fair-replay/03-04-SUMMARY.md`.
</output>

## PLANNING COMPLETE
