---
phase: 03-experiment-contracts-and-fair-replay
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - work2_coding/Src/experiment_contracts.py
  - work2_coding/Src/policy_adapters.py
  - work2_coding/scripts/test_policy_fairness_contract.py
autonomous: true
requirements:
  - EXP-02
  - EXP-03
  - EXP-04
user_setup: []
must_haves:
  truths:
    - "D-06: Required baselines include full display, home only, nearest heuristic, top-k cheapest, min-lateness, hard filter, no-filter diagnostic, robust time-window menu, and optional random top-k."
    - "D-08: Compared policies share fair replay fields unless intentionally varied."
    - "D-10: Policy adapters set only intended knobs."
    - "D-11: Uptake regime checks include at least low and medium behaviorally live settings when runtime permits."
  artifacts:
    - path: "work2_coding/scripts/test_policy_fairness_contract.py"
      provides: "Baseline policy adapter and uptake-regime fairness tests"
      min_lines: 100
      contains: "service_guarded_expected_profit"
  key_links:
    - from: "work2_coding/Src/Algorithms/DSPO_Menu.py"
      to: "work2_coding/Src/policy_adapters.py"
      via: "Manifest policy tags map to parser/runtime menu policies"
      pattern: "menu_policy"
---

<objective>
Add policy adapters and fairness validators so required baselines map cleanly onto parser/runtime knobs without unintended drift.

Purpose: make study contracts executable and scientifically comparable by separating policy-specific changes from shared paired replay settings.

Output: policy adapter helpers and script tests for required baselines, fair knobs, and uptake regimes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-experiment-contracts-and-fair-replay/03-CONTEXT.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-RESEARCH.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-01-PLAN.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-02-PLAN.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-04-SUMMARY.md

@work2_coding/Src/parser.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
@work2_coding/Src/experiment_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define required policy adapters</name>
  <files>work2_coding/Src/policy_adapters.py</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Src/Algorithms/DSPO_Menu.py, work2_coding/experiments/studies/smoke_robust_menu.yaml</read_first>
  <action>Create a small adapter table or functions mapping manifest policy tags to parser overrides. Include: full display -> `offer_all_feasible_bundles`; home only -> `home_only`; nearest heuristic -> `nearest_heuristic`; top-k cheapest -> `top_k_cheapest`; min-lateness -> `min_lateness`; hard filter -> robust objective with `menu_eta_filter_mode=hard`; no-filter diagnostic -> same comparable objective with `menu_eta_filter_mode=none` and diagnostic flag; robust risk-adjusted -> `risk_adjusted_expected_profit` plus robust ETA mode; robust service-guarded -> `service_guarded_expected_profit` plus robust ETA mode; optional random -> `random_top_k`.</action>
  <verify>`cd work2_coding; python scripts/test_policy_fairness_contract.py` after Task 4.</verify>
  <acceptance_criteria>
    - Every required baseline has exactly one adapter.
    - Adapter outputs are parser-compatible.
    - No-filter adapter carries `diagnostic=True`.
    - Robust policies keep `risk_adjusted_expected_profit` and `service_guarded_expected_profit` separate.
  </acceptance_criteria>
  <done>Required baseline tags map to explicit parser/runtime settings.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce allowed policy-only drift</name>
  <files>work2_coding/Src/experiment_contracts.py, work2_coding/Src/policy_adapters.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/Src/paired_replay.py</read_first>
  <action>Connect policy adapters to manifest resolution. Validate that adapters only alter allowed fields: policy tag, `menu_policy`, ETA filter/mode parameters, optional objective/guardrail settings, and diagnostic marker. Reject adapter or manifest overrides that alter shared data split, seed, HGS, pricing, checkpoint, `menu_k`, or `max_candidates` unless explicitly declared as a study-level varied field with rationale.</action>
  <verify>Script tests assert that changing `hgs_final_time` or `checkpoint_path` inside a policy fails fairness validation.</verify>
  <acceptance_criteria>
    - Fairness validation runs for all policies in each split.
    - Allowed and disallowed fields are centralized and easy to inspect.
    - Errors explain whether the drift came from adapter defaults or manifest overrides.
  </acceptance_criteria>
  <done>Policy adapters cannot accidentally break paired replay fairness.</done>
</task>

<task type="auto">
  <name>Task 3: Add uptake regime contracts</name>
  <files>work2_coding/Src/experiment_contracts.py, work2_coding/experiments/studies/*.yaml</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Environments/OOH/customerchoice.py</read_first>
  <action>Add manifest support for named uptake regimes, at least `low` and `medium`, by varying behavior parameters such as `home_util`, `base_util`, or incentive sensitivity at the split/regime level. Include a validation rule that every pilot/formal study declares low and medium regimes or explicitly marks a runtime limitation. Do not tune regimes to produce desired conclusions; just declare reproducible settings and required row fields.</action>
  <verify>Script tests assert pilot/formal manifests include low and medium regimes and smoke includes at least one behaviorally live regime.</verify>
  <acceptance_criteria>
    - Low and medium uptake regimes are named and reproducible.
    - Regime changes are split-level or study-level, not hidden inside policy adapters.
    - Rows can carry `uptake_regime`.
  </acceptance_criteria>
  <done>Uptake regime contracts exist without contaminating policy-level fairness.</done>
</task>

<task type="auto">
  <name>Task 4: Add policy fairness contract tests</name>
  <files>work2_coding/scripts/test_policy_fairness_contract.py</files>
  <read_first>work2_coding/Src/policy_adapters.py, work2_coding/Src/experiment_contracts.py, work2_coding/Src/paired_replay.py</read_first>
  <action>Create script-style tests for required adapter coverage, parser compatibility, no-filter diagnostic flag, robust policy separation, fairness rejection for disallowed policy drift, allowed filter/objective drift, uptake regime declarations, and row-ready `uptake_regime` metadata.</action>
  <verify>`cd work2_coding; python scripts/test_policy_fairness_contract.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Tests fail if any required baseline is removed.
    - Tests fail if no-filter loses diagnostic labeling.
    - Tests fail if uptake regimes disappear from pilot/formal contracts.
  </acceptance_criteria>
  <done>Baseline mapping and policy fairness rules are locked by deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_experiment_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_paired_replay_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_policy_fairness_contract.py` passes.
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
</verification>

<success_criteria>
- EXP-02 is covered by required baseline adapter coverage.
- EXP-03 is strengthened by adapter-level fairness validation.
- EXP-04 is partially covered by reproducible low/medium uptake regime contracts.
- CONTEXT decisions D-06, D-08, D-10, and D-11 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experiment-contracts-and-fair-replay/03-03-SUMMARY.md`.
</output>

## PLANNING COMPLETE
