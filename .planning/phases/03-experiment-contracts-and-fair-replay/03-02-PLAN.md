---
phase: 03-experiment-contracts-and-fair-replay
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - work2_coding/Src/experiment_contracts.py
  - work2_coding/Src/paired_replay.py
  - work2_coding/scripts/test_paired_replay_contract.py
autonomous: true
requirements:
  - EXP-01
  - EXP-03
user_setup: []
must_haves:
  truths:
    - "D-08: Compared policies share traces, split, seeds, checkpoint, pricing, HGS, and candidate settings unless intentionally varied."
    - "D-09: Request traces are materialized or reconstructible with trace ID/hash."
    - "D-10: Policy adapters must set only intended knobs."
    - "D-12: Normalized rows include study/run IDs, policy tag, seed, split ID, trace ID, checkpoint status/path/hash, manifest hash, run mode, menu_k, filter mode, effective policy, solver diagnostics, acceptance/opt-out metrics, and placeholder/incomplete status."
    - "D-13: Output schemas are validated before artifact builders consume rows."
  artifacts:
    - path: "work2_coding/scripts/test_paired_replay_contract.py"
      provides: "Paired setting, trace hash, and normalized row schema tests"
      min_lines: 100
      contains: "trace_id"
  key_links:
    - from: "work2_coding/Environments/OOH/Parcelpoint_py.py"
      to: "work2_coding/Src/paired_replay.py"
      via: "stats[8] opt-out and acceptance metadata"
      pattern: "acceptance_rate"
---

<objective>
Implement the paired replay contract and normalized row writer that Phase 3 studies will use.

Purpose: make fairness auditable by resolving manifests into paired settings, trace identifiers, and row-ready metadata before any paper-facing artifacts are generated.

Output: paired replay helper module, normalized row schema validation, and deterministic script tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-experiment-contracts-and-fair-replay/03-CONTEXT.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-RESEARCH.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-01-PLAN.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-02-SUMMARY.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-03-SUMMARY.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-04-SUMMARY.md

@work2_coding/Src/experiment_contracts.py
@work2_coding/Src/parser.py
@work2_coding/Src/config.py
@work2_coding/Environments/OOH/Parcelpoint_py.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve paired settings from manifests</name>
  <files>work2_coding/Src/experiment_contracts.py, work2_coding/Src/paired_replay.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/Src/parser.py, work2_coding/experiments/studies/smoke_robust_menu.yaml</read_first>
  <action>Add helpers that expand every split/policy pair into a resolved setting dictionary or parser namespace. Compare all manifest `paired_fields` across policies within each split. Permit only declared `varied_fields`, such as `menu_policy` and `menu_eta_filter_mode`, to differ. Produce clear errors listing field, baseline value, variant value, split ID, and policy tag.</action>
  <verify>`cd work2_coding; python scripts/test_paired_replay_contract.py` after Task 4.</verify>
  <acceptance_criteria>
    - Same split policies share seed, data seed, test seed, pricing mode, HGS settings, candidate settings, and checkpoint fields unless explicitly varied.
    - Validation detects accidental drift in `hgs_final_time`, `pricing`, `checkpoint_path`, `menu_k`, or `max_candidates`.
    - Error messages identify the offending policy and field.
  </acceptance_criteria>
  <done>Manifests can be resolved into paired policy settings with fairness validation.</done>
</task>

<task type="auto">
  <name>Task 2: Add trace ID and manifest provenance helpers</name>
  <files>work2_coding/Src/paired_replay.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/Environments/OOH/Parcelpoint_py.py</read_first>
  <action>Add deterministic helpers for trace identity. At minimum, derive `trace_id` from study name, split ID, seed, data seeds, episode count, and request count settings. If actual request traces are later materialized, hash their serialized content and keep the same row field. Also expose `manifest_hash` and `settings_hash` helpers for row provenance.</action>
  <verify>Script tests assert stable trace IDs and changed trace IDs when seed/split changes.</verify>
  <acceptance_criteria>
    - Trace IDs are deterministic and independent of policy tag.
    - Trace IDs change when split/seed/data settings change.
    - The helper does not run the simulator or write files.
  </acceptance_criteria>
  <done>Paired replay has stable trace and settings provenance before runtime execution.</done>
</task>

<task type="auto">
  <name>Task 3: Add normalized row builder and schema validator</name>
  <files>work2_coding/Src/paired_replay.py</files>
  <read_first>work2_coding/Environments/OOH/Parcelpoint_py.py, work2_coding/Src/Algorithms/DSPO_Menu.py, work2_coding/Src/Utils/Utils.py</read_first>
  <action>Define `NORMALIZED_ROW_FIELDS` and a row builder that combines manifest setting metadata, checkpoint metadata, environment stats metadata, selected menu metadata, and run status. Include fields required by D-12, with `None` allowed for unavailable optional metrics. Add a schema validator that rejects missing required fields and placeholder formal rows. Keep smoke/diagnostic rows allowed when status is explicit.</action>
  <verify>Script tests build rows from synthetic stats/checkpoint/menu metadata and validate required fields.</verify>
  <acceptance_criteria>
    - Rows include opt-out and acceptance metrics from `stats[8]`.
    - Rows include checkpoint metadata from Phase 2 helper dictionaries.
    - Rows include ETA/filter/effective-policy/solver fields from selected-offer metadata when available.
    - Formal rows cannot be `placeholder_only=True`.
  </acceptance_criteria>
  <done>Normalized rows have a stable schema and provenance guardrails.</done>
</task>

<task type="auto">
  <name>Task 4: Add paired replay and row schema tests</name>
  <files>work2_coding/scripts/test_paired_replay_contract.py</files>
  <read_first>work2_coding/Src/paired_replay.py, work2_coding/Src/experiment_contracts.py</read_first>
  <action>Create script-style tests for paired field validation, allowed varied fields, trace ID stability, settings hash stability, row building from synthetic Phase 2 metadata, row schema failure on missing fields, and formal placeholder rejection.</action>
  <verify>`cd work2_coding; python scripts/test_paired_replay_contract.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Tests are deterministic and do not require HGS, training, or external outputs.
    - Tests prove row schema and paired fairness fail closed on common mistakes.
  </acceptance_criteria>
  <done>Paired replay contracts and normalized row schema have deterministic coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_experiment_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_paired_replay_contract.py` passes.
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
</verification>

<success_criteria>
- EXP-01 is further covered by normalized output schema.
- EXP-03 is covered by paired setting validation, trace identity, and row provenance.
- CONTEXT decisions D-08, D-09, D-10, D-12, and D-13 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experiment-contracts-and-fair-replay/03-02-SUMMARY.md`.
</output>

## PLANNING COMPLETE
