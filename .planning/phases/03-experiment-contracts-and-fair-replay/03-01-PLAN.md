---
phase: 03-experiment-contracts-and-fair-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - work2_coding/Src/experiment_contracts.py
  - work2_coding/experiments/studies/smoke_robust_menu.yaml
  - work2_coding/experiments/studies/pilot_robust_menu.yaml
  - work2_coding/experiments/studies/formal_robust_menu.yaml
  - work2_coding/experiments/suites/work2_robust_menu.yaml
  - work2_coding/scripts/test_experiment_contracts.py
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
user_setup: []
must_haves:
  truths:
    - "D-01: Use only `work2_coding/`; do not create `ooh_code/`."
    - "D-03: No-filter is diagnostic and must be labeled as such in manifests."
    - "D-04: Define smoke, pilot, and formal manifest tiers."
    - "D-05: Validate policy tags, parser-compatible policy settings, seeds, split IDs, menu_k, filter modes, run mode, checkpoint requirement, HGS parameters, and output schema version."
    - "D-06: Include required baselines: full display, home only, nearest heuristic, top-k cheapest, min-lateness, hard filter, no-filter diagnostic, robust time-window menu, and optional random top-k."
    - "D-07: Pilot/formal require explicit shared checkpoint provenance."
  artifacts:
    - path: "work2_coding/scripts/test_experiment_contracts.py"
      provides: "Manifest tier, baseline, parser-choice, checkpoint, and schema validation coverage"
      min_lines: 100
      contains: "no_filter_diagnostic"
  key_links:
    - from: "work2_coding/Src/parser.py"
      to: "work2_coding/Src/experiment_contracts.py"
      via: "Parser choices are the source of truth for manifest policy/filter validation"
      pattern: "menu_policy"
---

<objective>
Create the Phase 3 study-contract layer: manifest schema helpers plus smoke, pilot, formal, and suite YAML contracts for robust menu experiments.

Purpose: make experiment intent auditable before any expensive evidence runs, and ensure all required baselines and fairness fields are declared in parser-compatible form.

Output: contract module, study/suite manifests, and script-style validation tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-CONTEXT.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-RESEARCH.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-04-SUMMARY.md

@work2_coding/Src/parser.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
@work2_coding/scripts/test_menu_runtime_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest loading and validation helpers</name>
  <files>work2_coding/Src/experiment_contracts.py</files>
  <read_first>work2_coding/Src/parser.py, .planning/phases/03-experiment-contracts-and-fair-replay/03-CONTEXT.md</read_first>
  <action>Create a small module that loads YAML manifests, computes a stable manifest hash, exposes parser-derived choices, validates required top-level fields, resolves base args plus split/policy overrides into parser namespaces, and raises clear `ValueError` messages for invalid policy tags, unknown parser keys, invalid ETA filter modes, duplicate policy tags, duplicate split IDs, missing seeds, invalid `menu_k`, invalid run mode, or missing output schema version. Keep dependencies to standard library and PyYAML, already present in requirements.</action>
  <verify>`cd work2_coding; python -c "import sys; sys.path.insert(0,'.'); from Src.experiment_contracts import parser_choices; print('CONTRACT_HELPERS_OK', bool(parser_choices()['menu_policy']))"` prints a truthy helper result.</verify>
  <acceptance_criteria>
    - Parser choices are derived from `Parser().get_parser()` where practical.
    - Validation errors name the bad manifest field and value.
    - Manifest hash is deterministic for stable YAML content.
  </acceptance_criteria>
  <done>Study contract helpers load and validate manifests without constructing `Config`.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke, pilot, formal, and suite manifests</name>
  <files>work2_coding/experiments/studies/smoke_robust_menu.yaml, work2_coding/experiments/studies/pilot_robust_menu.yaml, work2_coding/experiments/studies/formal_robust_menu.yaml, work2_coding/experiments/suites/work2_robust_menu.yaml</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Src/Algorithms/DSPO_Menu.py, .planning/REQUIREMENTS.md</read_first>
  <action>Create study directories and manifests. Each study should declare `schema_version`, `name`, `tier`, `run_mode`, `description`, `base_args`, `splits`, `policies`, `paired_fields`, and `output_schema`. Include required baselines with explicit tags: full display, home only, nearest heuristic, top-k cheapest, min-lateness, hard filter, no-filter diagnostic, robust risk-adjusted menu, robust service-guarded menu, and optional random top-k in smoke/pilot. Pilot/formal manifests must require checkpoint metadata and mark no-filter as diagnostic.</action>
  <verify>`cd work2_coding; python scripts/test_experiment_contracts.py` after Task 3.</verify>
  <acceptance_criteria>
    - Smoke uses one cheap split/seed and short runtime/HGS settings.
    - Pilot/formal declare multiple seeds or split IDs but are not executed in this plan.
    - Every policy resolves to parser-compatible `algo_name=DSPO_Menu`, `menu_mode=True`, and intended `menu_policy`/filter settings unless it intentionally tests a legacy baseline.
    - No-filter policy tag or metadata includes `diagnostic: true`.
  </acceptance_criteria>
  <done>Required study contracts exist and are explicit about tier, policy set, and diagnostic baselines.</done>
</task>

<task type="auto">
  <name>Task 3: Add manifest contract tests</name>
  <files>work2_coding/scripts/test_experiment_contracts.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/experiments/studies/*.yaml, work2_coding/experiments/suites/*.yaml</read_first>
  <action>Create script-style tests with direct asserts. Cover valid manifest loading, manifest hash stability, required baseline tags, parser-compatible policy/filter choices, no-filter diagnostic labeling, pilot/formal checkpoint requirement, duplicate policy rejection, invalid filter rejection, duplicate split rejection, unknown parser override rejection, and suite member resolution.</action>
  <verify>`cd work2_coding; python scripts/test_experiment_contracts.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Tests fail on missing required baselines.
    - Tests fail if no-filter is not diagnostic.
    - Tests do not construct `Config`, run HGS, train, or write experiment outputs.
  </acceptance_criteria>
  <done>Manifest schema and baseline contracts have deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_experiment_contracts.py` passes.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0,'.'); from Src.experiment_contracts import load_manifest, validate_manifest; m=load_manifest('experiments/studies/smoke_robust_menu.yaml'); validate_manifest(m); print('SMOKE_MANIFEST_OK')"` prints `SMOKE_MANIFEST_OK`.
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] `Test-Path ooh_code` remains false.
</verification>

<success_criteria>
- EXP-01 is partially covered by smoke/pilot/formal contracts and output schema declarations.
- EXP-02 is partially covered by required baseline declarations.
- EXP-03 is partially covered by paired-field declarations.
- CONTEXT decisions D-01, D-03, D-04, D-05, D-06, and D-07 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-experiment-contracts-and-fair-replay/03-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
