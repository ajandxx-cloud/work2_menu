---
phase: 01-claim-and-filter-reliability-tightening
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - ooh_code/manuscript/sections/results.tex
  - ooh_code/manuscript/sections/limitations.tex
autonomous: true
requirements: [FILT-04]

must_haves:
  truths:
    - "The manuscript explicitly explains that FN pruning concentrates in the far distance band (>1500m), so nearby meeting points are preserved despite large ETA/IVT prediction errors"
    - "The limitations section names large ETA/IVT prediction errors as 'a key limitation' explicitly"
    - "The filter claim remains useful but bounded -- filtering is operationally conservative for nearby points"
  artifacts:
    - path: "ooh_code/manuscript/sections/results.tex"
      provides: "Added 1-2 sentences after quantile error paragraph explaining why large errors don't invalidate near-band FN finding"
      contains: "operationally conservative"
    - path: "ooh_code/manuscript/sections/limitations.tex"
      provides: "Explicit 'key limitation' language about large filter errors"
      contains: "a key limitation"
  key_links:
    - from: "results.tex quantile error paragraph"
      to: "filter_validity_summary.tex FN near/mid/far columns"
      via: "textual reference to concentration pattern"
      pattern: "far.*band.*near.*band.*preserved"
    - from: "limitations.tex filter validity paragraph"
      to: "large ETA/IVT error acknowledgment"
      via: "explicit limitation statement"
      pattern: "key limitation"
---

<objective>
Add explicit explanation for why large ETA/IVT errors do not fully invalidate the filtering diagnostic, and name them as a key limitation.

Purpose: The 6.5/10 review identifies filter validity as a remaining vulnerability because large ETA/IVT errors (P95 ~3,600s and ~3,370s) do not fully reassure that filtering decisions are operationally reliable. Per user decision D-04, a dual framing is required: results explains the mechanism (FN pruning concentrates in far band, nearby points preserved), and limitations names it explicitly as "a key limitation."

Output: Two targeted text additions -- 1-2 sentences in the results quantile error paragraph and 1-2 sentences in the limitations filter-validity paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-claim-and-filter-reliability-tightening/01-CONTEXT.md
@.planning/phases/01-claim-and-filter-reliability-tightening/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add large-error explanation to results.tex and key-limitation language to limitations.tex</name>
  <files>
    ooh_code/manuscript/sections/results.tex
    ooh_code/manuscript/sections/limitations.tex
  </files>
  <read_first>
    ooh_code/manuscript/sections/results.tex
    ooh_code/manuscript/sections/limitations.tex
    ooh_code/artifacts/tables/filter_validity_summary.tex
  </read_first>
  <action>
**A. results.tex -- Add explanation after the quantile error paragraph (after line 25)**

The quantile error paragraph (approximately lines 24-25) currently ends with:
```
The heuristic and stronger filters achieve near-zero ETA errors by construction because they only display bundles whose heuristic ETA window is sufficiently narrow.
```

Add the following 2 sentences immediately after this closing sentence (before the next `\paragraph{False-negative pruning by distance.}`):

```
Despite these large prediction errors, the filtering diagnostic remains informative because false-negative pruning concentrates in the far distance band, where meeting points are least likely to be accepted; the near-band false-negative rate remains below 0.04 candidates per request (Table~\ref{tab:filter_validity_summary}). The filtering signal is coarse at the individual-offer level, but operationally it preferentially removes distant bundles while preserving nearby options that passengers are more likely to select.
```

This directly explains why large ETA/IVT errors do not invalidate the key finding: nearby points are preserved.

**B. limitations.tex -- Add key-limitation language to the filter-validity paragraph (paragraph 2, approximately line 6)**

The second paragraph of limitations.tex currently contains the filter-validity discussion. Find the sentence that currently reads (approximately):

```
However, a true filtering confusion matrix requiring ground-truth feasibility labels remains unavailable, so the filter-validity evidence is still partially indirect.
```

Replace this sentence with:

```
However, a true filtering confusion matrix requiring ground-truth feasibility labels remains unavailable, so the filter-validity evidence is still partially indirect. The large prediction errors documented above represent a key limitation: while the FN concentration pattern suggests that filtering is operationally conservative for nearby meeting points, the coarse prediction signal means that the operational reliability of filtering-based assortment decisions cannot be fully established from the current diagnostics alone.
```

This adds exactly one new sentence that explicitly names large prediction errors as "a key limitation" and connects it to the concentration pattern argument from results.tex, while maintaining the honest assessment that filtering reliability cannot be fully established.

**Key data for verification (from filter_validity_summary.tex):**
- ETA P95: ~3,631s (deployed blended), ~3,612s (oracle)
- IVT P95: ~3,373s across all variants
- FN Near (<500m): 0.011-0.035
- FN Mid (500-1500m): 0.113-0.130
- FN Far (>1500m): 8.421-8.698

Do NOT modify any other paragraphs in results.tex or limitations.tex.
  </action>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/2.paper_2_menu optimization-6分（需降ai）" && grep -c "key limitation" ooh_code/manuscript/sections/limitations.tex && grep -c "operationally conservative" ooh_code/manuscript/sections/results.tex</automated>
  </verify>
  <done>
    - results.tex quantile error paragraph has 2 new sentences explaining why large errors don't invalidate near-band FN finding
    - The new sentences reference "far distance band", "near-band false-negative rate below 0.04", and "operationally conservative"
    - limitations.tex filter-validity paragraph contains the exact phrase "a key limitation"
    - The new limitations sentence connects large prediction errors to the concentration pattern argument
    - No other paragraphs in results.tex or limitations.tex are modified
    - FN near/mid/far statistics cited in new text match filter_validity_summary.tex values
  </done>
</task>

</tasks>

<verification>
1. `grep -n "key limitation" ooh_code/manuscript/sections/limitations.tex` returns exactly 1 hit in the filter-validity paragraph.
2. `grep -n "operationally conservative" ooh_code/manuscript/sections/results.tex` returns exactly 1 hit in the quantile error section.
3. `grep -n "near-band false-negative" ooh_code/manuscript/sections/results.tex` returns exactly 1 hit.
4. The added text does NOT modify any other paragraph or section.
</verification>

<success_criteria>
The manuscript explains (in results) that large ETA/IVT errors do not invalidate the near-band FN finding because FN pruning concentrates in the far band, and names large filter errors as "a key limitation" in the limitations section. The filter claim remains useful but bounded.
</success_criteria>

<output>
After completion, create `.planning/phases/01-claim-and-filter-reliability-tightening/01-02-SUMMARY.md`
</output>
