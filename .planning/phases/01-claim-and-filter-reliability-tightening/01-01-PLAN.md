---
phase: 01-claim-and-filter-reliability-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ooh_code/manuscript/sections/results.tex
  - ooh_code/manuscript/sections/limitations.tex
  - ooh_code/manuscript/sections/conclusion.tex
  - ooh_code/manuscript/sections/appendix.tex
autonomous: true
requirements: [CLAIM-02]

must_haves:
  truths:
    - "No instance of the word 'stable' in results.tex lines 53 or 64 when describing menu-optimization gap across MNL parameterizations or outside-option range"
    - "No instance of 'directional stability' in limitations.tex paragraphs about the outside-option scan"
    - "Conclusion.tex does not restate stability claims for the outside-option scan"
    - "All softening uses 'directionally consistent' or 'not obviously brittle' language"
  artifacts:
    - path: "ooh_code/manuscript/sections/results.tex"
      provides: "Soften 'stable' to 'directionally consistent' at lines 53 and 64"
      contains: "directionally consistent"
    - path: "ooh_code/manuscript/sections/limitations.tex"
      provides: "Soften 'directional stability' to appropriate bounded language"
      contains: "not obviously brittle"
    - path: "ooh_code/manuscript/sections/appendix.tex"
      provides: "Soften 'directional stability' to 'directional consistency'"
      contains: "directional consistency"
  key_links:
    - from: "results.tex line 53"
      to: "MNL sensitivity claim"
      via: "text replacement"
      pattern: "directionally consistent"
    - from: "results.tex line 64"
      to: "outside-option scan claim"
      via: "text replacement"
      pattern: "directionally consistent"
---

<objective>
Soften all outside-option and MNL sensitivity "stable/stability" claims in the manuscript.

Purpose: The 6.5/10 review explicitly requests softening "stable across demand assumptions" to "not obviously brittle within this RC stress test." The user decision (per D-01) specifies replacing "stable" with "directionally consistent" as the primary term in results.tex, and adding "not obviously brittle" clarification in limitations.tex.

Output: All instances of "stable" describing outside-option or MNL sensitivity findings are replaced with bounded language; conclusion.tex and appendix.tex are checked and aligned; table captions are scanned for overclaiming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-claim-and-filter-reliability-tightening/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Soften "stable" claims in results.tex, limitations.tex, appendix.tex, and verify conclusion.tex and table captions</name>
  <files>
    ooh_code/manuscript/sections/results.tex
    ooh_code/manuscript/sections/limitations.tex
    ooh_code/manuscript/sections/conclusion.tex
    ooh_code/manuscript/sections/appendix.tex
  </files>
  <read_first>
    ooh_code/manuscript/sections/results.tex
    ooh_code/manuscript/sections/limitations.tex
    ooh_code/manuscript/sections/conclusion.tex
    ooh_code/manuscript/sections/appendix.tex
    ooh_code/artifacts/tables/city_split_gap_table.tex
    ooh_code/artifacts/tables/policy_gap_uncertainty_summary.tex
  </read_first>
  <action>
Perform the following exact text replacements across the manuscript files. Each replacement addresses a specific "stable" or "stability" instance identified by line number in the current files.

**A. results.tex -- Line 53 (MNL sensitivity section)**

Current text (around line 53):
```
The sensitivity evidence shows that the direction and rough magnitude of the menu-optimization gap are stable across the tested MNL parameterizations.
```

Replace with:
```
The sensitivity evidence shows that the direction and rough magnitude of the menu-optimization gap are directionally consistent across the tested MNL parameterizations.
```

Only change the word "stable" to "directionally consistent" -- do NOT modify any other text on this line.

**B. results.tex -- Line 64 (Outside-option sensitivity section)**

Current text (around line 64):
```
the sign and rough magnitude of the menu-optimization gap relative to full display are stable across the tested $u_0$ range
```

Replace with:
```
the sign and rough magnitude of the menu-optimization gap relative to full display are directionally consistent across the tested $u_0$ range
```

Only change the word "stable" to "directionally consistent" -- do NOT modify any other text on this line.

**C. limitations.tex -- Line 10 (MNL parameter paragraph, paragraph 4)**

Current text (around line 10):
```
demonstrating directional stability across five $u_0$ levels
```

Replace with:
```
demonstrating that the results are not obviously brittle within this RC stress test across five $u_0$ levels
```

This replaces "directional stability" with the user-approved "not obviously brittle" framing per the review's exact language.

**D. limitations.tex -- Line 14 (Scoring parameters paragraph, paragraph 7)**

Current text (around line 14):
```
demonstrates directional stability within the tested $u_0$ range
```

Replace with:
```
demonstrates directional consistency within the tested $u_0$ range
```

**E. appendix.tex -- Line 125**

Current text (around line 125):
```
descriptive evidence about directional stability rather than as precise interval estimates
```

Replace with:
```
descriptive evidence about directional consistency rather than as precise interval estimates
```

**F. conclusion.tex verification**

Read conclusion.tex and verify it does NOT contain "stable" or "stability" in reference to outside-option scan or MNL sensitivity findings. The current conclusion text does not contain these words (confirmed in planning), but if any are found, apply the same "directionally consistent" / "not obviously brittle" replacement.

**G. Table caption scan**

Read the following artifact table files and check for "stable" or "stability" language that overclaims:
- `ooh_code/artifacts/tables/city_split_gap_table.tex` -- line 5 caption contains "stable interval estimates" -- this is appropriate (it says results should NOT be read as stable), so leave as-is.
- `ooh_code/artifacts/tables/policy_gap_uncertainty_summary.tex` -- line 5 caption contains "stable interval estimates" -- same appropriate usage, leave as-is.
- Any other table .tex files in `ooh_code/artifacts/tables/` that contain "stable" in their caption should be checked. The word "stable" in context of "not stable" or "should not be read as stable" is appropriate and should NOT be changed.

Do NOT modify the calibration-scope paragraph in results.tex (the paragraph starting "This outside-option scan constitutes literature-bounded sensitivity testing" at approximately line 67). Per user decision D-02, this paragraph stays as-is.
  </action>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/2.paper_2_menu optimization-6分（需降ai）" && grep -n "stable" ooh_code/manuscript/sections/results.tex | grep -v "descriptive\|not stable\|should not\|numerical stability\|pricing-stability\|stability constant" || echo "NO_STABLE_FOUND_IN_RESULTS"</automated>
  </verify>
  <done>
    - results.tex line 53 uses "directionally consistent" instead of "stable"
    - results.tex line 64 uses "directionally consistent" instead of "stable"
    - limitations.tex line 10 uses "not obviously brittle within this RC stress test" instead of "directional stability"
    - limitations.tex line 14 uses "directional consistency" instead of "directional stability"
    - appendix.tex line 125 uses "directional consistency" instead of "directional stability"
    - conclusion.tex contains no unmodified "stable/stability" references to outside-option or MNL findings
    - Table captions with "stable" are verified as appropriate hedging (unchanged)
    - Calibration-scope paragraph (results.tex ~line 67) is unchanged
  </done>
</task>

</tasks>

<verification>
1. `grep -n "stable" ooh_code/manuscript/sections/results.tex` returns no hits on lines 53 or 64 (or returns only lines with "numerical stability" / "pricing-stability" which are unrelated).
2. `grep -n "directional stability" ooh_code/manuscript/sections/limitations.tex` returns zero hits.
3. `grep -n "directionally consistent" ooh_code/manuscript/sections/results.tex` returns at least 2 hits (lines 53, 64).
4. `grep -n "not obviously brittle" ooh_code/manuscript/sections/limitations.tex` returns at least 1 hit.
5. `grep -n "directional consistency" ooh_code/manuscript/sections/appendix.tex` returns at least 1 hit.
</verification>

<success_criteria>
All "stable" / "stability" claims describing outside-option scan and MNL sensitivity findings are replaced with "directionally consistent" or "not obviously brittle" language. No calibration-scope paragraph or unrelated "stability" (numerical, pricing) is changed. Table captions remain appropriately hedged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-claim-and-filter-reliability-tightening/01-01-SUMMARY.md`
</output>
