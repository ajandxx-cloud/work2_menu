# Phase 10 Plan: Pilot Attention Evidence Run

**Status:** Planned
**Created:** 2026-06-11
**Mode:** MVP

## Goal

Run checkpoint-backed pilot evidence for `DSPO_original` vs `DSPO_attention`, rebuild artifacts, and make an honest go/no-go decision.

## Requirements

- PILOT-01: pilot completes with non-placeholder rows and loaded checkpoint provenance.
- PILOT-02: every attention pair contains both variants.
- PILOT-03: low and medium uptake regimes are present and behaviorally live.
- PILOT-04: pilot artifacts and claim guard are rebuilt from the completed run.
- PILOT-05: pilot summary reports primary/secondary deltas, attention weights, checkpoint provenance, and regime-level results.
- PILOT-06: written go/no-go decision controls formal evidence or ablation.

## Work Items

1. Run pilot actual replay.
   - `python scripts/run_study.py --study pilot_attention_dspo --execute`

2. Rebuild artifacts and mirror.
   - `python scripts/build_attention_artifacts.py --study pilot_attention_dspo --allow-incomplete --default-mirror`

3. Inspect claim guard and deltas.
   - Confirm completion status.
   - Confirm checkpoint statuses.
   - Confirm pair completeness.
   - Confirm low/medium regimes.
   - Compute primary and secondary deltas.

4. Write Phase 10 outputs.
   - `10-PILOT-SUMMARY.md`
   - `10-GO-NOGO.md`
   - `10-VERIFICATION.md`
   - `10-VALIDATION.md`
   - `10-SUMMARY.md`

5. Update requirements traceability.

## Acceptance Criteria

- The pilot run is completed, non-placeholder, and checkpoint-loaded, or the phase explicitly fails.
- Artifacts are rebuilt from the completed pilot run.
- `ATTENTION_CLAIM_GUARD.json` is the source of truth for claim permission.
- If attention does not improve the primary metric, the go/no-go is `NO-GO` and the claim is marked failed/not supported.

