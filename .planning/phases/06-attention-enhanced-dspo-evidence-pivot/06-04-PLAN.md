---
phase: 06-attention-enhanced-dspo-evidence-pivot
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - work2_coding/scripts/run_study.py
  - work2_coding/outputs/studies/smoke_attention_dspo/
  - work2_coding/artifacts/work2_attention_dspo/
  - artifacts/work2_attention_dspo/
  - work2_coding/scripts/test_attention_smoke_execution.py
autonomous: true
requirements:
  - ATTN-02
  - ATTN-03
  - ATTN-04
must_haves:
  truths:
    - "D-09: Smoke evidence validates executability and schema only, not directional improvement claims."
    - "D-10: Completed, non-placeholder, checkpoint-valid paired evidence is required before an attention-improves claim is allowed."
    - "D-11: Net objective/profit proxy is primary, while acceptance, opt-out, non-home uptake, and service cost are service constraints."
    - "D-12: If attention tuning is needed, tuning splits and held-out/formal evaluation are separated; held-out results are not used to pick settings."
    - "D-13: Attention smoke/pilot/formal manifests are the execution ladder."
    - "D-15: Paired delta and attention claim guard artifacts are generated from run outputs, not hand-edited."
  artifacts:
    - path: "work2_coding/scripts/test_attention_smoke_execution.py"
      provides: "End-to-end smoke execution and blocker-path coverage for attention study outputs"
      contains: "smoke_attention_dspo"
    - path: "artifacts/work2_attention_dspo/ATTENTION_CLAIM_GUARD.json"
      provides: "Reviewer-facing mirrored attention claim guard"
      contains: "claim_ready"
  key_links:
    - from: "work2_coding/experiments/studies/smoke_attention_dspo.yaml"
      to: "work2_coding/artifacts/work2_attention_dspo/ATTENTION_CLAIM_GUARD.json"
      via: "run_study.py followed by build_attention_artifacts.py"
      pattern: "DSPO_original"
---

<objective>
Run and verify the Phase 6 evidence ladder at the smoke level, then produce honest attention artifacts. If pilot/formal prerequisites such as shared checkpoints are absent, preserve blockers rather than generating empirical claims.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-CONTEXT.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-RESEARCH.md
@work2_coding/scripts/run_study.py
@work2_coding/scripts/build_attention_artifacts.py
@work2_coding/experiments/studies/smoke_attention_dspo.yaml
@work2_coding/experiments/studies/pilot_attention_dspo.yaml
@work2_coding/experiments/studies/formal_attention_dspo.yaml
@work2_coding/Src/paired_replay.py
@work2_coding/Src/attention_artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add smoke execution regression coverage</name>
  <files>work2_coding/scripts/test_attention_smoke_execution.py</files>
  <read_first>work2_coding/scripts/run_study.py, work2_coding/scripts/test_smoke_study_rows.py, work2_coding/scripts/test_study_execution_status.py</read_first>
  <action>Add a script-style test that runs the attention smoke study in contract mode and, if actual replay is cheap enough, with `--execute --max-policies 2`. Assert both method tags appear, trace IDs are paired, attention fields exist, and smoke rows cannot unlock improvement claims. Add a blocker-path assertion for pilot/formal missing checkpoint behavior if those manifests point to unavailable checkpoints.</action>
  <verify>`cd work2_coding; python scripts/test_attention_smoke_execution.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Test uses temporary output roots.
    - Contract-only smoke rows contain both `DSPO_original` and `DSPO_attention`.
    - Actual smoke is attempted only when it is stable within the existing runtime; otherwise the blocker path is explicit.
    - Pilot/formal missing-checkpoint behavior stays fail-closed.
  </acceptance_criteria>
  <done>Attention study execution behavior is covered at the public runner boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Run attention smoke and build incomplete-safe artifacts</name>
  <files>work2_coding/outputs/studies/smoke_attention_dspo/, work2_coding/artifacts/work2_attention_dspo/, artifacts/work2_attention_dspo/</files>
  <read_first>work2_coding/experiments/studies/smoke_attention_dspo.yaml, work2_coding/scripts/run_study.py, work2_coding/scripts/build_attention_artifacts.py</read_first>
  <action>Run the public smoke attention study path. Prefer actual execution if it remains cheap and stable; otherwise run contract-only and record that smoke is schema evidence only. Then build attention artifacts with incomplete allowed and mirror them to root artifacts. Do not hand-edit rows or artifact outputs.</action>
  <verify>`cd work2_coding; python scripts/run_study.py --study smoke_attention_dspo --contract-only` and `python scripts/build_attention_artifacts.py --study smoke_attention_dspo --allow-incomplete --mirror-root ../artifacts/work2_attention_dspo`.</verify>
  <acceptance_criteria>
    - Run output includes normalized rows for both method variants.
    - Paired delta artifacts are generated or explicitly marked incomplete if rows are contract-only.
    - `ATTENTION_CLAIM_GUARD.json` blocks attention-improves claims for smoke/contract-only evidence.
    - Root mirror is refreshed from generated outputs.
  </acceptance_criteria>
  <done>Reviewable Phase 6 smoke artifacts exist and are scientifically labeled.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pilot/formal gates remain blocked without prerequisites</name>
  <files>work2_coding/outputs/studies/pilot_attention_dspo/, work2_coding/outputs/studies/formal_attention_dspo/</files>
  <read_first>work2_coding/experiments/studies/pilot_attention_dspo.yaml, work2_coding/experiments/studies/formal_attention_dspo.yaml, work2_coding/Src/study_execution.py</read_first>
  <action>Exercise or test the pilot/formal prerequisite path enough to prove missing shared checkpoint files produce blocker metadata rather than random-weight evidence. Do not attempt expensive formal execution. If a valid checkpoint is later supplied, leave instructions in the summary for rerunning pilot/formal once.</action>
  <verify>`cd work2_coding; python scripts/test_attention_smoke_execution.py` plus targeted runner invocation for pilot with a temporary output root if tests do not already cover it.</verify>
  <acceptance_criteria>
    - Missing pilot/formal checkpoint paths produce blocker records.
    - Formal placeholder rows remain rejected.
    - No pilot/formal attention superiority claim is generated from blocked outputs.
  </acceptance_criteria>
  <done>The evidence ladder fails closed beyond smoke when prerequisites are missing.</done>
</task>

<task type="auto">
  <name>Task 4: Run full Phase 6 verification set and document outcome</name>
  <files>.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-04-SUMMARY.md</files>
  <read_first>.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-01-SUMMARY.md, .planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-02-SUMMARY.md, .planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-03-SUMMARY.md</read_first>
  <action>Run the accumulated Phase 6 verification commands. Summarize whether attention smoke executed as actual or contract-only, whether artifacts are claim-ready or blocked/incomplete, and what exact command should be used later for pilot/formal evidence once checkpoint provenance is available.</action>
  <verify>Run all commands listed in this plan's verification block.</verify>
  <acceptance_criteria>
    - Summary clearly separates runnable smoke/schema evidence from unsupported improvement claims.
    - Any blockers are named with code, path, and required follow-up.
    - The final summary does not strengthen paper claims beyond the claim guard.
  </acceptance_criteria>
  <done>Phase 6 execution status is ready for verification or follow-up evidence runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_attention_menu_logic.py` passes.
- [ ] `cd work2_coding; python scripts/test_attention_manifest_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_attention_paired_rows.py` passes.
- [ ] `cd work2_coding; python scripts/test_attention_artifact_gate.py` passes.
- [ ] `cd work2_coding; python scripts/test_attention_smoke_execution.py` passes.
- [ ] `cd work2_coding; python scripts/run_study.py --study smoke_attention_dspo --contract-only` writes normalized rows.
- [ ] `cd work2_coding; python scripts/build_attention_artifacts.py --study smoke_attention_dspo --allow-incomplete --mirror-root ../artifacts/work2_attention_dspo` writes mirrored attention artifacts.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
</verification>

<success_criteria>
- ATTN-02 attention behavior remains compatible with opt-out accounting, checkpoint metadata, and routing feasibility.
- ATTN-03 paired smoke output keeps original/attention rows aligned on identical trace settings.
- ATTN-04 artifacts and claim guard report deltas but block unsupported superiority claims.
- Phase 6 decisions D-09 through D-15 are represented in verification and outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-04-SUMMARY.md`, then run phase verification.
</output>

## PLANNING COMPLETE
