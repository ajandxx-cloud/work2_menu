---
phase: 06-attention-enhanced-dspo-evidence-pivot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - work2_coding/Src/parser.py
  - work2_coding/Src/Algorithms/DSPO_Menu.py
  - work2_coding/scripts/test_attention_menu_logic.py
autonomous: true
requirements:
  - ATTN-01
  - ATTN-02
  - BEHAV-01
must_haves:
  truths:
    - "D-01: Attention enters the menu scoring layer first, not the lower-level cost or ETA predictor."
    - "D-02: Deterministic attention uses existing candidate and MenuOffer metadata signals such as ETA risk, walk/time, price, predicted/system cost, route delay, and capacity risk."
    - "D-03: The default implementation is deterministic and explainable, while parser/config hooks preserve an explicit future neural attention mode."
    - "D-04: Attention adjusts candidate or menu objective scores and can change selected bundles; it is not diagnostics-only."
    - "D-05: DSPO_original preserves current safe no-attention DSPO/Menu logic, including Phase 2-5 guardrails."
  artifacts:
    - path: "work2_coding/scripts/test_attention_menu_logic.py"
      provides: "Deterministic attention scoring and selection-change regression coverage"
      contains: "DSPO_attention"
  key_links:
    - from: "work2_coding/Src/parser.py"
      to: "work2_coding/Src/Algorithms/DSPO_Menu.py"
      via: "Parser flags become DSPO_Menu attention config"
      pattern: "attention_enabled"
---

<objective>
Implement the Phase 6 core method treatment: a deterministic, explainable candidate-attention layer inside `DSPO_Menu` that can alter menu selection while preserving the current no-attention behavior as `DSPO_original`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-CONTEXT.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-RESEARCH.md
@work2_coding/Src/parser.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
@work2_coding/Environments/OOH/containers.py
@work2_coding/scripts/test_robust_menu_logic.py
@work2_coding/scripts/test_menu_runtime_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parser/config knobs for method and attention mode</name>
  <files>work2_coding/Src/parser.py</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Src/config.py</read_first>
  <action>Add parser flags for `method_variant`, `attention_enabled`, `attention_mode`, `attention_strength`, and deterministic feature weights or penalties needed by the v1 formula. Defaults must preserve original behavior: `method_variant=DSPO_original`, `attention_enabled=False`, and no objective change unless attention is explicitly enabled.</action>
  <verify>`cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); from Src.parser import Parser; args=Parser().get_parser().parse_args([]); assert args.attention_enabled is False; assert args.method_variant == 'DSPO_original'"`</verify>
  <acceptance_criteria>
    - New parser choices are compatible with manifest validation.
    - Defaults do not change existing robust-menu behavior.
    - A future neural mode is represented as an explicit mode value or reserved config path, but deterministic remains the only required implemented mode.
  </acceptance_criteria>
  <done>Runtime config can distinguish original and attention variants without ad hoc flags.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic candidate attention scoring</name>
  <files>work2_coding/Src/Algorithms/DSPO_Menu.py</files>
  <read_first>work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Add small helper methods that compute per-offer attention features, an explainable attention weight, and a score/objective adjustment. Integrate the adjustment in the menu objective path used by exact and greedy selection so attention can change selected bundles. Keep the no-attention path numerically equivalent to current scoring.</action>
  <verify>`cd work2_coding; python scripts/test_attention_menu_logic.py` after Task 3.</verify>
  <acceptance_criteria>
    - Attention uses existing `MenuOffer` fields and metadata only; it does not call routing, mutate simulator state, or change choice semantics.
    - Attention-adjusted objective values are used by both exact and greedy selection paths where applicable.
    - Selected offers receive diagnostics such as `attention_weight`, `attention_score_delta`, `attention_mode`, and `attention_enabled`.
    - `last_policy_diagnostic` records a compact attention weight summary.
  </acceptance_criteria>
  <done>`DSPO_attention` has a real treatment effect in menu selection.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused attention scoring tests</name>
  <files>work2_coding/scripts/test_attention_menu_logic.py</files>
  <read_first>work2_coding/scripts/test_robust_menu_logic.py, work2_coding/scripts/test_menu_runtime_contract.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Create script-style tests using synthetic `MenuOffer` objects and lightweight `DSPO_Menu` instances via `object.__new__` where possible. Cover disabled-attention equivalence, deterministic attention diagnostics, and a constructed case where attention changes the selected meeting-point bundle.</action>
  <verify>`cd work2_coding; python scripts/test_attention_menu_logic.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Tests avoid heavy HGS/runtime execution.
    - Disabled attention returns the same objective/selection as original for the synthetic fixture.
    - Enabled deterministic attention changes selection in at least one fixture.
    - Tests assert diagnostic fields are present and finite.
  </acceptance_criteria>
  <done>Core attention behavior has deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_attention_menu_logic.py` passes.
- [ ] `cd work2_coding; python scripts/test_robust_menu_logic.py` passes.
- [ ] `cd work2_coding; python scripts/test_menu_runtime_contract.py` passes.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] `cd work2_coding; python -m py_compile Src/parser.py Src/Algorithms/DSPO_Menu.py scripts/test_attention_menu_logic.py` passes.
</verification>

<success_criteria>
- `DSPO_original` remains the default no-attention behavior.
- `DSPO_attention` can adjust objective scoring and alter selected bundles.
- Attention diagnostics are recorded on offers and policy diagnostics.
- Phase 6 decisions D-01 through D-05 are represented in code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
