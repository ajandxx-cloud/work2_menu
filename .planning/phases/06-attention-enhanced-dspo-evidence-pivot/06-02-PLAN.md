---
phase: 06-attention-enhanced-dspo-evidence-pivot
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - work2_coding/Src/policy_adapters.py
  - work2_coding/Src/experiment_contracts.py
  - work2_coding/Src/paired_replay.py
  - work2_coding/Src/study_execution.py
  - work2_coding/experiments/studies/smoke_attention_dspo.yaml
  - work2_coding/experiments/studies/pilot_attention_dspo.yaml
  - work2_coding/experiments/studies/formal_attention_dspo.yaml
  - work2_coding/experiments/suites/work2_attention_dspo.yaml
  - work2_coding/scripts/test_attention_manifest_contracts.py
  - work2_coding/scripts/test_attention_paired_rows.py
autonomous: true
requirements:
  - ATTN-01
  - ATTN-03
must_haves:
  truths:
    - "D-06: Main comparison variants are exposed as policy tags DSPO_original and DSPO_attention in manifests and normalized rows."
    - "D-07: DSPO_original and DSPO_attention share menu policy, filter mode, objective mode, pricing, routing/HGS settings, checkpoint policy, request traces, and seeds; only attention behavior substantively differs."
    - "D-08: Normalized rows record method identity and attention metadata explicitly, including method_variant, attention_enabled, attention_mode, and attention diagnostics."
    - "D-12: Tuning/search and held-out/formal evaluation remain separated by manifest split/metadata contracts."
    - "D-13: Attention-specific smoke, pilot, and formal manifests exist and focus on DSPO_original and DSPO_attention."
    - "D-16: Robust policies, no_filter, home_only, and meeting-point-only outputs remain supporting diagnostics and do not enter the main attention ranking."
  artifacts:
    - path: "work2_coding/experiments/studies/smoke_attention_dspo.yaml"
      provides: "Cheap attention-vs-original study contract"
      contains: "DSPO_attention"
    - path: "work2_coding/scripts/test_attention_paired_rows.py"
      provides: "Row schema and pair-alignment tests for attention fields"
      contains: "method_variant"
  key_links:
    - from: "work2_coding/Src/policy_adapters.py"
      to: "work2_coding/Src/paired_replay.py"
      via: "Method adapters provide explicit row metadata"
      pattern: "DSPO_attention"
---

<objective>
Expose `DSPO_original` and `DSPO_attention` as first-class method variants in manifests and normalized rows, with paired replay validation proving that the main comparison only varies attention-specific fields.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-CONTEXT.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-RESEARCH.md
@work2_coding/Src/policy_adapters.py
@work2_coding/Src/experiment_contracts.py
@work2_coding/Src/paired_replay.py
@work2_coding/Src/study_execution.py
@work2_coding/scripts/run_study.py
@work2_coding/experiments/studies/smoke_robust_menu.yaml
@work2_coding/scripts/test_policy_fairness_contract.py
@work2_coding/scripts/test_paired_replay_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add attention method adapters and fairness allowlist</name>
  <files>work2_coding/Src/policy_adapters.py</files>
  <read_first>work2_coding/Src/policy_adapters.py, work2_coding/Src/parser.py</read_first>
  <action>Add adapter metadata for `DSPO_original` and `DSPO_attention`. Both should use the same base menu policy/objective/filter defaults; the attention adapter should enable attention and set `method_variant=DSPO_attention`. Extend policy-only fields to include only method/attention knobs required for the treatment. Preserve robust-menu required policy tags for existing studies.</action>
  <verify>`cd work2_coding; python scripts/test_attention_manifest_contracts.py` after Task 4.</verify>
  <acceptance_criteria>
    - `DSPO_original` metadata is non-diagnostic and comparison role `method`.
    - `DSPO_attention` metadata is non-diagnostic and comparison role `method`.
    - Adapter validation rejects checkpoint, seed, pricing, routing, or HGS drift.
    - Existing robust-menu policy tests still pass.
  </acceptance_criteria>
  <done>Manifest authors can select the two method variants without hand-written runtime overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Support attention-focused manifest requirements</name>
  <files>work2_coding/Src/experiment_contracts.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/experiments/studies/smoke_robust_menu.yaml</read_first>
  <action>Allow a study manifest to declare an attention comparison family or explicit `required_policy_tags` so attention studies require `DSPO_original` and `DSPO_attention` instead of the robust-menu baseline set. Keep the old robust baseline requirement as the default for existing manifests. Add validation that main attention manifests list only the two main method tags unless marked diagnostic or separate-suite.</action>
  <verify>`cd work2_coding; python scripts/test_experiment_contracts.py` and `python scripts/test_attention_manifest_contracts.py`.</verify>
  <acceptance_criteria>
    - Existing robust smoke/pilot/formal manifests remain valid.
    - Attention smoke/pilot/formal manifests require both method tags.
    - Attention pilot/formal manifests retain shared checkpoint and low/medium uptake-regime gates.
    - Formal attention manifests can be contract-valid while still blocked at execution time when checkpoint files are missing.
  </acceptance_criteria>
  <done>Contract validation understands both robust-menu and attention-method study families.</done>
</task>

<task type="auto">
  <name>Task 3: Extend normalized rows with attention identity and pair fields</name>
  <files>work2_coding/Src/paired_replay.py, work2_coding/Src/study_execution.py</files>
  <read_first>work2_coding/Src/paired_replay.py, work2_coding/Src/study_execution.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Add explicit row fields for `method_variant`, `attention_enabled`, `attention_mode`, `attention_strength`, `attention_weight_summary`, `attention_pair_id`, `attention_pair_complete`, and a primary `net_objective_proxy` or equivalent profit/objective field. Populate these fields from adapter args and `last_policy_diagnostic` in actual replay, and from contract settings in contract/blocker rows.</action>
  <verify>`cd work2_coding; python scripts/test_attention_paired_rows.py`.</verify>
  <acceptance_criteria>
    - Row validation requires attention identity fields for every row.
    - Existing robust-menu rows get safe default values such as `DSPO_original` and `attention_enabled=False`.
    - Original/attention rows with the same split, seed, and trace share the same `attention_pair_id`.
    - Pair completeness can be computed deterministically from available rows.
  </acceptance_criteria>
  <done>Artifact builders can compare attention variants without inferring identity from policy tags alone.</done>
</task>

<task type="auto">
  <name>Task 4: Add attention study manifests and manifest tests</name>
  <files>work2_coding/experiments/studies/smoke_attention_dspo.yaml, work2_coding/experiments/studies/pilot_attention_dspo.yaml, work2_coding/experiments/studies/formal_attention_dspo.yaml, work2_coding/experiments/suites/work2_attention_dspo.yaml, work2_coding/scripts/test_attention_manifest_contracts.py, work2_coding/scripts/test_attention_paired_rows.py</files>
  <read_first>work2_coding/experiments/studies/smoke_robust_menu.yaml, work2_coding/experiments/studies/pilot_robust_menu.yaml, work2_coding/experiments/studies/formal_robust_menu.yaml, work2_coding/experiments/suites/work2_robust_menu.yaml</read_first>
  <action>Create attention-specific smoke, pilot, formal, and suite manifests. Smoke should be cheap and runnable. Pilot/formal should require checkpoint provenance. Add script-style tests for required tags, paired fields, varied attention fields, row schema defaults, and rejection of unfair drift.</action>
  <verify>`cd work2_coding; python scripts/test_attention_manifest_contracts.py && python scripts/test_attention_paired_rows.py` prints `PASS` for both.</verify>
  <acceptance_criteria>
    - Main attention manifests include only `DSPO_original` and `DSPO_attention` as claim-ranking methods.
    - `paired_fields` include seed, split/data seed, request/runtime settings, pricing, routing/HGS, checkpoint path/requirement, menu policy, ETA filter, objective mode, `menu_k`, and `max_candidates`.
    - `varied_fields` are limited to method/attention switches.
    - Output schema advertises all attention row fields added in Task 3.
  </acceptance_criteria>
  <done>Phase 6 has reproducible attention-vs-original study contracts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_policy_fairness_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_paired_replay_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_experiment_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_attention_manifest_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_attention_paired_rows.py` passes.
- [ ] `cd work2_coding; python scripts/run_study.py --study smoke_attention_dspo --contract-only` writes normalized rows containing `DSPO_original`, `DSPO_attention`, and explicit attention fields.
- [ ] `cd work2_coding; python -m py_compile Src/policy_adapters.py Src/experiment_contracts.py Src/paired_replay.py Src/study_execution.py scripts/test_attention_manifest_contracts.py scripts/test_attention_paired_rows.py` passes.
</verification>

<success_criteria>
- ATTN-01 variants are explicit and named.
- ATTN-03 paired replay fairness is enforced by manifest and row tests.
- Robust/no-filter/cost-bound diagnostics remain outside the main attention ranking.
- Phase 6 decisions D-06, D-07, D-08, D-12, D-13, and D-16 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-02-SUMMARY.md`.
</output>

## PLANNING COMPLETE
