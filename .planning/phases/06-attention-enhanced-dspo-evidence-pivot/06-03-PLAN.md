---
phase: 06-attention-enhanced-dspo-evidence-pivot
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - work2_coding/Src/attention_artifacts.py
  - work2_coding/scripts/build_attention_artifacts.py
  - work2_coding/scripts/test_attention_artifact_gate.py
  - work2_coding/artifacts/work2_attention_dspo/
  - artifacts/work2_attention_dspo/
autonomous: true
requirements:
  - ATTN-04
must_haves:
  truths:
    - "D-09: Smoke evidence only proves both variants run on identical traces and emit valid schema outputs; it does not unlock directional improvement language."
    - "D-10: The attention-improves-DSPO claim is unlocked only by completed, non-placeholder, checkpoint-valid paired evidence with aligned original/attention rows."
    - "D-11: Net objective or profit proxy is primary; acceptance, opt-out, non-home uptake, and service cost are service constraints that must not materially degrade."
    - "D-14: Attention artifacts use a separate work2_attention_dspo family under work2_coding/artifacts and root artifacts."
    - "D-15: Attention artifacts include original-vs-attention paired deltas and an attention claim guard JSON."
    - "D-16: Robust/no-filter/cost-bound outputs are supporting diagnostics only and are excluded from the main attention claim ranking."
  artifacts:
    - path: "work2_coding/scripts/test_attention_artifact_gate.py"
      provides: "Fail-closed paired-delta and attention claim guard tests"
      contains: "attention_claim_guard"
    - path: "work2_coding/artifacts/work2_attention_dspo/ATTENTION_CLAIM_GUARD.json"
      provides: "Machine-readable attention claim eligibility"
      contains: "attention_improves_dspo"
  key_links:
    - from: "work2_coding/Src/paired_replay.py"
      to: "work2_coding/Src/attention_artifacts.py"
      via: "Attention rows become paired delta and claim guard artifacts"
      pattern: "attention_pair_id"
---

<objective>
Create the `work2_attention_dspo` artifact family: paired original-vs-attention delta outputs plus a fail-closed claim guard that prevents attention superiority claims without completed non-placeholder paired evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-CONTEXT.md
@.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-RESEARCH.md
@work2_coding/Src/artifact_builder.py
@work2_coding/Src/artifact_status.py
@work2_coding/Src/manuscript_claims.py
@work2_coding/Src/paired_replay.py
@work2_coding/scripts/build_artifacts.py
@work2_coding/scripts/test_artifact_gates.py
@work2_coding/scripts/test_manuscript_claim_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paired attention delta helpers</name>
  <files>work2_coding/Src/attention_artifacts.py</files>
  <read_first>work2_coding/Src/artifact_builder.py, work2_coding/Src/artifact_status.py, work2_coding/Src/paired_replay.py</read_first>
  <action>Create reusable helpers that load a study run, group rows by `attention_pair_id`, match `DSPO_original` and `DSPO_attention`, compute pair completeness, and calculate deltas for `net_objective_proxy`, acceptance, opt-out, meeting-point uptake, service cost/time, net price revenue, and attention diagnostics. Missing pairs must be represented as incomplete, not silently dropped.</action>
  <verify>`cd work2_coding; python scripts/test_attention_artifact_gate.py` after Task 3.</verify>
  <acceptance_criteria>
    - Pair matching uses explicit row fields and trace/split/seed identity.
    - Delta rows include original value, attention value, delta, checkpoint statuses, placeholder/status flags, and pair completeness.
    - Aggregation keeps primary metric direction separate from service constraint metrics.
  </acceptance_criteria>
  <done>Attention pair deltas can be computed from normalized rows alone.</done>
</task>

<task type="auto">
  <name>Task 2: Add fail-closed attention claim guard</name>
  <files>work2_coding/Src/attention_artifacts.py</files>
  <read_first>work2_coding/Src/artifact_status.py, work2_coding/Src/manuscript_claims.py</read_first>
  <action>Implement an attention-specific guard payload. It should block `attention_improves_dspo` unless rows are completed, non-placeholder, paired complete, checkpoint-valid when required, pilot/formal or otherwise claim-eligible, and show positive primary-metric delta without material degradation in acceptance, opt-out, non-home uptake, or service cost constraints. Smoke-only runs should always remain schema/execution evidence, not improvement evidence.</action>
  <verify>`cd work2_coding; python scripts/test_attention_artifact_gate.py`.</verify>
  <acceptance_criteria>
    - Guard JSON includes `claim_ready`, `attention_improves_dspo_allowed`, `primary_metric`, `service_constraints`, `pair_completeness`, `checkpoint_statuses`, `blocked_claims`, and `blockers`.
    - Missing or failed checkpoint provenance blocks pilot/formal claims.
    - Placeholder or contract-only rows block improvement claims.
    - Negative or mixed deltas block superiority language while preserving diagnostic outputs.
  </acceptance_criteria>
  <done>Claim eligibility is machine-readable and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3: Add builder CLI, mirror output, and artifact tests</name>
  <files>work2_coding/scripts/build_attention_artifacts.py, work2_coding/scripts/test_attention_artifact_gate.py</files>
  <read_first>work2_coding/scripts/build_artifacts.py, work2_coding/scripts/test_artifact_gates.py, work2_coding/scripts/test_manuscript_claim_guard.py</read_first>
  <action>Add a public CLI that reads a run directory or latest attention study, writes JSON/CSV/LaTeX paired delta artifacts under `work2_coding/artifacts/work2_attention_dspo/`, mirrors lightweight outputs to `artifacts/work2_attention_dspo/`, and emits `ATTENTION_CLAIM_GUARD.json`. Tests should use temporary synthetic rows for claim-ready, smoke-only, missing-pair, bad-checkpoint, placeholder, and negative-delta cases.</action>
  <verify>`cd work2_coding; python scripts/test_attention_artifact_gate.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Generated outputs include paired delta JSON/CSV, a LaTeX table, pair completeness/status JSON, and attention claim guard JSON.
    - The CLI can run with `--allow-incomplete` for smoke or blocked runs.
    - Root mirror contains the same lightweight review artifacts.
    - Tests do not require real checkpoint files or heavy simulator execution.
  </acceptance_criteria>
  <done>`work2_attention_dspo` artifacts are reproducible and reviewable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_attention_artifact_gate.py` passes.
- [ ] `cd work2_coding; python scripts/test_artifact_gates.py` passes.
- [ ] `cd work2_coding; python scripts/test_manuscript_claim_guard.py` passes.
- [ ] `cd work2_coding; python scripts/run_study.py --study smoke_attention_dspo --contract-only` writes rows.
- [ ] `cd work2_coding; python scripts/build_attention_artifacts.py --study smoke_attention_dspo --allow-incomplete --mirror-root ../artifacts/work2_attention_dspo` writes incomplete/blocked smoke artifacts and `ATTENTION_CLAIM_GUARD.json`.
- [ ] `cd work2_coding; python -m py_compile Src/attention_artifacts.py scripts/build_attention_artifacts.py scripts/test_attention_artifact_gate.py` passes.
</verification>

<success_criteria>
- ATTN-04 artifact and claim gates report attention-vs-original deltas.
- Claim guard blocks superiority language for smoke, placeholder, incomplete, unpaired, or checkpoint-invalid evidence.
- The artifact family is separate from `work2_robust_menu`.
- Phase 6 decisions D-09, D-10, D-11, D-14, D-15, and D-16 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-attention-enhanced-dspo-evidence-pivot/06-03-SUMMARY.md`.
</output>

## PLANNING COMPLETE
