# Phase 1 Research: Repository Audit And State Locking

**Researched:** 2026-06-14T22:59:40+08:00
**Mode:** Inline GSD planning research
**Status:** Complete

## Scope

Phase 1 is a diagnostic and documentation phase. It must confirm the current
repository state before any algorithm behavior changes, and it must produce
`.planning/STATE_LOCK.md` as the durable audit artifact.

The active runtime root is `work2_coding/`. Historical `.planning/codebase/`
documents still reference `ooh_code/`; those references are useful prompts for
what to inspect, but they must be rechecked against the current filesystem.

## Evidence Gathered

- The GSD init query identifies Phase 1 as pending, with phase directory
  `.planning/phases/01-repository-audit-and-state-locking`.
- `01-CONTEXT.md` already exists and explicitly defines the audit boundary.
- Lightweight import smoke from `work2_coding/` passed:
  `python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"`
  returned `IMPORT_OK`.
- `git status --short` currently reports 110 changed paths. This is audit
  input only; Phase 1 must not clean, revert, stage, or delete unrelated files.
- The seven mainline tags are present in both manifests and adapter code:
  `mainline_no_menu`, `mainline_fixed_menu`, `mainline_random_menu`,
  `mainline_optimized_m`, `mainline_optimized_mw`,
  `mainline_optimized_fixed_window`, and `mainline_optimized_adaptive`.
- Key active runtime files exist under `work2_coding/`, including:
  `Src/Algorithms/DSPO_Menu.py`, `Src/paired_replay.py`,
  `Src/policy_adapters.py`, `Src/study_execution.py`,
  `Src/formal_readiness.py`, `Src/artifact_builder.py`,
  `Src/artifact_status.py`, and `Src/manuscript_claims.py`.
- Key evidence artifacts are present but not claim-ready:
  `FORMAL_READINESS.json` reports `status: blocked`;
  `ARTIFACT_STATUS.json` reports `claim_ready: False`;
  `CLAIM_GUARD.json` reports `claim_ready: False`.

## Current Filesystem Observations

### Active manifests

`work2_coding/Experiments/studies/` includes robust-menu manifests for smoke,
pilot, and formal evidence:

- `smoke_robust_menu.yaml`
- `pilot_robust_menu.yaml`
- `formal_robust_menu.yaml`

It also includes attention and later-phase diagnostic manifests. Phase 1 should
inventory them, but attention remains outside the v1 main claim scope.

### Test style

Tests are script-style Python files in `work2_coding/scripts/test_*.py`, using
direct execution rather than a configured repository-wide test runner. Relevant
Phase 1 inventory targets include:

- `test_optout_accounting.py`
- `test_paired_replay_contract.py`
- `test_policy_fairness_contract.py`
- `test_artifact_gates.py`
- `test_formal_readiness.py`
- `test_study_execution_status.py`
- `test_checkpoint_provenance.py`
- `test_smoke_study_rows.py`

### Existing output status

The formal shared checkpoint path exists:
`work2_coding/outputs/shared_training/work2_robust_menu/formal/supervised_ml.pt`.

The readiness and claim-gate artifacts exist, but their current metadata does
not support a formal paper claim yet. Phase 1 should record this status plainly
and leave claim interpretation to later phases.

## Planning Implications

1. The execution plan should be a single audit plan. Parallel plans would add
   coordination overhead without reducing risk.
2. The only intended deliverable is `.planning/STATE_LOCK.md`, plus the normal
   GSD execution summary generated by execute-phase.
3. The plan must explicitly forbid heavy studies, formal replay, checkpoint
   training, artifact regeneration, manuscript claim upgrades, and destructive
   git cleanup.
4. Verification should rely on the import smoke and structural checks. Any
   optional test runs must be cheap, read-mostly, and recorded as diagnostic.

## Research Complete

Phase 1 has enough context for planning. The plan should translate the context
decisions D-01 through D-12 into observable `must_haves` and executable tasks.

## RESEARCH COMPLETE
