---
phase: 02-core-semantics-and-robust-menu-logic
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - work2_coding/Src/parser.py
  - work2_coding/Src/Algorithms/Agent.py
  - work2_coding/Src/Utils/Predictors.py
  - work2_coding/Src/Utils/Utils.py
  - work2_coding/Src/config.py
  - work2_coding/scripts/test_checkpoint_provenance.py
autonomous: true
requirements:
  - ACCT-04
user_setup: []
must_haves:
  truths:
    - "D-09: Formal and pilot comparisons fail closed when a required shared predictor checkpoint fails to load; smoke/diagnostic fallback is explicit in metadata."
    - "D-10: Result metadata reaches formal provenance level: status, path, error, hash, model type, compatibility reason, run id, settings summary, and intentional mismatch marker."
    - "D-11: Intentional mismatch or random initialization is diagnostic only and must not enter formal policy comparisons."
    - "D-12: Tests cover load success/failure, hash/provenance, and allowed/blocked intentional mismatch behavior."
  artifacts:
    - path: "work2_coding/scripts/test_checkpoint_provenance.py"
      provides: "Checkpoint load status and provenance contract tests"
      min_lines: 80
      contains: "checkpoint_load_status"
  key_links:
    - from: "work2_coding/Src/Algorithms/Agent.py"
      to: "work2_coding/Src/Utils/Predictors.py"
      via: "Structured checkpoint load wrapper"
      pattern: "checkpoint_load_status"
---

<objective>
Make predictor checkpoint loading explicit, provenance-bearing, and fail-closed for pilot/formal comparisons.

Purpose: prevent silent random initialization or incompatible checkpoint reuse from entering later robust-menu policy comparisons.

Output: structured checkpoint status metadata, parser/config run-mode controls, and deterministic checkpoint provenance tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-audit-and-runtime-baseline/01-STAGE0-AUDIT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-RESEARCH.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-01-PLAN.md

@work2_coding/Src/parser.py
@work2_coding/Src/config.py
@work2_coding/Src/Algorithms/Agent.py
@work2_coding/Src/Utils/Predictors.py
@work2_coding/Src/Utils/Utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checkpoint run-mode and provenance controls</name>
  <files>work2_coding/Src/parser.py, work2_coding/Src/config.py</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Src/config.py, .planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md</read_first>
  <action>Add parser flags for `run_mode` with choices such as `smoke`, `diagnostic`, `pilot`, and `formal`; `checkpoint_path`; `require_checkpoint`; `allow_checkpoint_mismatch`; and optional `run_id`/metadata summary fields if absent. In `Config`, initialize a `checkpoint_metadata` dictionary with default explicit status such as `not_requested`, path, run mode, requirement flag, and intentional mismatch flag. Do not load checkpoints during `Config(args)` unless the existing runtime already does so; keep load behavior close to algorithms/modules.</action>
  <verify>`python -c "import sys; sys.path.insert(0, 'work2_coding'); from Src.parser import Parser; args=Parser().get_parser().parse_args(['--run_mode','formal','--require_checkpoint','True','--checkpoint_path','dummy.pt']); print(args.run_mode, args.require_checkpoint, args.checkpoint_path)"` prints `formal True dummy.pt`.</verify>
  <acceptance_criteria>
    - Run mode is explicit and parseable.
    - Formal/pilot strictness can be determined from config without guessing.
    - Default smoke/diagnostic behavior remains conservative and metadata-visible.
  </acceptance_criteria>
  <done>Runtime config can distinguish diagnostic fallback from formal required checkpoint loading.</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured load status and safe hash provenance</name>
  <files>work2_coding/Src/Algorithms/Agent.py, work2_coding/Src/Utils/Predictors.py, work2_coding/Src/Utils/Utils.py</files>
  <read_first>work2_coding/Src/Algorithms/Agent.py, work2_coding/Src/Utils/Predictors.py, work2_coding/Src/Utils/Utils.py</read_first>
  <action>Add a small checkpoint status helper that records `checkpoint_load_status`, `checkpoint_path`, `checkpoint_hash`, `checkpoint_load_error`, `checkpoint_model_type`, `checkpoint_compatibility_reason`, `checkpoint_intentional_mismatch`, and `checkpoint_required`. Use SHA-256 for existing files. Update predictor/module `load()` methods to return or populate status on success/failure. Prefer `torch.load(..., map_location='cpu', weights_only=True)` when supported, with a version-compatible fallback for local trusted files. Do not swallow load exceptions silently; return structured failure for diagnostic modes and raise for required formal/pilot loads.</action>
  <verify>Create a tiny temporary model in the test script and confirm success metadata includes `loaded` and a hash; missing-path load records or raises failure according to mode.</verify>
  <acceptance_criteria>
    - Success status includes path, hash, and model type.
    - Missing/incompatible checkpoint records the error text.
    - Required formal/pilot load failure raises a clear exception.
    - Diagnostic fallback metadata cannot be mistaken for loaded formal provenance.
  </acceptance_criteria>
  <done>Checkpoint load behavior is explicit and provenance-bearing.</done>
</task>

<task type="auto">
  <name>Task 3: Attach checkpoint metadata to algorithms for later rows</name>
  <files>work2_coding/Src/Algorithms/Agent.py, work2_coding/Src/config.py</files>
  <read_first>work2_coding/Src/Algorithms/Agent.py, work2_coding/Src/config.py, work2_coding/run.py</read_first>
  <action>Add an `Agent.load_checkpoint(...)` or equivalent method that iterates known modules and updates `self.checkpoint_metadata` and `self.config.checkpoint_metadata`. If no checkpoint is requested, status must remain explicit (`not_requested`). If a diagnostic mismatch is allowed, status must include `intentional_mismatch` or equivalent and should not be `loaded`. Keep this as metadata plumbing only; do not create formal normalized result rows in Phase 2.</action>
  <verify>`rg -n "checkpoint_metadata|checkpoint_load_status|not_requested|intentional_mismatch" work2_coding/Src` finds explicit metadata fields.</verify>
  <acceptance_criteria>
    - Algorithm instances expose checkpoint metadata for later row writers.
    - `Config` carries the same metadata or a safe copy.
    - Formal/pilot fail-closed behavior is reachable from the algorithm load path.
  </acceptance_criteria>
  <done>Checkpoint metadata is accessible to later experiment/result surfaces.</done>
</task>

<task type="auto">
  <name>Task 4: Add checkpoint provenance tests</name>
  <files>work2_coding/scripts/test_checkpoint_provenance.py</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Src/Algorithms/Agent.py, work2_coding/Src/Utils/Predictors.py, work2_coding/Src/Utils/Utils.py</read_first>
  <action>Create a script-style test using `TemporaryDirectory`. Cover: parser run-mode flags; successful save/load of a tiny predictor with hash metadata; missing checkpoint in smoke/diagnostic mode records explicit fallback/failure metadata; missing checkpoint in formal required mode raises; intentional mismatch is marked diagnostic-only. Avoid full `Config(args)` construction and avoid HGS/data loading.</action>
  <verify>Run `cd work2_coding; python scripts/test_checkpoint_provenance.py` and confirm it prints a PASS line.</verify>
  <acceptance_criteria>
    - Test asserts `checkpoint_load_status` values, not just absence of exceptions.
    - Test asserts hash is present for success.
    - Test asserts formal required load failure is blocked.
    - Test asserts intentional mismatch cannot appear as a formal loaded checkpoint.
  </acceptance_criteria>
  <done>Checkpoint provenance has deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_menu_runtime_contract.py` still passes.
- [ ] `cd work2_coding; python scripts/test_checkpoint_provenance.py` passes.
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] `rg -n "checkpoint_load_status|checkpoint_hash|intentional_mismatch|require_checkpoint" work2_coding/Src work2_coding/scripts` finds status/provenance coverage.
</verification>

<success_criteria>
- ACCT-04 is covered: checkpoint load success, failure, intentional mismatch, and provenance are explicit in runtime metadata.
- CONTEXT decisions D-09 through D-12 are represented.
- The plan can run autonomously after Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-semantics-and-robust-menu-logic/02-03-SUMMARY.md`.
</output>

## PLANNING COMPLETE
