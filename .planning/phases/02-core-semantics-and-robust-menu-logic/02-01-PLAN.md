---
phase: 02-core-semantics-and-robust-menu-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - work2_coding/Src/parser.py
  - work2_coding/Environments/OOH/containers.py
  - work2_coding/Src/Utils/option_features.py
  - work2_coding/Src/Utils/Predictors.py
  - work2_coding/Src/Utils/Utils.py
  - work2_coding/scripts/test_menu_runtime_contract.py
autonomous: true
requirements:
  - ACCT-01
  - ETA-01
  - MENU-03
user_setup: []
must_haves:
  truths:
    - "D-01: Use minimal menu runtime activation: `Src.config` import and parser contract, not a full study run."
    - "D-02: Add only the missing `MenuOffer`, `ServiceBundle`, and `Src.Utils.option_features` contracts needed by `DSPO_Menu.py`."
    - "D-03: Expose `DSPO_Menu` and required `menu_*` controls in `work2_coding/Src/parser.py` with conservative defaults."
    - "D-04: Acceptance target is import plus parser contract; constructor smoke is optional only if it stays low-side-effect."
    - "D-20/D-21: Parser defaults must expose exact/greedy thresholds and diagnostic controls needed by later solver metadata."
  artifacts:
    - path: "work2_coding/scripts/test_menu_runtime_contract.py"
      provides: "Import/parser/dataclass/helper compatibility checks for menu runtime"
      min_lines: 40
      contains: "DSPO_Menu"
  key_links:
    - from: "work2_coding/Src/parser.py"
      to: "work2_coding/Src/Algorithms/DSPO_Menu.py"
      via: "Algorithm registration and menu runtime flags"
      pattern: "menu_"
    - from: "work2_coding/Environments/OOH/containers.py"
      to: "work2_coding/Src/Algorithms/DSPO_Menu.py"
      via: "ServiceBundle and MenuOffer contracts"
      pattern: "MenuOffer"
---

<objective>
Restore the low-side-effect menu runtime contract so `DSPO_Menu` can be imported and configured through `work2_coding/` without creating a parallel runtime root.

Purpose: unblock all later Phase 2 work by making the existing menu algorithm asset compatible with the current parser, containers, option-feature helper, and predictor constructor surfaces.

Output: parser/config-compatible menu controls plus a deterministic script-style runtime contract test.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-repository-audit-and-runtime-baseline/01-STAGE0-AUDIT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-RESEARCH.md

@work2_coding/Src/parser.py
@work2_coding/Src/config.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
@work2_coding/Src/Utils/Predictors.py
@work2_coding/Src/Utils/Utils.py
@work2_coding/Environments/OOH/containers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose the menu runtime CLI contract</name>
  <files>work2_coding/Src/parser.py</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Src/Algorithms/DSPO_Menu.py, .planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md</read_first>
  <action>Add `DSPO_Menu` to `--algo_name` choices. Add a compact `Menu_parameters(parser)` section and call it from `Parser.__init__`. Include conservative defaults for every field read by `DSPO_Menu.__init__`: `menu_mode`, `menu_policy`, `menu_k`, `max_candidates`, `menu_keep_home`, `menu_use_exact_eval`, `menu_exact_threshold`, `menu_selection_solver`, `menu_exact_gap_threshold`, `menu_objective_mode`, `menu_time_filtering`, pickup/display window controls, time-risk scoring weights, route/capacity risk lambdas, time-head flags, ETA controls, pricing controls, service quit/guardrail controls, `freeze_learning`, and `eval_only`. Include Phase 2 modes in choices: `hard`, `calibrated`, `interval_overlap`, `chance_constraint`, `soft_penalty`, and `none`. Do not instantiate `Config(args)` in this task.</action>
  <verify>From the repository root, run `python -c "import sys; sys.path.insert(0, 'work2_coding'); from Src.parser import Parser; p=Parser().get_parser(); args=p.parse_args(['--algo_name','DSPO_Menu','--menu_mode','True','--menu_eta_filter_mode','chance_constraint']); print(args.algo_name, args.menu_mode, args.menu_eta_filter_mode)"` and confirm it prints `DSPO_Menu True chance_constraint`.</verify>
  <acceptance_criteria>
    - Parser accepts `--algo_name DSPO_Menu`.
    - Parser accepts every `menu_*` field read by `DSPO_Menu.__init__`.
    - Defaults keep menu mode disabled unless the user selects `DSPO_Menu` or explicitly sets `--menu_mode True`.
  </acceptance_criteria>
  <done>`DSPO_Menu` and Phase 2 menu flags are parseable with no runtime side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing menu domain and option-feature contracts</name>
  <files>work2_coding/Environments/OOH/containers.py, work2_coding/Src/Utils/option_features.py</files>
  <read_first>work2_coding/Environments/OOH/containers.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Add minimal dataclasses for `ServiceBundle` and `MenuOffer` in `containers.py`. Preserve existing dataclasses and `__getitem__` behavior. `MenuOffer` must expose direct properties used by `DSPO_Menu.py`, including `bundle_id`, `is_home`, `location`, and `parcelpoint_id`, either as fields or properties mapped from `bundle`. Create `Src/Utils/option_features.py` with `normalize_features(raw, menu_time_scale, menu_target_arrival_time)` and `build_option_tensor(normed, max_k, device)` returning padded feature and mask tensors. Keep helper behavior deterministic and free of external dependencies beyond NumPy/Torch.</action>
  <verify>`python -c "import sys; sys.path.insert(0, 'work2_coding'); from Environments.OOH.containers import ServiceBundle, MenuOffer; from Src.Utils.option_features import normalize_features, build_option_tensor; print('MENU_CONTRACT_OK')"` prints `MENU_CONTRACT_OK`.</verify>
  <acceptance_criteria>
    - `DSPO_Menu.py` imports `MenuOffer`, `ServiceBundle`, `normalize_features`, and `build_option_tensor`.
    - Option tensor helper returns a fixed `max_k x 6` tensor and boolean mask without resizing the layout.
    - Existing `Location`, `ParcelPoint`, `ParcelPoints`, `Vehicle`, `Fleet`, and `Customer` contracts remain compatible.
  </acceptance_criteria>
  <done>Missing menu containers and option-feature helpers exist with the narrow shape expected by `DSPO_Menu.py`.</done>
</task>

<task type="auto">
  <name>Task 3: Make predictors compatible with menu auxiliary outputs</name>
  <files>work2_coding/Src/Utils/Predictors.py, work2_coding/Src/Utils/Utils.py</files>
  <read_first>work2_coding/Src/Utils/Predictors.py, work2_coding/Src/Utils/Utils.py, work2_coding/Src/Algorithms/DSPO.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Extend `LinReg`, `CNN_2d`, and `CNN_3d` constructors to accept optional `aux_dim=1` and `output_dim=1` while preserving legacy defaults. Replace hard-coded capacity feature width assumptions with `aux_dim`, and make final layers emit `output_dim`. Update `MemoryBuffer` only as needed so `target_dim` and `aux_dim` work for both legacy DSPO and `DSPO_Menu`; preserve the existing default single-output behavior.</action>
  <verify>`python -c "import sys; sys.path.insert(0, 'work2_coding'); from Src.Utils.Predictors import CNN_2d, LinReg; CNN_2d(11,2,16,0.05,aux_dim=4,output_dim=3); LinReg(242,aux_dim=4,output_dim=3); print('PREDICTOR_CONTRACT_OK')"` prints `PREDICTOR_CONTRACT_OK`.</verify>
  <acceptance_criteria>
    - Legacy constructor calls in `DSPO.py` still work.
    - Menu constructor calls in `DSPO_Menu.py` work.
    - `MemoryBuffer(..., target_dim=3, aux_dim=4)` can allocate, add, and sample small synthetic rows.
  </acceptance_criteria>
  <done>Predictor and memory contracts support both legacy scalar cost prediction and menu cost/ETA/IVT outputs.</done>
</task>

<task type="auto">
  <name>Task 4: Add runtime contract regression script</name>
  <files>work2_coding/scripts/test_menu_runtime_contract.py</files>
  <read_first>work2_coding/Src/parser.py, work2_coding/Environments/OOH/containers.py, work2_coding/Src/Utils/option_features.py, work2_coding/Src/Utils/Predictors.py</read_first>
  <action>Create `work2_coding/scripts/` if absent and add a script-style test with direct asserts. Cover: `Src.config` import; parser accepts `DSPO_Menu` and representative menu flags; containers instantiate a home and meeting-point `MenuOffer`; option-feature padding/mask shape; predictors instantiate with legacy and menu output shapes; no `Config(args)` construction in this script.</action>
  <verify>Run `cd work2_coding; python scripts/test_menu_runtime_contract.py` and confirm it prints a PASS line.</verify>
  <acceptance_criteria>
    - Test fails before the runtime contract is implemented and passes after.
    - Test is deterministic and has no filesystem output other than Python bytecode cache.
    - Test does not run `run.py`, `run_ppo.py`, HGS, data loading, or episodes.
  </acceptance_criteria>
  <done>Menu runtime contract has a repeatable low-side-effect regression check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] Parser accepts `--algo_name DSPO_Menu --menu_mode True --menu_eta_filter_mode chance_constraint`.
- [ ] `cd work2_coding; python scripts/test_menu_runtime_contract.py` passes.
- [ ] No `ooh_code/` root or files are created.
</verification>

<success_criteria>
- ACCT-01 is partially enabled by providing the domain surfaces later used by structured outcomes.
- ETA-01 is partially enabled by parser support for required ETA filter mode names.
- MENU-03 is partially enabled by parser support for exact/greedy controls.
- CONTEXT decisions D-01 through D-04 and D-20 through D-21 are represented.
- The plan can run autonomously.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-semantics-and-robust-menu-logic/02-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
