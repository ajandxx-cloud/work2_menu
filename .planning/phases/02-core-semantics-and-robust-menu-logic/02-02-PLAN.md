---
phase: 02-core-semantics-and-robust-menu-logic
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - work2_coding/Environments/OOH/containers.py
  - work2_coding/Environments/OOH/customerchoice.py
  - work2_coding/Environments/OOH/Parcelpoint_py.py
  - work2_coding/Src/Utils/Utils.py
  - work2_coding/run.py
  - work2_coding/run_ppo.py
  - work2_coding/scripts/test_optout_accounting.py
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
user_setup: []
must_haves:
  truths:
    - "D-05: Passenger choice is represented by a structured result carrying outcome, location, offer, price, and route_mutates."
    - "D-06: `opted_out` is recorded separately but does not append to HGS/routing data, mutate fleet routes, increment accepted home delivery, or add accepted-service time."
    - "D-07: `Parcelpoint_py.step()` must support both legacy tuple returns and structured choice results during migration."
    - "D-08: Deterministic script tests cover forced opt-out non-mutation, all three outcomes, opt-out count, acceptance rate, service cost, and route-state effects."
  artifacts:
    - path: "work2_coding/scripts/test_optout_accounting.py"
      provides: "Deterministic opt-out and accepted-service accounting checks"
      min_lines: 80
      contains: "opted_out"
  key_links:
    - from: "work2_coding/Environments/OOH/customerchoice.py"
      to: "work2_coding/Environments/OOH/Parcelpoint_py.py"
      via: "Structured choice result adapter"
      pattern: "ChoiceResult"
---

<objective>
Separate opted-out demand from accepted home and accepted meeting-point service in the simulator transition.

Purpose: prevent outside-option choices from contaminating route costs, service time, home-delivery counts, acceptance rates, and later robust-menu evidence.

Output: structured choice-result support, dual legacy adapter, opt-out logs/metrics, and deterministic script-style tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-audit-and-runtime-baseline/01-STAGE0-AUDIT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-RESEARCH.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-01-PLAN.md

@work2_coding/Environments/OOH/containers.py
@work2_coding/Environments/OOH/customerchoice.py
@work2_coding/Environments/OOH/Parcelpoint_py.py
@work2_coding/Src/Utils/Utils.py
@work2_coding/run.py
@work2_coding/run_ppo.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce structured choice results with legacy conversion</name>
  <files>work2_coding/Environments/OOH/containers.py, work2_coding/Environments/OOH/customerchoice.py</files>
  <read_first>work2_coding/Environments/OOH/containers.py, work2_coding/Environments/OOH/customerchoice.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Add a minimal `ChoiceResult` dataclass with fields `outcome`, `location`, `offer`, `price`, `parcelpoint_id`, `route_mutates`, and `metadata`. Add helpers or constructors for `accepted_home`, `accepted_meeting_point`, and `opted_out`. Update customer-choice paths to optionally return structured results while preserving legacy tuple compatibility. For menu actions returned as a list of `MenuOffer`, compute MNL utilities over displayed offers plus outside option; return `opted_out` when outside is chosen, `accepted_home` for a home offer, and `accepted_meeting_point` for a non-home offer. Keep existing offer/pricing tuple paths working.</action>
  <verify>`python -c "import sys; sys.path.insert(0, 'work2_coding'); from Environments.OOH.containers import ChoiceResult; print(ChoiceResult(outcome='opted_out', location=None, offer=None, price=0.0, parcelpoint_id=-1, route_mutates=False).outcome)"` prints `opted_out`.</verify>
  <acceptance_criteria>
    - Structured result distinguishes exactly `accepted_home`, `accepted_meeting_point`, and `opted_out`.
    - Legacy tuple returns can be converted into structured results inside the simulator.
    - Menu-offer actions do not force outside option through a fake home accepted-service offer.
  </acceptance_criteria>
  <done>Choice model surfaces can represent opt-out separately from accepted home pickup.</done>
</task>

<task type="auto">
  <name>Task 2: Guard simulator mutation by outcome</name>
  <files>work2_coding/Environments/OOH/Parcelpoint_py.py</files>
  <read_first>work2_coding/Environments/OOH/Parcelpoint_py.py, work2_coding/Environments/OOH/customerchoice.py, work2_coding/Environments/OOH/containers.py</read_first>
  <action>Add an adapter in `Parcelpoint_py.step()` that normalizes either legacy tuple returns or `ChoiceResult` into a structured result. If `route_mutates` is false or `outcome == 'opted_out'`, do not append coordinates/id/time to `self.data`, do not call `cheapestInsertionRoute`, do not mutate route plans, do not decrement parcel-point capacity, do not increment `count_home_delivery`, and do not add service time. Add `count_opted_out`, `count_accepted_home`, `count_accepted_meeting_point`, `last_choice_result`, and `choice_log` fields reset on `reset()`. Populate `last_selected_offer` for accepted menu offers so `DSPO_Menu.update()` keeps working; leave it `None` or clearly opted-out for `opted_out`.</action>
  <verify>Use the opt-out test script from Task 4. Also run an import smoke after edits: `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Environments.OOH.Parcelpoint_py; print('PARCELPOINT_OK')"`.</verify>
  <acceptance_criteria>
    - Opt-out leaves `self.data['id']`, fleet route lengths, parcel-point capacities, `service_time`, and `count_home_delivery` unchanged except for demand/log counters.
    - Accepted home still mutates route/service state as legacy home delivery.
    - Accepted meeting point still decrements parcel-point capacity and mutates route state.
  </acceptance_criteria>
  <done>Simulator transition honors choice outcome when mutating routing and service state.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve downstream stats while adding opt-out metrics</name>
  <files>work2_coding/Environments/OOH/Parcelpoint_py.py, work2_coding/Src/Utils/Utils.py, work2_coding/run.py, work2_coding/run_ppo.py</files>
  <read_first>work2_coding/run.py, work2_coding/run_ppo.py, work2_coding/Src/Utils/Utils.py, work2_coding/Environments/OOH/Parcelpoint_py.py</read_first>
  <action>Keep the legacy stats tuple indexes used by `run.py` and `run_ppo.py` stable where possible. Append or expose additional opt-out fields through a stats dict/log attribute rather than shifting existing positions. Add acceptance-rate and opt-out-rate calculation helpers close to simulator stats or `Utils`, but do not create formal normalized rows in Phase 2. Update `run.py` and `run_ppo.py` only if needed to avoid assumptions that menu actions are always splattable numeric arrays.</action>
  <verify>`rg -n "stats\\[[0-9]+\\]|count_opted_out|acceptance_rate|choice_log" work2_coding` shows legacy stats references remain understandable and new opt-out metrics are available.</verify>
  <acceptance_criteria>
    - Existing legacy training/eval loops can still consume stats positions 1, 2, 3, 4, 5, 6, and 7.
    - Opt-out count and acceptance rate are available from simulator state or stats metadata.
    - No generated result rows or paper artifacts are hand-edited.
  </acceptance_criteria>
  <done>Opt-out metrics are exposed without breaking legacy tuple consumers.</done>
</task>

<task type="auto">
  <name>Task 4: Add deterministic opt-out accounting tests</name>
  <files>work2_coding/scripts/test_optout_accounting.py</files>
  <read_first>work2_coding/Environments/OOH/Parcelpoint_py.py, work2_coding/Environments/OOH/containers.py, work2_coding/Environments/OOH/customerchoice.py</read_first>
  <action>Create a script-style test that avoids HGS/data loading by constructing a generated-data or hand-built lightweight environment state. Force each of the three outcomes by stubbing `get_delivery_loc` or `customerChoice`. Assert opt-out does not mutate route data or service/home counters; accepted home mutates home service accounting; accepted meeting point decrements capacity and appends to route state; opt-out count and acceptance rate are correct. Keep the test deterministic and runnable with `python scripts/test_optout_accounting.py` from `work2_coding/`.</action>
  <verify>Run `cd work2_coding; python scripts/test_optout_accounting.py` and confirm it prints a PASS line.</verify>
  <acceptance_criteria>
    - Test covers `accepted_home`, `accepted_meeting_point`, and `opted_out`.
    - Test explicitly checks route length or route data id count before/after opt-out.
    - Test explicitly checks `count_opted_out`, `count_home_delivery`, acceptance rate, and service-time effects.
  </acceptance_criteria>
  <done>Opt-out accounting has deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_menu_runtime_contract.py` still passes.
- [ ] `cd work2_coding; python scripts/test_optout_accounting.py` passes.
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] `rg -n "opted_out|accepted_home|accepted_meeting_point|route_mutates" work2_coding/Environments/OOH` finds the structured semantics.
</verification>

<success_criteria>
- ACCT-01 is covered: outcomes are represented as `accepted_home`, `accepted_meeting_point`, and `opted_out`.
- ACCT-02 is covered: opted-out events do not mutate route plans as accepted home service.
- ACCT-03 is covered: opt-out count, acceptance rate, service cost, and route-state effects are testable.
- CONTEXT decisions D-05 through D-08 are represented.
- The plan can run autonomously after Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-semantics-and-robust-menu-logic/02-02-SUMMARY.md`.
</output>

## PLANNING COMPLETE
