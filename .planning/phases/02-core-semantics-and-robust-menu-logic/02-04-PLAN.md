---
phase: 02-core-semantics-and-robust-menu-logic
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02-PLAN.md
  - 02-03-PLAN.md
files_modified:
  - work2_coding/Src/Algorithms/DSPO_Menu.py
  - work2_coding/Src/parser.py
  - work2_coding/scripts/test_robust_menu_logic.py
autonomous: true
requirements:
  - ETA-01
  - ETA-02
  - ETA-03
  - ETA-04
  - MENU-01
  - MENU-02
  - MENU-03
  - MENU-04
user_setup: []
must_haves:
  truths:
    - "D-13: Implement core robust ETA modes `hard`, `calibrated`, `interval_overlap`, `chance_constraint`, `soft_penalty`, and `none`."
    - "D-14: `none` disables ETA pruning only; routing/capacity feasibility still apply. Wider candidate upper-bound diagnostics must use a separate name."
    - "D-15: Candidate diagnostics include predicted ETA, sigma, bounds, pass/fail, prune reason, violation probability, filter mode, ETA source/variant, true/oracle ETA when available, soft penalty, risk score, and retained-in-objective flag."
    - "D-16: `soft_penalty` preserves candidates and applies time risk only through objective penalty."
    - "D-17: Use an initial global sigma/config value and record its source."
    - "D-18/D-22: Preserve both `risk_adjusted_expected_profit` and `service_guarded_expected_profit` as dual mainline candidate methods; do not choose a final policy."
    - "D-19: Service guardrail picks highest-profit feasible menu when possible and lowest-opt-out fallback with reason when not."
    - "D-20/D-21: Exact-small/greedy-large switching and diagnostics record solver, candidate count, enumerated count, gap, overlap, build time, and fallback reason."
    - "D-23: Add objective/solver tests for menu ordering, solver switching, diagnostic fields, gap metrics, and fallback reasons."
  artifacts:
    - path: "work2_coding/scripts/test_robust_menu_logic.py"
      provides: "Robust ETA filter, objective, and exact/greedy diagnostics tests"
      min_lines: 120
      contains: "chance_constraint"
  key_links:
    - from: "work2_coding/Src/Algorithms/DSPO_Menu.py"
      to: "work2_coding/scripts/test_robust_menu_logic.py"
      via: "Filter/objective/solver metadata contract"
      pattern: "eta_filter_mode"
---

<objective>
Lock robust ETA filter semantics, menu objective behavior, and exact-small/greedy-large solver diagnostics in `DSPO_Menu`.

Purpose: make the Phase 2 menu algorithm scientifically safe enough for Phase 3 paired replay contracts without selecting a final winning policy or generating paper artifacts.

Output: robust filter/objective/solver implementation fixes plus deterministic script-style tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-audit-and-runtime-baseline/01-STAGE0-AUDIT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-RESEARCH.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-01-PLAN.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-02-PLAN.md
@.planning/phases/02-core-semantics-and-robust-menu-logic/02-03-PLAN.md

@work2_coding/Src/Algorithms/DSPO_Menu.py
@work2_coding/Src/parser.py
@work2_coding/Environments/OOH/containers.py
@work2_coding/Environments/OOH/Parcelpoint_py.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize robust ETA filter modes and diagnostics</name>
  <files>work2_coding/Src/Algorithms/DSPO_Menu.py, work2_coding/Src/parser.py</files>
  <read_first>work2_coding/Src/Algorithms/DSPO_Menu.py, work2_coding/Src/parser.py, .planning/phases/02-core-semantics-and-robust-menu-logic/02-CONTEXT.md</read_first>
  <action>Refactor ETA filtering around a small helper that evaluates one candidate and returns diagnostic metadata. Support mode aliases only for backward compatibility (`interval` may map to `interval_overlap`), but emit canonical mode names. For every candidate considered, record predicted ETA, filter ETA, sigma, sigma source, preferred/display window bounds, interval bounds, pass/fail, prune reason, violation probability, filter mode, ETA source/variant, true/oracle ETA when available, soft penalty value, risk score, and `retained_in_objective`. Ensure `none` skips ETA pruning but still leaves capacity/routing candidate checks intact. Add a distinct diagnostic name for any wider all-feasible behavior if needed; do not overload `none`.</action>
  <verify>`cd work2_coding; python scripts/test_robust_menu_logic.py --filter-only` if implemented, or the full test script from Task 4.</verify>
  <acceptance_criteria>
    - `hard` prunes outside the preferred window.
    - `interval_overlap` passes when ETA uncertainty interval overlaps the allowed window.
    - `chance_constraint` prunes by violation probability threshold.
    - `soft_penalty` never prunes for ETA risk.
    - `none` never prunes for ETA risk but still excludes capacity-infeasible candidates.
  </acceptance_criteria>
  <done>ETA filtering has canonical modes and audit-grade candidate diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Apply ETA risk and opt-out penalties in objective metadata</name>
  <files>work2_coding/Src/Algorithms/DSPO_Menu.py</files>
  <read_first>work2_coding/Src/Algorithms/DSPO_Menu.py, work2_coding/Environments/OOH/containers.py</read_first>
  <action>Update objective evaluation so `risk_adjusted_expected_profit` includes opt-out penalty and ETA risk penalty semantics; `service_guarded_expected_profit` uses opt-out/service guardrails and fallback reason semantics; `soft_penalty` risk affects objective value without removing the candidate. Keep pricing and system-aware cost definitions consistent across compared policies. Preserve both objective families as candidate methods and do not set a final recommended policy.</action>
  <verify>Full `test_robust_menu_logic.py` asserts hand-built menus rank differently when ETA risk or opt-out penalties change.</verify>
  <acceptance_criteria>
    - Expected profit, opt-out penalty, ETA risk penalty, and service guardrail fields are visible in selected-offer metadata.
    - Service-guarded selection chooses highest-profit guardrail-feasible menu when one exists.
    - If no guardrail-feasible menu exists, fallback chooses lowest predicted opt-out and records the reason.
  </acceptance_criteria>
  <done>Menu objectives encode robust risk and service guardrail semantics without choosing a final policy winner.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten exact-small and greedy-large diagnostics</name>
  <files>work2_coding/Src/Algorithms/DSPO_Menu.py</files>
  <read_first>work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Ensure solver selection consistently records `menu_selection_solver`, effective solver used, candidate count, enumerated count, exact/greedy build times, exact and greedy values when measured, relative gap, overlap, threshold, time-budget fallback status, and fallback reason. Add or expose a time-budget fallback control if already implied by context/parser. For small candidate sets, compute exact-vs-greedy diagnostics; for large sets, use greedy and record why exact was skipped.</action>
  <verify>Full `test_robust_menu_logic.py` asserts exact-small uses exact diagnostics and greedy-large records threshold/time fallback reasons.</verify>
  <acceptance_criteria>
    - Exact enumeration is limited to configured small sets.
    - Greedy fallback is used for larger or over-budget sets and is recorded.
    - Exact-vs-greedy relative gap and overlap are logged for small diagnostic sets.
    - Menu size respects `menu_k` and `menu_keep_home`.
  </acceptance_criteria>
  <done>Solver diagnostics are complete enough for Phase 3/4 approximation evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Add robust menu logic regression tests</name>
  <files>work2_coding/scripts/test_robust_menu_logic.py</files>
  <read_first>work2_coding/Src/Algorithms/DSPO_Menu.py, work2_coding/Environments/OOH/containers.py, work2_coding/scripts/test_menu_runtime_contract.py, work2_coding/scripts/test_optout_accounting.py</read_first>
  <action>Create a script-style test using lightweight synthetic `MenuOffer`, `ServiceBundle`, customer, and `object.__new__(DSPO_Menu)` fixtures where possible. Cover filter modes, candidate diagnostic fields, `soft_penalty` retention, `none` boundary, objective ordering under opt-out/ETA risk penalties, service-guarded fallback, exact-small enumeration diagnostics, greedy-large fallback diagnostics, and menu size constraints. Avoid HGS/data loading and avoid formal experiment artifacts.</action>
  <verify>Run `cd work2_coding; python scripts/test_robust_menu_logic.py` and confirm it prints a PASS line.</verify>
  <acceptance_criteria>
    - Test names every required ETA mode.
    - Test asserts diagnostic field presence, not only selected menu length.
    - Test distinguishes no-filter diagnostic from all-feasible upper-bound behavior.
    - Test covers exact and greedy solver paths with deterministic fixture data.
  </acceptance_criteria>
  <done>Robust ETA, objective, and solver semantics have deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_menu_runtime_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_optout_accounting.py` passes.
- [ ] `cd work2_coding; python scripts/test_checkpoint_provenance.py` passes.
- [ ] `cd work2_coding; python scripts/test_robust_menu_logic.py` passes.
- [ ] `python -c "import sys; sys.path.insert(0, 'work2_coding'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
- [ ] `rg -n "interval_overlap|chance_constraint|soft_penalty|violation_probability|relative_optimality_gap|menu_overlap_rate" work2_coding/Src/Algorithms/DSPO_Menu.py work2_coding/scripts/test_robust_menu_logic.py` finds implementation and test coverage.
</verification>

<success_criteria>
- ETA-01 is covered: required filter modes are supported.
- ETA-02 is covered: candidate-level ETA diagnostics are recorded.
- ETA-03 is covered: no-filter disables ETA pruning only.
- ETA-04 is covered: soft-penalty preserves candidates and changes objective value through risk penalty.
- MENU-01 is covered: menus evaluate expected profit, opt-out penalty, ETA risk penalty, and service guardrails.
- MENU-02 is covered: pricing and system-aware costs remain consistent across compared policies.
- MENU-03 is covered: exact-small and greedy-large selection paths respect menu size.
- MENU-04 is covered: exact-vs-greedy value/gap/overlap/build-time/candidate/enumeration diagnostics are logged.
- CONTEXT decisions D-13 through D-23 are represented.
- The plan can run autonomously after Plans 02 and 03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-semantics-and-robust-menu-logic/02-04-SUMMARY.md`.
</output>

## PLANNING COMPLETE
