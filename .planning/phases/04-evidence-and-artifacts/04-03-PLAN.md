---
phase: 04-evidence-and-artifacts
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - work2_coding/Src/artifact_status.py
  - work2_coding/Src/artifact_builder.py
  - work2_coding/scripts/build_artifacts.py
  - work2_coding/scripts/test_artifact_gates.py
autonomous: true
requirements:
  - ART-04
user_setup: []
must_haves:
  truths:
    - "D-09: placeholder_only rows produce only incomplete or diagnostic artifacts, never claim-ready tables or figures."
    - "D-10: Invalid checkpoint status blocks claim-ready pilot/formal artifacts and produces blocker/status artifacts."
    - "D-12: no_filter_diagnostic cannot participate in recommended-policy ranking."
    - "D-14: Every generated artifact has adjacent metadata/status JSON recording source rows, run, manifest hash, generation time, and placeholder/incomplete status."
    - "D-15: Environment provenance records Python and package versions or pip freeze; formal artifacts require this snapshot."
    - "D-16: Git commit and dirty-state markers enter provenance."
  artifacts:
    - path: "work2_coding/scripts/test_artifact_gates.py"
      provides: "Placeholder, checkpoint, no-filter, environment, git, and sidecar gate tests"
      min_lines: 120
      contains: "claim_ready"
  key_links:
    - from: "work2_coding/Src/paired_replay.py"
      to: "work2_coding/Src/artifact_status.py"
      via: "Row status and checkpoint fields drive artifact eligibility"
      pattern: "checkpoint_load_status"
---

<objective>
Add provenance sidecars and artifact gates so only eligible evidence can become claim-ready, while incomplete and blocked evidence remains inspectable.

Purpose: prevent placeholder rows, bad checkpoint status, unlabeled diagnostics, missing dependency snapshots, or dirty git ambiguity from supporting formal claims.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-evidence-and-artifacts/04-CONTEXT.md
@.planning/phases/04-evidence-and-artifacts/04-RESEARCH.md
@.planning/phases/04-evidence-and-artifacts/04-01-PLAN.md
@.planning/phases/04-evidence-and-artifacts/04-02-PLAN.md

@work2_coding/Src/paired_replay.py
@work2_coding/Src/artifact_builder.py
@work2_coding/scripts/build_artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact eligibility classifier</name>
  <files>work2_coding/Src/artifact_status.py</files>
  <read_first>work2_coding/Src/paired_replay.py, .planning/phases/04-evidence-and-artifacts/04-CONTEXT.md</read_first>
  <action>Create a small classifier that maps rows plus study summary into artifact statuses: `claim_ready`, `diagnostic`, `incomplete`, or `blocked`. Claim-ready must require non-placeholder rows, completed execution status, acceptable checkpoint status for pilot/formal, required source metadata, and no disallowed diagnostic ranking. Placeholder or contract-only rows should produce diagnostic/incomplete status only.</action>
  <verify>`cd work2_coding; python scripts/test_artifact_gates.py` after Task 4.</verify>
  <acceptance_criteria>
    - Classifier returns reasons, not just booleans.
    - Placeholder rows are never claim-ready.
    - Pilot/formal invalid checkpoint status is blocked.
    - `no_filter_diagnostic` is allowed as labeled diagnostic but excluded from recommendation ranking.
  </acceptance_criteria>
  <done>Artifact eligibility is explicit and testable.</done>
</task>

<task type="auto">
  <name>Task 2: Write sidecar metadata for every artifact</name>
  <files>work2_coding/Src/artifact_builder.py, work2_coding/Src/artifact_status.py</files>
  <read_first>work2_coding/Src/artifact_builder.py, work2_coding/scripts/build_artifacts.py</read_first>
  <action>For every generated JSON/CSV/table/figure/status report, write an adjacent `.status.json` or `.metadata.json` with artifact path, source run directory, source rows, source run ID, study, manifest hash, generation timestamp, placeholder/incomplete/blocked status, checkpoint summary, uptake regimes covered, and diagnostic policy labels.</action>
  <verify>Tests assert sidecars exist beside representative JSON, table, and figure/status artifacts.</verify>
  <acceptance_criteria>
    - Sidecars are deterministic except for timestamp.
    - Sidecars do not claim formal readiness when rows are incomplete.
    - Metadata can be consumed later by Phase 5 claim checklist.
  </acceptance_criteria>
  <done>Generated artifacts carry adjacent provenance/status metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Capture environment and git provenance</name>
  <files>work2_coding/Src/artifact_status.py, work2_coding/scripts/build_artifacts.py</files>
  <read_first>AGENTS.md, .planning/phases/04-evidence-and-artifacts/04-CONTEXT.md</read_first>
  <action>Add provenance collection for Python version, platform, selected package versions or `pip freeze`, git commit, git dirty flag, and changed/untracked path summary. Formal claim-ready artifact generation must require a dependency snapshot; smoke/pilot diagnostic artifacts may record missing dependency snapshot as incomplete or warning status.</action>
  <verify>Tests exercise provenance collection without requiring a clean worktree.</verify>
  <acceptance_criteria>
    - Dirty worktree does not block diagnostic/pilot artifact generation but is visible.
    - Formal claim-ready path fails if dependency snapshot is missing.
    - The implementation never stages or reverts unrelated user files.
  </acceptance_criteria>
  <done>Environment and git provenance are recorded for artifact consumers.</done>
</task>

<task type="auto">
  <name>Task 4: Add artifact gate tests</name>
  <files>work2_coding/scripts/test_artifact_gates.py</files>
  <read_first>work2_coding/Src/artifact_status.py, work2_coding/Src/artifact_builder.py, work2_coding/Src/paired_replay.py</read_first>
  <action>Create script-style tests with temporary rows and output directories. Cover claim-ready synthetic pilot rows, placeholder rows downgraded to incomplete/diagnostic, bad checkpoint status blocked, missing formal dependency snapshot blocked, no-filter excluded from recommended ranking, sidecar creation, and dirty git provenance recording.</action>
  <verify>`cd work2_coding; python scripts/test_artifact_gates.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Tests use synthetic normalized rows rather than editing generated result rows.
    - Tests prove claim-ready generation fails closed.
    - Tests are deterministic on a dirty worktree.
  </acceptance_criteria>
  <done>Artifact gate behavior has deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_artifact_gates.py` passes.
- [ ] `cd work2_coding; python scripts/test_artifact_builder.py` still passes.
- [ ] `cd work2_coding; python scripts/build_artifacts.py --study smoke_robust_menu --allow-incomplete` produces sidecar metadata and non-claim-ready status for contract-only rows.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
</verification>

<success_criteria>
- ART-04 is covered by status/provenance sidecars and claim-ready gates.
- CONTEXT decisions D-09, D-10, D-12, D-14, D-15, and D-16 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-and-artifacts/04-03-SUMMARY.md`.
</output>

## PLANNING COMPLETE

