---
phase: 04-evidence-and-artifacts
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - work2_coding/Src/artifact_builder.py
  - work2_coding/scripts/build_artifacts.py
  - work2_coding/scripts/test_artifact_builder.py
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
user_setup: []
must_haves:
  truths:
    - "D-05: Artifact builder generates machine-readable JSON/CSV, core LaTeX tables, and PNG figures."
    - "D-06: Core tables cover policy summary, robust filtering, exact/greedy, uptake regime, and provenance/status."
    - "D-07: Core figures cover profit gap, acceptance/opt-out, ETA pruning, home-only share, and exact/greedy time or gap behavior."
    - "D-08: Raw outputs stay under work2_coding/outputs; lightweight artifacts go under work2_coding/artifacts and mirror to artifacts/work2_robust_menu."
    - "D-11: Available uptake regimes may be emitted, but coverage must be explicit."
    - "D-12: no_filter_diagnostic is labeled as diagnostic upper bound and excluded from recommended-policy ranking."
  artifacts:
    - path: "work2_coding/scripts/test_artifact_builder.py"
      provides: "Artifact table, figure, JSON/CSV, mirror, and diagnostic-label tests"
      min_lines: 120
      contains: "no_filter_diagnostic"
  key_links:
    - from: "work2_coding/outputs/studies"
      to: "work2_coding/artifacts"
      via: "Artifact builder consumes normalized rows and writes derived tables/figures"
      pattern: "normalized_rows.json"
---

<objective>
Create the artifact builder that consumes normalized study rows and writes aggregate summaries, LaTeX tables, PNG figures, and mirrored review artifacts.

Purpose: turn Phase 4 evidence rows into paper-facing but provenance-labeled artifacts without hand-editing generated results.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-evidence-and-artifacts/04-CONTEXT.md
@.planning/phases/04-evidence-and-artifacts/04-RESEARCH.md
@.planning/phases/04-evidence-and-artifacts/04-01-PLAN.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-04-SUMMARY.md

@work2_coding/scripts/run_study.py
@work2_coding/Src/paired_replay.py
@work2_coding/Src/experiment_contracts.py
@work2_coding/Src/policy_adapters.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact loading and aggregation helpers</name>
  <files>work2_coding/Src/artifact_builder.py</files>
  <read_first>work2_coding/Src/paired_replay.py, work2_coding/scripts/run_study.py</read_first>
  <action>Add helpers to locate a study run directory, load `study_summary.json`, `normalized_rows.json`, and `manifest_snapshot.yaml`, validate rows, and aggregate metrics by policy, filter mode, uptake regime, and diagnostic status. Keep missing optional metrics as explicit `null`/`NA`; do not invent runtime values for contract-only rows.</action>
  <verify>`cd work2_coding; python scripts/test_artifact_builder.py` after Task 4.</verify>
  <acceptance_criteria>
    - Aggregation preserves row counts, policy tags, split IDs, uptake regimes, manifest hash, and placeholder status.
    - JSON/CSV output uses deterministic ordering.
    - Missing optional metrics do not crash diagnostic/incomplete artifact generation.
  </acceptance_criteria>
  <done>Normalized rows can be loaded and summarized deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Generate core table families</name>
  <files>work2_coding/Src/artifact_builder.py, work2_coding/scripts/build_artifacts.py</files>
  <read_first>.planning/phases/04-evidence-and-artifacts/04-CONTEXT.md, work2_coding/Src/policy_adapters.py</read_first>
  <action>Generate JSON/CSV aggregates and LaTeX tables for five families: policy summary, robust filtering, exact/greedy solver diagnostics, uptake regime behavior, and provenance/status. Ensure `no_filter_diagnostic` is grouped or labeled as diagnostic and is not included in any recommended-policy ranking.</action>
  <verify>Tests assert expected `.json`, `.csv`, and `.tex` files exist and contain diagnostic labels.</verify>
  <acceptance_criteria>
    - Table filenames are stable and reviewer-readable.
    - Tables include source study/run IDs or companion metadata sidecars.
    - Provenance/status table shows placeholder/incomplete/checkpoint fields.
  </acceptance_criteria>
  <done>Required table families are generated from row data.</done>
</task>

<task type="auto">
  <name>Task 3: Generate reviewer-facing figures</name>
  <files>work2_coding/Src/artifact_builder.py, work2_coding/scripts/build_artifacts.py</files>
  <read_first>work2_coding/Src/paired_replay.py, work2_coding/Src/policy_adapters.py</read_first>
  <action>Use matplotlib to generate 4-6 PNG figures when metrics are available: net profit/profit gap, acceptance versus opt-out, ETA pruning/false-negative proxy, home-only share, and exact/greedy build time or relative gap. If a figure lacks required metrics, write an incomplete figure status sidecar instead of fabricating a chart.</action>
  <verify>Tests assert generated PNGs for synthetic metric-rich rows and incomplete status files for metric-poor rows.</verify>
  <acceptance_criteria>
    - Figures have labels, units where available, and source study/run metadata.
    - Missing metrics produce explicit incomplete status.
    - Plot generation does not modify manuscript files.
  </acceptance_criteria>
  <done>Artifact builder can produce reviewer-facing figures or honest incomplete statuses.</done>
</task>

<task type="auto">
  <name>Task 4: Add public artifact builder CLI and tests</name>
  <files>work2_coding/scripts/build_artifacts.py, work2_coding/scripts/test_artifact_builder.py</files>
  <read_first>work2_coding/Src/artifact_builder.py, work2_coding/scripts/test_smoke_study_rows.py</read_first>
  <action>Create `scripts/build_artifacts.py` with options such as `--study`, `--run-dir`, `--output-root`, `--mirror-root`, `--allow-incomplete`, and `--claim-ready`. Add script-style tests using temporary normalized rows to verify aggregate files, table files, figure files/status files, mirror output, and diagnostic no-filter labeling.</action>
  <verify>`cd work2_coding; python scripts/test_artifact_builder.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - CLI can build from an explicit run directory and from the latest run for a study.
    - Outputs land under `work2_coding/artifacts/` by default and mirror to root artifacts when requested.
    - Tests do not require real pilot checkpoint files.
  </acceptance_criteria>
  <done>Artifact generation has a public command and deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_artifact_builder.py` passes.
- [ ] `cd work2_coding; python scripts/run_study.py --study smoke_robust_menu --contract-only --output-root <temp>` creates a run.
- [ ] `cd work2_coding; python scripts/build_artifacts.py --run-dir <temp-run-dir> --allow-incomplete` writes JSON/CSV, tables, and status artifacts.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
</verification>

<success_criteria>
- ART-01 aggregate summaries exist.
- ART-02 table families exist.
- ART-03 figure generation exists with incomplete-status fallback.
- CONTEXT decisions D-05, D-06, D-07, D-08, D-11, and D-12 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-and-artifacts/04-02-SUMMARY.md`.
</output>

## PLANNING COMPLETE

