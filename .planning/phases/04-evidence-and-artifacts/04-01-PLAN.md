---
phase: 04-evidence-and-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - work2_coding/scripts/run_study.py
  - work2_coding/Src/paired_replay.py
  - work2_coding/Src/study_execution.py
  - work2_coding/scripts/test_study_execution_status.py
autonomous: true
requirements:
  - ART-01
  - ART-04
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 4 minimum completion is real pilot non-placeholder evidence, or an explicit blocker when runtime prerequisites prevent it."
    - "D-02: contract_only rows may feed only placeholder or incomplete artifacts."
    - "D-03: Missing pilot/formal checkpoint produces incomplete status plus blocker report, not claim-ready evidence."
    - "D-04: Smoke and pilot may train or reuse a shared checkpoint; formal requires explicit checkpoint provenance."
    - "D-13: Rows carry run_id, manifest_hash, settings_hash, trace ID/hash, checkpoint status/path/hash, git marker, and schema version."
    - "D-16: Dirty git state is allowed only when explicitly recorded in provenance."
  artifacts:
    - path: "work2_coding/scripts/test_study_execution_status.py"
      provides: "Study execution status, checkpoint blocker, and non-placeholder row guard tests"
      min_lines: 100
      contains: "placeholder_only"
  key_links:
    - from: "work2_coding/scripts/run_study.py"
      to: "work2_coding/Src/paired_replay.py"
      via: "Public runner writes schema-validated normalized rows"
      pattern: "normalized_rows"
---

<objective>
Extend the Phase 3 study runner so Phase 4 can produce honest evidence rows: real non-placeholder rows when smoke/pilot runtime execution succeeds, and explicit incomplete/blocker metadata when checkpoint or runtime prerequisites are unavailable.

Purpose: make artifact builders consume row outputs that are scientifically labeled before any tables or figures exist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evidence-and-artifacts/04-CONTEXT.md
@.planning/phases/04-evidence-and-artifacts/04-RESEARCH.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-VERIFICATION.md
@.planning/phases/03-experiment-contracts-and-fair-replay/03-04-SUMMARY.md

@work2_coding/scripts/run_study.py
@work2_coding/Src/paired_replay.py
@work2_coding/Src/experiment_contracts.py
@work2_coding/Src/policy_adapters.py
@work2_coding/Src/config.py
@work2_coding/run.py
@work2_coding/Environments/OOH/Parcelpoint_py.py
@work2_coding/Src/Algorithms/DSPO_Menu.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit study execution statuses</name>
  <files>work2_coding/Src/paired_replay.py, work2_coding/scripts/run_study.py</files>
  <read_first>work2_coding/Src/paired_replay.py, work2_coding/scripts/run_study.py, .planning/phases/04-evidence-and-artifacts/04-CONTEXT.md</read_first>
  <action>Introduce a small status vocabulary used consistently by rows and summaries: `completed`, `contract_only`, `diagnostic`, `incomplete`, and `blocked`. Keep `placeholder_only` separate from status. Add row validation rules that reject claim-ready/formal-looking rows when status is incomplete/blocked or placeholder-only. Preserve existing Phase 3 formal placeholder rejection.</action>
  <verify>`cd work2_coding; python scripts/test_paired_replay_contract.py` still passes after the change.</verify>
  <acceptance_criteria>
    - Status values are centralized or clearly documented.
    - Contract-only rows remain allowed for smoke diagnostics.
    - Formal rows still cannot be placeholder-only.
    - Incomplete/blocked rows cannot be mistaken for completed evidence.
  </acceptance_criteria>
  <done>Row status semantics are explicit enough for artifact gates to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime prerequisite and checkpoint blockers</name>
  <files>work2_coding/scripts/run_study.py, work2_coding/Src/study_execution.py</files>
  <read_first>work2_coding/Src/experiment_contracts.py, work2_coding/Experiments/studies/pilot_robust_menu.yaml, work2_coding/Src/Utils/Utils.py</read_first>
  <action>Add helper logic that inspects a manifest before execution and records blocker details for missing required checkpoints, missing checkpoint hashes, unsupported formal placeholder runs, or runtime execution errors. For pilot/formal manifests with required checkpoints, do not silently continue with random weights. Write `blockers.json` beside `study_summary.json` whenever execution is incomplete or blocked.</action>
  <verify>`cd work2_coding; python scripts/test_study_execution_status.py` after Task 4.</verify>
  <acceptance_criteria>
    - Pilot/formal missing checkpoint path is reported with path and expected status.
    - Blocked runs still write summary/status artifacts but not completed non-placeholder rows.
    - Existing smoke contract-only behavior stays available.
  </acceptance_criteria>
  <done>Study runner fails closed into explicit blocker metadata when prerequisites are missing.</done>
</task>

<task type="auto">
  <name>Task 3: Add safe non-placeholder smoke/pilot execution path</name>
  <files>work2_coding/scripts/run_study.py, work2_coding/Src/study_execution.py</files>
  <read_first>work2_coding/run.py, work2_coding/Src/config.py, work2_coding/Environments/OOH/Parcelpoint_py.py, work2_coding/Src/Algorithms/DSPO_Menu.py</read_first>
  <action>Add an opt-in execution mode such as `--execute` or `--run-mode actual` that attempts the cheapest safe smoke/pilot replay using the existing manifest/policy setting path. Extract `stats[8]` acceptance/opt-out metadata and selected-offer menu metadata into normalized rows. If actual execution proves too expensive or unstable, keep the implementation honest by producing `incomplete` status and a blocker report; do not synthesize runtime metrics.</action>
  <verify>Run the cheapest implemented command. At minimum, `cd work2_coding; python scripts/run_study.py --study smoke_robust_menu --contract-only` must still pass; if actual execution is enabled, also run it with a reduced smoke/pilot manifest and verify `placeholder_only=False` rows.</verify>
  <acceptance_criteria>
    - Non-placeholder rows are only emitted from actual runtime stats/menu metadata.
    - `no_filter_diagnostic` remains diagnostic in rows.
    - Checkpoint status from runtime/provenance helpers is propagated into every row.
    - Trace/settings hashes remain paired across compared policies.
  </acceptance_criteria>
  <done>Phase 4 has a public real-or-blocked study execution path.</done>
</task>

<task type="auto">
  <name>Task 4: Add execution status tests</name>
  <files>work2_coding/scripts/test_study_execution_status.py</files>
  <read_first>work2_coding/scripts/run_study.py, work2_coding/Src/paired_replay.py, work2_coding/Experiments/studies/pilot_robust_menu.yaml</read_first>
  <action>Create script-style tests using temporary output roots. Cover contract-only smoke status, formal placeholder rejection, pilot missing-checkpoint blocker output, row schema validation for incomplete rows, git/provenance fields when available, and preservation of diagnostic no-filter labeling. Use synthetic or temp manifests only when needed to avoid requiring a real checkpoint in tests.</action>
  <verify>`cd work2_coding; python scripts/test_study_execution_status.py` prints `PASS`.</verify>
  <acceptance_criteria>
    - Tests do not require heavy HGS or a real checkpoint.
    - Tests prove missing checkpoint cannot create claim-ready rows.
    - Tests exercise the public runner path where possible.
  </acceptance_criteria>
  <done>Execution status and prerequisite gates have deterministic regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd work2_coding; python scripts/test_experiment_contracts.py` passes.
- [ ] `cd work2_coding; python scripts/test_paired_replay_contract.py` passes.
- [ ] `cd work2_coding; python scripts/test_smoke_study_rows.py` passes.
- [ ] `cd work2_coding; python scripts/test_study_execution_status.py` passes.
- [ ] `cd work2_coding; python scripts/run_study.py --study smoke_robust_menu --contract-only` writes normalized rows and summary.
- [ ] `cd work2_coding; python -c "import sys; sys.path.insert(0, '.'); import Src.config; print('IMPORT_OK')"` prints `IMPORT_OK`.
</verification>

<success_criteria>
- ART-01 row/status requirements are implementable through public runner outputs.
- ART-04 incomplete/blocker status is explicit before artifact rendering.
- CONTEXT decisions D-01, D-02, D-03, D-04, D-13, and D-16 are represented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-and-artifacts/04-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE

