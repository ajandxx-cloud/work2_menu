---
phase: 01-operational-baseline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ooh_code/manuscript/sections/method.tex
  - ooh_code/manuscript/sections/experiments.tex
autonomous: true
requirements:
  - BASE-01

must_haves:
  truths:
    - "A reader encountering the Baseline Policies subsection sees three operational baselines (insertion-cost greedy, minimum-lateness ranking, random-top-k) named alongside the five existing heuristic baselines"
    - "The experiments paragraph states the correct total policy count and notes that operational baselines use the same evaluation protocol without optimizing the menu"
    - "Operational baselines are clearly distinguished from heuristic baselines by a structural paragraph break and label"
  artifacts:
    - path: "ooh_code/manuscript/sections/method.tex"
      provides: "Updated baseline taxonomy with operational-baseline paragraph"
      contains: "insertion-cost greedy"
    - path: "ooh_code/manuscript/sections/experiments.tex"
      provides: "Updated policy count and operational-baseline evaluation note"
      contains: "operational baselines"
  key_links:
    - from: "ooh_code/manuscript/sections/method.tex"
      to: "sec:baselines"
      via: "paragraph addition after existing heuristic baselines"
      pattern: "operational.*baseline"
    - from: "ooh_code/manuscript/sections/experiments.tex"
      to: "sec:baselines"
      via: "policy count and cross-reference"
      pattern: "operational baselines.*same.*evaluation protocol"
---

<objective>
Update the manuscript's method and experiments sections to name and describe the three operational baselines (insertion-cost greedy, minimum-lateness ranking, random-top-k) that are currently implemented but invisible in the text.

Purpose: Round 2 explicitly criticizes that "the manuscript's Method still describes only five empirical policies" while artifact tables for three additional operational baselines exist. This plan closes that gap by updating the baseline taxonomy and policy count.

Output: method.tex with expanded baselines subsection; experiments.tex with corrected policy count and operational-baseline evaluation note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-operational-baseline-integration/01-CONTEXT.md
@ooh_code/manuscript/sections/method.tex
@ooh_code/manuscript/sections/experiments.tex
@ooh_code/artifacts/tables/operational_baselines_summary.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add operational-baseline paragraph to method.tex sec:baselines</name>
  <files>ooh_code/manuscript/sections/method.tex</files>
  <read_first>
    - ooh_code/manuscript/sections/method.tex (full file, to see the baselines subsection at lines 135-138 and understand surrounding context)
    - ooh_code/artifacts/tables/operational_baselines_summary.tex (to see the exact policy names used in the artifact table)
  </read_first>
  <action>
    In `ooh_code/manuscript/sections/method.tex`, locate subsection 4.5 "Baseline Policies" (label `sec:baselines`, starting at line 135). The current text lists five heuristic baselines in a single paragraph ending at line 138.

    AFTER the existing paragraph (after the sentence ending "All limited-menu policies append the home bundle and use the same common-offset pricing heuristic after menu assembly."), add a new paragraph that introduces the three operational baselines. The paragraph must:

    1. Open with a structural distinction sentence: "Three additional operational baselines provide dispatch-floor and greedy-policy reference points." This distinguishes them from the heuristic baselines above per D-01.

    2. Describe each operational baseline in one line, matching the names in the artifact table exactly:
       - "Insertion-cost greedy selects the $k$ non-home bundles with the lowest predicted marginal cost $\hat{c}(b)$, representing a cost-aware dispatch heuristic."
       - "Minimum-lateness ranking selects by smallest predicted pickup-time deviation $\Delta_b$, representing a time-priority dispatch heuristic."
       - "Random-top-k floor samples $k$ non-home bundles uniformly at random from the feasible set, providing a non-optimized floor that quantifies the value of any informed selection rule."

    3. Close with a sentence establishing their role: "These operational baselines use the same common-offset pricing and home-retention rules as the menu-optimization policies, but they do not apply the menu-value surrogate or ETA-based filtering. They therefore benchmark menu optimization against dispatch-floor selection rather than against alternative optimization objectives."

    Use the label "operational baselines" consistently per D-03. Do NOT create a new subsection -- fold into the existing sec:baselines per D-01.
  </action>
  <acceptance_criteria>
    - method.tex contains the exact string "insertion-cost greedy" (matching artifact table)
    - method.tex contains the exact string "minimum-lateness ranking" (matching artifact table)
    - method.tex contains the exact string "random-top-k" (matching artifact table)
    - method.tex contains the phrase "operational baselines" (D-03 label)
    - method.tex contains the phrase "dispatch-floor" (D-10 framing)
    - The new paragraph appears AFTER the sentence ending "menu assembly." and BEFORE the next subsection "Evidence Scope and Limitations"
    - No new \subsection or \subsubsection command is added (folds into existing sec:baselines per D-01)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "insertion-cost greedy\|minimum-lateness ranking\|random-top-k" "ooh_code/manuscript/sections/method.tex" | grep -q "3"</automated>
  </verify>
  <done>The Baseline Policies subsection (sec:baselines) in method.tex contains a new paragraph after the existing heuristic baselines that names and describes insertion-cost greedy, minimum-lateness ranking, and random-top-k as operational baselines, distinguishes them from heuristic baselines, and explains their dispatch-floor reference role.</done>
</task>

<task type="auto">
  <name>Task 2: Update experiments.tex policy count and add operational-baseline evaluation note</name>
  <files>ooh_code/manuscript/sections/experiments.tex</files>
  <read_first>
    - ooh_code/manuscript/sections/experiments.tex (full file, to see the Policies paragraph at lines 12-18 and the exact current wording)
  </read_first>
  <action>
    In `ooh_code/manuscript/sections/experiments.tex`, locate the Policies paragraph starting at line 12. The current text says "The main comparison evaluates seven policies". This needs two changes:

    1. **Update the policy count.** Change "seven policies" to "ten policies: the seven heuristic and menu-optimization policies described below, plus three operational baselines (insertion-cost greedy, minimum-lateness ranking, and random-top-k floor) that use the same evaluation protocol but do not optimize the menu." This updates the count from 7 to 10 to include the three operational baselines per D-02 and D-13.

    2. **After the sentence ending "All limited-menu policies use $k = 3$." (line 18), add one sentence:** "The three operational baselines also use $k = 3$ and the same common-offset pricing heuristic, but bypass the menu-value surrogate and ETA-based filtering, providing non-optimized selection references."

    These are the only changes to experiments.tex. Do not modify any other paragraph.
  </action>
  <acceptance_criteria>
    - experiments.tex contains the exact string "ten policies" (updated from "seven")
    - experiments.tex does NOT contain the old string "seven policies" anywhere in the Policies paragraph
    - experiments.tex contains the phrase "three operational baselines"
    - experiments.tex contains the phrase "insertion-cost greedy, minimum-lateness ranking, and random-top-k floor"
    - experiments.tex contains the phrase "do not optimize the menu"
    - experiments.tex contains the sentence about operational baselines using k = 3 and same pricing heuristic
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ten policies\|three operational baselines\|do not optimize the menu" "ooh_code/manuscript/sections/experiments.tex" | grep -q "3"</automated>
  </verify>
  <done>The experiments.tex Policies paragraph states "ten policies" (seven heuristic/menu-optimization plus three operational baselines), names the three operational baselines, and includes a sentence noting they use the same protocol but do not optimize the menu.</done>
</task>

</tasks>

<verification>
1. `grep "insertion-cost greedy" ooh_code/manuscript/sections/method.tex` returns a match.
2. `grep "ten policies" ooh_code/manuscript/sections/experiments.tex` returns a match.
3. `grep "operational baselines" ooh_code/manuscript/sections/method.tex ooh_code/manuscript/sections/experiments.tex` returns matches in both files.
4. No `\subsection` or `\subsubsection` was added to method.tex -- operational baselines fold into existing sec:baselines.
</verification>

<success_criteria>
- method.tex sec:baselines names insertion-cost greedy, minimum-lateness ranking, and random-top-k as operational baselines.
- method.tex distinguishes operational baselines (dispatch-floor references) from heuristic baselines (policy alternatives).
- experiments.tex states "ten policies" and notes operational baselines use the same evaluation protocol without menu optimization.
- All policy names match the artifact table exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-operational-baseline-integration/01-01-SUMMARY.md`
</output>
