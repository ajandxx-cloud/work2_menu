---
phase: 01-operational-baseline-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ooh_code/manuscript/sections/results.tex
  - ooh_code/manuscript/sections/limitations.tex
autonomous: true
requirements:
  - BASE-02

must_haves:
  truths:
    - "A reader encountering the Results section sees the operational-baseline table with a reader-facing caption and can interpret the dispatch-floor reference gap"
    - "The manuscript explicitly states what operational baselines do NOT prove -- they show dispatch-floor reference performance, not service-quality bounds"
    - "The limitations section notes operational-baseline evidence is limited to the RC low-uptake regime"
    - "Round 2's 'artifact exists but is not integrated' criticism is directly addressed by the table appearing in the main Results narrative"
  artifacts:
    - path: "ooh_code/manuscript/sections/results.tex"
      provides: "ArtifactTable reference to operational_baselines_summary.tex with interpretation paragraph"
      contains: "operational_baselines_summary"
    - path: "ooh_code/manuscript/sections/limitations.tex"
      provides: "Scope note on operational-baseline evidence coverage"
      contains: "operational baselines"
  key_links:
    - from: "ooh_code/manuscript/sections/results.tex"
      to: "../artifacts/tables/operational_baselines_summary.tex"
      via: "ArtifactTable macro"
      pattern: "ArtifactTable.*operational_baselines"
    - from: "ooh_code/manuscript/sections/results.tex"
      to: "tab:operational_baselines"
      via: "Table reference in interpretation text"
      pattern: "Table.*operational_baselines"
---

<objective>
Place the operational-baseline table in the main Results section with a reader-facing caption and interpretation paragraph, and add a scope note to the Limitations section.

Purpose: Round 2 states "the artifact table exists, but is not integrated." The table must appear where reviewers encounter it during normal reading, with interpretation that frames the dispatch-floor reference gap honestly. This completes the manuscript integration of operational baselines.

Output: results.tex with operational-baseline table placement and interpretation; limitations.tex with evidence-scope note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-operational-baseline-integration/01-CONTEXT.md
@ooh_code/manuscript/sections/results.tex
@ooh_code/manuscript/sections/limitations.tex
@ooh_code/artifacts/tables/operational_baselines_summary.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add operational-baseline table and interpretation to results.tex</name>
  <files>ooh_code/manuscript/sections/results.tex</files>
  <read_first>
    - ooh_code/manuscript/sections/results.tex (full file, to understand the existing subsection flow and identify the natural insertion point)
    - ooh_code/artifacts/tables/operational_baselines_summary.tex (to see the table structure, label, and column names for caption alignment)
  </read_first>
  <action>
    In `ooh_code/manuscript/sections/results.tex`, add the operational-baseline comparison into the existing results narrative flow per D-08. The natural insertion point is AFTER the "RC Outside-Option Benchmark" subsection (after the paragraph about profit decomposition, around line 55-56) and BEFORE the "Cross-Instance Evaluation" subsection (line 58). This places it where the reader has just seen the main RC comparison and can now see the dispatch-floor reference.

    Insert the following content between those two subsections:

    **An \ArtifactTable call** referencing the operational-baselines table:
    ```latex
    \ArtifactTable{../artifacts/tables/operational_baselines_summary.tex}{Operational baseline comparison on the RC low-uptake benchmark. Insertion-cost greedy, minimum-lateness ranking, and random-top-k floor provide dispatch-floor reference points that do not apply menu optimization or ETA filtering. Gap and win rate are computed against exhaustive full display.}
    ```

    **An interpretation paragraph** (2-3 sentences) immediately after the \ArtifactTable call:
    ```latex
    Table~\ref{tab:operational_baselines} reports the operational-baseline comparison under the RC low-uptake regime. All three operational baselines underperform full display on net profit, with gaps of approximately 4--6 units, confirming that even random selection does not catastrophically degrade performance in this low-uptake setting. The menu-optimization policies in Table~\ref{tab:rc_main_optout} therefore operate in a narrow reference-performance band: they must add value beyond what dispatch-floor selection already achieves, not merely beat a naive floor. This comparison does not establish service-quality bounds or generalize to higher-uptake regimes; it benchmarks the starting point from which menu optimization seeks improvement.
    ```

    Key design decisions reflected:
    - Uses `operational_baselines_summary.tex` (label `tab:operational_baselines`) per D-06, NOT the phase32 version which has duplicate labels.
    - Reader-facing caption summarizes key findings per D-07.
    - Interpretation states what baselines do NOT prove per D-09.
    - Frames as "reference performance gap" per D-10.
    - No self-referential "we now include..." language per D-11.
    - Links to main results table via cross-reference per D-14.
    - Integrates into existing flow (after RC benchmark, before cross-instance) per D-08, NOT a standalone subsection.
  </action>
  <acceptance_criteria>
    - results.tex contains the exact string `\ArtifactTable{../artifacts/tables/operational_baselines_summary.tex}`
    - results.tex contains the phrase "dispatch-floor reference points" in the caption
    - results.tex contains a `Table~\ref{tab:operational_baselines}` reference
    - results.tex contains the phrase "does not establish service-quality bounds"
    - results.tex contains the phrase "reference-performance band"
    - results.tex contains a cross-reference to `tab:rc_main_optout`
    - The insertion appears AFTER the RC Outside-Option Benchmark content and BEFORE the Cross-Instance Evaluation subsection
    - No new `\subsection` command is added (integrates into existing flow per D-08)
    - No self-referential language like "we now include" or "the revised manuscript" appears
  </acceptance_criteria>
  <verify>
    <automated>grep -c "operational_baselines_summary\|dispatch-floor reference\|does not establish service-quality bounds" "ooh_code/manuscript/sections/results.tex" | grep -q "3"</automated>
  </verify>
  <done>The results.tex file includes the operational-baseline table via \ArtifactTable with a reader-facing caption, followed by a 2-3 sentence interpretation paragraph that frames the dispatch-floor reference gap, states what the baselines do not prove, and links to the main results table. The content integrates naturally after the RC Outside-Option Benchmark and before Cross-Instance Evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Add operational-baseline scope note to limitations.tex</name>
  <files>ooh_code/manuscript/sections/limitations.tex</files>
  <read_first>
    - ooh_code/manuscript/sections/limitations.tex (full file, to find the right insertion point and match existing tone/style)
  </read_first>
  <action>
    In `ooh_code/manuscript/sections/limitations.tex`, add a brief scope note about the operational-baseline evidence. The natural insertion point is in the third paragraph (starting "The empirical evaluation covers three instance types...") which discusses instance and evidence coverage. This is the right place because it already discusses evidence scope limitations.

    At the END of that paragraph (before the sentence "Broader coverage --- including larger fleets..."), insert one sentence:

    "The operational-baseline comparison in Table~\ref{tab:operational_baselines} covers only the RC low-uptake regime and does not span the calibrated higher-uptake or city instances; extending this reference to additional demand conditions is left as future work."

    This single sentence per D-12 transparently acknowledges the operational-baseline evidence scope. It matches the existing limitations tone (honest, specific about what is missing, deferred to future work where appropriate).

    Do NOT add a new paragraph or subsection -- append to the existing instance-coverage paragraph.
  </action>
  <acceptance_criteria>
    - limitations.tex contains the exact string `Table~\ref{tab:operational_baselines}`
    - limitations.tex contains the phrase "RC low-uptake regime"
    - limitations.tex contains the phrase "does not span the calibrated higher-uptake or city instances"
    - The sentence is added within the existing instance-coverage paragraph (the one mentioning "three instance types"), NOT as a standalone paragraph
    - No new `\subsection` or `\paragraph` command is added
  </acceptance_criteria>
  <verify>
    <automated>grep -c "tab:operational_baselines\|RC low-uptake regime" "ooh_code/manuscript/sections/limitations.tex" | grep -q "2"</automated>
  </verify>
  <done>The limitations.tex instance-coverage paragraph includes a sentence noting that the operational-baseline comparison covers only the RC low-uptake regime and does not span higher-uptake or city instances.</done>
</task>

</tasks>

<verification>
1. `grep "operational_baselines_summary" ooh_code/manuscript/sections/results.tex` returns a match -- table is placed.
2. `grep "tab:operational_baselines" ooh_code/manuscript/sections/results.tex` returns a match -- table is referenced in text.
3. `grep "does not establish service-quality bounds" ooh_code/manuscript/sections/results.tex` returns a match -- interpretation is honest.
4. `grep "tab:operational_baselines" ooh_code/manuscript/sections/limitations.tex` returns a match -- scope is limited.
5. No self-referential "we now include" or "the revised manuscript" language in either file.
</verification>

<success_criteria>
- results.tex includes the operational-baseline table via \ArtifactTable with a reader-facing caption summarizing findings.
- An interpretation paragraph follows the table, framing the dispatch-floor reference gap and stating what baselines do NOT prove.
- limitations.tex contains a scope note restricting operational-baseline evidence to RC low-uptake.
- Round 2's "artifact exists but is not integrated" criticism is directly addressed by the table appearing in the main Results narrative with interpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-operational-baseline-integration/01-02-SUMMARY.md`
</output>
