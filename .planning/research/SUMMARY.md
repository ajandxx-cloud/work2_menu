# Research Summary

## Stack

Use the current `work2_coding/` package as the active runtime root unless Phase 1 discovers a more authoritative root. It imports `Src.config` successfully, includes `DSPO_Menu.py`, and contains the simulator and bundled data. Existing `.planning/codebase` references to `ooh_code/` should be treated as stale until audited.

## Table Stakes

- Stage 0 repository audit and import/runner verification.
- Explicit opt-out semantics and service/routing accounting.
- Robust ETA/time-window filters with metadata.
- Robust menu objective with exact-small and greedy-large solver diagnostics.
- Smoke, pilot, and formal experiment contracts.
- Normalized rows, artifact tables/figures, and provenance gates.
- Paper-ready framing and claim checklist.

## Watch Out For

- Do not create a parallel root by accident.
- Do not trust formal comparisons until checkpoint load status is visible.
- Do not route opted-out passengers as accepted home pickups.
- Do not overclaim no-filter diagnostics.
- Do not run exact enumeration beyond configured thresholds.
- Do not let placeholder artifacts support manuscript claims.

## Recommended First Phase

Start with Stage 0 repository audit. Produce a concise audit report that identifies:

- active runtime root,
- import status,
- relevant runner scripts,
- existing menu policies,
- broken or stale imports,
- current opt-out and checkpoint risks,
- minimal patch plan for a runnable robust time-window menu experiment.

---
*Research synthesized: 2026-06-10*
