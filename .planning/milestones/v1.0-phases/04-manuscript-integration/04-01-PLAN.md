---
phase: 04
plan: 01
name: Narrative, terminology, and caption integration
created: 2026-05-14
status: pending
depends_on: []
must_haves:
  - "All phase-numbered references in prose replaced with descriptive reader-facing labels"
  - "Experiments section opens with a unifying study-structure paragraph"
  - "Table captions use journal-facing language with first-use explanations"
  - "Code variable names have reader-facing descriptions on first use"
---

# Plan 04-01: Narrative, Terminology, and Caption Integration

## Goal

Replace internal names with reader-facing labels throughout the manuscript, add a unifying study-structure paragraph, and convert table captions to journal-facing language.

## Tasks

### Task 1: Rewrite experiments.tex companion-studies paragraph as unified study-structure paragraph

**File:** `ooh_code/manuscript/sections/experiments.tex`

The current lines 20-39 contain the companion-studies paragraph and pipeline paragraph with phase-numbered names. Replace with a unified paragraph:

"The experimental evidence is organized into three tiers. First, mechanism diagnostics evaluate the exact-vs-greedy assortment approximation and the ETA-filtering comparison under controlled RC conditions. Second, behavioral stress tests examine how menu-optimization outcomes respond to MNL parameter variation and uptake-regime changes. Third, descriptive external checks apply the best-performing policies to Austin and Seattle instances. This tiered structure separates internal mechanism evidence from external generalization checks."

Also rewrite the pipeline paragraph (lines 31-39) to use descriptive labels instead of phase names. Replace `phase27_mnl_sensitivity` with "MNL sensitivity suite", `phase31_uptake_menu_value` with "uptake-regime suite", etc.

### Task 2: Replace phase names in results.tex table labels and prose

**File:** `ooh_code/manuscript/sections/results.tex`

Changes needed:
- `\ArtifactTable{../artifacts/tables/phase29_exact_greedy_gap_summary.tex}` — keep the file path as-is (it references the actual artifact file), but note the table has a reader-facing label
- `Table~\ref{tab:phase29_exact_greedy_gap}` — these refs reference labels defined IN the artifact .tex files, so they cannot be changed here without changing the artifact files
- Instead, update the artifact table labels themselves

For prose: replace any remaining phase-numbered references with descriptive terms.

### Task 3: Update artifact table labels

**Files:** The artifact .tex files in `ooh_code/artifacts/tables/`

Rename labels in these files (keeping backward compatibility with \providecommand or by updating both the label and all references):
- `phase29_exact_greedy_gap_summary.tex`: label `tab:phase29_exact_greedy_gap` → `tab:exact_greedy_gap` (add alias)
- `phase30_robust_filtering_summary.tex`: label `tab:phase30_robust_filtering` → `tab:robust_filtering` (add alias)
- `phase31_uptake_menu_value_summary.tex`: label `tab:phase31_uptake_menu_value` → `tab:uptake_menu_value` (add alias)

For each: add a second `\label` with the new reader-facing name. Then update all `\ref{}` calls in results.tex and appendix.tex to use the new labels.

### Task 4: Fix appendix.tex prose phase references

**File:** `ooh_code/manuscript/sections/appendix.tex`

- Line 119: "Phase 19 uncertainty summary" → replace with descriptive label (e.g., "split-level gap summary")
- Do NOT change verbatim blocks (lines 172-186) — those are literal CLI commands

### Task 5: Update table captions to journal-facing language

**Files:** Artifact table files in `ooh_code/artifacts/tables/`

Key caption updates:
- `rc_main_optout_summary.tex`: "RC outside-option evaluation setup" → explain what the outside-option setup is in the caption itself
- `benchmark_bridge_summary.tex`: "Benchmark-role bridge summary" → explain the bridge concept or use plain language
- `policy_gap_uncertainty_summary.tex`: already updated in Phase 3, verify it reads well
- `city_split_gap_table.tex`: verify caption is reader-facing
- Add first-use explanations where needed ("RC" = "Ridepooling Calibration synthetic benchmark")

### Task 6: Add reader-facing descriptions for code variable names

**File:** `ooh_code/manuscript/sections/experiments.tex`

On first use of `\texttt{menu_optimization}` and `\texttt{menu_optimization_v2}`, add a brief description: "the strict-filter menu-optimization policy (\texttt{menu\_optimization})" and "the no-filter variant (\texttt{menu\_optimization\_v2})"

## Execution Notes

- This plan is manuscript-only edits — no experiment code changes
- Read each file before editing
- Artifact .tex files are generated — editing them directly is fine since they won't be regenerated in this phase
- When done, write a SUMMARY.md to `.planning/phases/04-manuscript-integration/04-01-SUMMARY.md`
