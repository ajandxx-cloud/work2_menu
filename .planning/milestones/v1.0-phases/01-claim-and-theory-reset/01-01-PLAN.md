---
phase: 01-claim-and-theory-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ooh_code/manuscript/sections/abstract.tex
  - ooh_code/manuscript/sections/introduction.tex
  - ooh_code/manuscript/sections/conclusion.tex
  - ooh_code/manuscript/sections/method.tex
  - ooh_code/manuscript/sections/appendix.tex
autonomous: true
requirements:
  - CLAIM-01

must_haves:
  truths:
    - "Abstract no longer contains 'materially shrink' or any phrase implying universal operational improvement"
    - "Introduction contribution statements carry inline evidence-tier qualifiers such as '(mechanism-diagnostic evidence)'"
    - "Conclusion uses conditional framing with 'In the studied settings' and explicitly flags generalization as an open question"
    - "Evidence hierarchy table is present in method.tex as a subsection titled 'Evidence scope and limitations'"
    - "Each results subsection in results.tex has a short tier label like '[Mechanism diagnostic]' at its start"
    - "MNL parameter caveat is present in method.tex stating results are conditional on the parameterization"
  artifacts:
    - path: "ooh_code/manuscript/sections/abstract.tex"
      provides: "Diagnostic-framed abstract"
      contains: "can reshape"
    - path: "ooh_code/manuscript/sections/introduction.tex"
      provides: "Evidence-tier-qualified contributions"
      contains: "diagnostic eligibility comparison"
    - path: "ooh_code/manuscript/sections/conclusion.tex"
      provides: "Conditionally framed conclusion"
      contains: "In the studied settings"
    - path: "ooh_code/manuscript/sections/method.tex"
      provides: "Evidence hierarchy subsection and MNL caveat"
      contains: "Evidence scope and limitations"
    - path: "ooh_code/manuscript/sections/results.tex"
      provides: "Tier-labeled results subsections"
      contains: "[Mechanism diagnostic]"
    - path: "ooh_code/manuscript/sections/appendix.tex"
      provides: "Updated evidence hierarchy table with expanded 'Does not support' column"
      contains: "Does not support"
  key_links:
    - from: "ooh_code/manuscript/sections/method.tex"
      to: "ooh_code/manuscript/sections/appendix.tex"
      via: "Evidence hierarchy table moved from appendix to method, appendix retains back-reference"
      pattern: "Evidence scope and limitations"
    - from: "ooh_code/manuscript/sections/results.tex"
      to: "ooh_code/manuscript/sections/method.tex"
      via: "Tier labels in results subsections reference evidence scope definitions in method"
      pattern: "\\[Mechanism diagnostic\\]|\\[Behavioral stress test\\]|\\[Descriptive external check\\]"
---

<objective>
Rewrite claim language across abstract, introduction, and conclusion to use diagnostic/exploratory framing instead of universal-dominance language. Move the evidence hierarchy table from the appendix into the method section as a new subsection, add tier labels to results subsections, and insert an MNL parameter caveat.

Purpose: Addresses reviewer Critical 1 (evidence insufficient for strong claims) by structurally narrowing every claim to match its evidence tier. The reader should be unable to find any sentence that implies the framework universally improves DRT operations.

Output: Rewritten abstract.tex, introduction.tex, conclusion.tex; new evidence subsection and MNL caveat in method.tex; tier labels in results.tex; updated appendix.tex with expanded "Does not support" column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-claim-and-theory-reset/01-CONTEXT.md
@review_ServiceMenuDRT_202605141407.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite abstract, introduction, and conclusion with diagnostic claim framing</name>
  <files>ooh_code/manuscript/sections/abstract.tex, ooh_code/manuscript/sections/introduction.tex, ooh_code/manuscript/sections/conclusion.tex</files>
  <read_first>
    ooh_code/manuscript/sections/abstract.tex
    ooh_code/manuscript/sections/introduction.tex
    ooh_code/manuscript/sections/conclusion.tex
    .planning/phases/01-claim-and-theory-reset/01-CONTEXT.md
  </read_first>
  <action>
Rewrite three files to replace universal-dominance language with diagnostic/exploratory framing. Specific changes per CONTEXT.md decisions:

**abstract.tex** (full rewrite, single paragraph):
- Replace "materially shrink the feasible meeting-point assortment before optimization acts" with "can reshape the feasible meeting-point assortment" (diagnostic framing per D-01)
- Replace "provides a transparent operational approximation" with something like "provides a transparent operational approximation whose gap is empirically auditable"
- Replace "pricing/menu effects should be interpreted only in regimes" -- keep this self-limiting language as-is, it is already good
- The phrase "can reshape" must appear in the final abstract text
- Keep the abstract as ONE paragraph, do not restructure into multiple paragraphs

**introduction.tex** (targeted edits, keep existing structure):
- In contribution 3 (line 9, "we treat ETA filtering as a robust eligibility layer"), replace "robust eligibility layer" with "diagnostic eligibility comparison" per D-03
- Add inline evidence-tier qualifiers to each of the four contributions. Insert parenthetical tags like "(mechanism-diagnostic evidence)" at the end of each contribution sentence. The four contributions and their tiers are:
  1. Formulation contribution: no tier needed (this is formal, not empirical)
  2. Exact-small/greedy-large: add "(mechanism-diagnostic evidence)"
  3. ETA filtering as diagnostic eligibility comparison: add "(mechanism-diagnostic evidence)"
  4. Behavioral regime organization: add "(behavioral stress-test and descriptive evidence)"
- Keep the self-limiting sentence at line 12 ("deliberately narrower than a universal dominance claim") as-is

**conclusion.tex** (targeted edits to paragraphs 3 and 4):
- Replace the prescriptive sentence "Operators should audit ETA filtering before tuning downstream ranking, benchmark greedy approximations against exact enumeration where possible, and interpret pricing or welfare results only in regimes where passengers actually face live tradeoffs." with conditional framing per D-04: "In the studied settings, ETA filtering was the most impactful layer; generalization to other demand regimes and network structures is an open question. Operators considering service-menu optimization should audit filtering and approximation quality before interpreting pricing or welfare comparisons, but the current evidence supports these recommendations only within the tested calibration range."
- Keep paragraphs 1 and 2 (evidence description) largely as-is since they already use disciplined language
  </action>
  <verify>
    grep -c "materially shrink" ooh_code/manuscript/sections/abstract.tex  # must return 0
    grep -c "can reshape" ooh_code/manuscript/sections/abstract.tex  # must return >= 1
    grep -c "robust eligibility layer" ooh_code/manuscript/sections/introduction.tex  # must return 0
    grep -c "diagnostic eligibility comparison" ooh_code/manuscript/sections/introduction.tex  # must return >= 1
    grep -c "mechanism-diagnostic" ooh_code/manuscript/sections/introduction.tex  # must return >= 1
    grep -c "In the studied settings" ooh_code/manuscript/sections/conclusion.tex  # must return >= 1
    grep -c "generalization.*open question" ooh_code/manuscript/sections/conclusion.tex  # must return >= 1
  </verify>
  <acceptance_criteria>
    - abstract.tex contains zero instances of "materially shrink"
    - abstract.tex contains at least one instance of "can reshape"
    - introduction.tex contains zero instances of "robust eligibility layer"
    - introduction.tex contains "diagnostic eligibility comparison" replacing it
    - introduction.tex contains at least 2 inline evidence-tier tags (parenthetical qualifiers)
    - conclusion.tex contains "In the studied settings" as conditional framing
    - conclusion.tex contains "generalization" and "open question" in the same sentence or paragraph
    - No structural changes to introduction's four-contribution paragraph structure (still 4 contributions)
    - All three files remain valid LaTeX (no broken commands, balanced braces)
  </acceptance_criteria>
  <done>All three files rewritten with diagnostic framing. No universal-dominance language remains. Introduction has evidence-tier qualifiers on contributions. Conclusion uses conditional framing with explicit generalization caveat.</done>
</task>

<task type="auto">
  <name>Task 2: Move evidence hierarchy to method section, add tier labels to results, add MNL caveat, update appendix</name>
  <files>ooh_code/manuscript/sections/method.tex, ooh_code/manuscript/sections/results.tex, ooh_code/manuscript/sections/appendix.tex</files>
  <read_first>
    ooh_code/manuscript/sections/method.tex
    ooh_code/manuscript/sections/results.tex
    ooh_code/manuscript/sections/appendix.tex
    .planning/phases/01-claim-and-theory-reset/01-CONTEXT.md
  </read_first>
  <action>
Four coordinated changes across three files:

**1. method.tex -- Add new subsection "Evidence scope and limitations" BEFORE the existing Baseline Policies subsection (Section 2.5 / subsection 5). Insert it as the last substantive subsection before results. This means inserting it after the ETA-Based Time-Window Filtering subsection (subsection 4) and before Baseline Policies (subsection 5). Actually, per CONTEXT.md decision D-05, it should go BEFORE results, so insert it as a new subsection after all current method subsections, i.e., after the Baseline Policies subsection at the end of the method section.**

The new subsection content:
- Subsection title: `\subsection{Evidence Scope and Limitations}`
- Add a label: `\label{sec:evidence_scope}`
- Insert the evidence hierarchy table (copy from appendix.tex lines 4-22). Change the table label to `tab:evidence_hierarchy_main` and the caption to something like: "Evidence hierarchy used to interpret the empirical results. The table separates evidence tiers so that no claim is stronger than its evidence category."
- Add the MNL parameter caveat as a paragraph after the table: "Results are conditional on the MNL parameterization reported in Table~\ref{tab:mnl_params} and should not be interpreted as behaviorally validated predictions. The demand model encodes one stylized commuter-DRT preference regime; sensitivity to alternative parameterizations is discussed in Section~\ref{sec:results}." (per D-07)
- Expand the "Does not support" column entries to address Critical 1 specifically:
  - Mechanism diagnostic: add "Strong operator-impact claims, demand-calibration predictions, or stable welfare rankings across parameter regimes"
  - Behavioral stress test: add "External validation of the true passenger demand model or calibrated welfare magnitudes"
  - Descriptive external check: add "Stable interval estimates, broad geographic generalization, or city-specific operational recommendations"
  - Archival diagnostic: add "Confirmatory robustness under the current pipeline or direct quantitative comparison with current results"

**2. results.tex -- Add short tier labels at the start of each results subsection. Insert immediately after each `\subsection{...}` line, before the first `\ArtifactTable` or paragraph:**
- Subsection "Exact-vs-Greedy Assortment Diagnostics" (line 6): add `[Mechanism diagnostic.]` as a short sentence, e.g., `\textit{[Mechanism diagnostic.]} This section provides mechanism diagnostics for the menu-construction approximation.`
- Subsection "Robust ETA Filtering" (line 14): add `\textit{[Mechanism diagnostic.]}`
- Subsection "Uptake-Regime Menu Value" (line 22): add `\textit{[Behavioral stress test.]}`
- Subsection "RC Outside-Option Benchmark": add `\textit{[Mechanism diagnostic.]}`
- Subsection "Cross-Instance Evaluation": add `\textit{[Descriptive external check.]}`

Also update line 4 which says "Appendix Table~\ref{tab:evidence_hierarchy}" to say "Table~\ref{tab:evidence_hierarchy_main}" since the table is now in the method section.

**3. appendix.tex -- Update the Evidence Hierarchy section:**
- Keep the section header `\section{Evidence Hierarchy}` and label `app:evidence_hierarchy`
- Replace the table itself with a cross-reference: "The evidence hierarchy table is presented in the main text as Table~\ref{tab:evidence_hierarchy_main}. The categories, supporting evidence, and limitations are defined there."
- This keeps the appendix section as a signpost without duplicating the table.

**4. appendix.tex -- Expand "Does not support" column in the main-text table copy (handled in step 1 above).**
  </action>
  <verify>
    grep -c "Evidence Scope and Limitations" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "evidence_hierarchy_main" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "conditional on the MNL parameterization" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "Mechanism diagnostic" ooh_code/manuscript/sections/results.tex  # must return >= 2
    grep -c "Behavioral stress test" ooh_code/manuscript/sections/results.tex  # must return >= 1
    grep -c "Descriptive external check" ooh_code/manuscript/sections/results.tex  # must return >= 1
    grep -c "evidence_hierarchy_main" ooh_code/manuscript/sections/results.tex  # must return >= 1
    grep -c "evidence_hierarchy_main" ooh_code/manuscript/sections/appendix.tex  # must return >= 1
  </verify>
  <acceptance_criteria>
    - method.tex contains a new subsection titled "Evidence Scope and Limitations" with label sec:evidence_scope
    - method.tex contains the evidence hierarchy table with label tab:evidence_hierarchy_main
    - method.tex contains the MNL caveat paragraph mentioning "conditional on the MNL parameterization"
    - The "Does not support" column entries are expanded beyond the original text (each entry is longer than the original)
    - results.tex has tier labels in at least 4 of the 5 subsections (using \textit{[Tier name.]} format)
    - results.tex references tab:evidence_hierarchy_main instead of tab:evidence_hierarchy
    - appendix.tex Evidence Hierarchy section now cross-references tab:evidence_hierarchy_main instead of containing the full table
    - All three files remain valid LaTeX
  </acceptance_criteria>
  <done>Evidence hierarchy table moved from appendix to method section as "Evidence Scope and Limitations" subsection. MNL parameter caveat added. "Does not support" column expanded with Critical 1 concerns. Results subsections carry tier labels. Appendix updated to cross-reference the moved table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manuscript text → Reader | Claims must not overstate evidence; wording changes are the primary control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Claim language | mitigate | Every contribution carries evidence-tier qualifier; grep-verifiable |
| T-01-02 | Elevation of Privilege | Evidence hierarchy | mitigate | Tier labels in results subsections prevent readers from misinterpreting evidence strength |
| T-01-03 | Information Disclosure | MNL parameters | mitigate | Explicit caveat in method section flags parameterization dependency |
</threat_model>

<verification>
1. Grep abstract.tex for zero instances of "materially shrink"
2. Grep introduction.tex for "diagnostic eligibility comparison" and evidence-tier tags
3. Grep conclusion.tex for "In the studied settings"
4. Grep method.tex for "Evidence Scope and Limitations" section
5. Grep results.tex for tier labels in all subsections
6. Read all modified files end-to-end to confirm LaTeX validity and narrative coherence
</verification>

<success_criteria>
- Abstract uses "can reshape" instead of "materially shrink"
- All four introduction contributions carry evidence-tier qualifiers
- Conclusion frames practical lessons as conditional on studied settings
- Evidence hierarchy table is in method section (not only appendix)
- MNL parameter caveat is present in method
- Results subsections have tier labels
- Reviewer Critical 1 has a clear response path through these changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-claim-and-theory-reset/01-01-SUMMARY.md`
</output>
