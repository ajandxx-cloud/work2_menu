---
phase: 01-claim-and-theory-reset
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ooh_code/manuscript/sections/method.tex
  - ooh_code/manuscript/sections/appendix.tex
autonomous: true
requirements:
  - THEORY-01

must_haves:
  truths:
    - "Method section presents Lambert-W as one of three equal pricing transforms (cost-plus, flat markdown, Lambert-W), each getting approximately one paragraph"
    - "Method subsection is titled 'Pricing transforms evaluated', not 'Reference common-offset pricing rule'"
    - "Appendix Lambert-W section is titled 'Lambert-W Reference Transform (Supplementary)'"
    - "Method section includes a sentence noting flat markdown's empirical advantage in some calibrated regimes"
    - "No explicit Critical 3 response paragraph exists; structural demotion itself is the response"
  artifacts:
    - path: "ooh_code/manuscript/sections/method.tex"
      provides: "Restructured pricing subsection presenting three equal transforms"
      contains: "Pricing transforms evaluated"
    - path: "ooh_code/manuscript/sections/method.tex"
      provides: "Flat markdown advantage note"
      contains: "flat markdown"
    - path: "ooh_code/manuscript/sections/appendix.tex"
      provides: "Renamed Lambert-W appendix section"
      contains: "Lambert-W Reference Transform (Supplementary)"
  key_links:
    - from: "ooh_code/manuscript/sections/method.tex"
      to: "ooh_code/manuscript/sections/appendix.tex"
      via: "Method references appendix derivation with supplementary framing"
      pattern: "Pricing transforms evaluated|appendix.*Lambert-W|Supplementary"
---

<objective>
Structurally demote Lambert-W pricing from a core contribution to one of three equally presented pricing transforms in the method section. Rename the method subsection and appendix section. Add a flat-markdown advantage note. Ensure no explicit "response to reviewer" paragraph exists.

Purpose: Addresses reviewer Critical 3 (Lambert-W theoretical status ambiguous) by making the demotion structural rather than disclaimer-based. The reader should see three pricing rules presented symmetrically, with Lambert-W as one tested option rather than the featured method.

Output: Restructured pricing subsection in method.tex; renamed appendix section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-claim-and-theory-reset/01-CONTEXT.md
@review_ServiceMenuDRT_202605141407.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure pricing subsection in method.tex to present three equal transforms</name>
  <files>ooh_code/manuscript/sections/method.tex</files>
  <read_first>
    ooh_code/manuscript/sections/method.tex
    .planning/phases/01-claim-and-theory-reset/01-CONTEXT.md
  </read_first>
  <action>
Rewrite the pricing-related paragraphs in method.tex (Section 2.2 "MNL Choice Model and Implemented Pricing Rules"). The current structure has:

1. Lines 19-58: MNL choice model equations (KEEP AS-IS)
2. Lines 60-79: "Reference common-offset pricing rule" paragraph with Lambert-W derivation
3. Lines 81-89: "Pricing baselines" paragraph with cost-plus and flat-markdown

Restructure as follows per CONTEXT.md decisions D-09 through D-11:

**Rename the paragraph header** on line 60 from `\paragraph{Reference common-offset pricing rule.}` to `\paragraph{Pricing transforms evaluated.}` -- this becomes the umbrella for all three transforms.

**Reorganize into three sub-paragraphs of roughly equal length, presented in this order:**

1. **Cost-plus pricing** (currently at lines 81-83): Move this to be the FIRST transform presented. Expand slightly to ~5-7 sentences. The cost-plus rule is `p_b = clip(c_hat(b) - R, p_min, p_max)`. Explain: removes the shared-offset optimization entirely; price equals predicted cost minus revenue target; simplest possible pricing that respects bounds; serves as a baseline showing what happens when demand-aware price adjustment is removed.

2. **Flat-markdown pricing** (currently at lines 85-89): Move this to be the SECOND transform. Expand to ~5-7 sentences. The flat-markdown rule is `p_b = clip(c_hat(b) + kappa, p_min, p_max)`. Explain: applies a single constant markdown/offset to all bundles; kappa is calibrated per regime; notably, flat markdown outperforms the Lambert-W transform in some calibrated uptake regimes, suggesting that the closed-form optimization does not always dominate simpler alternatives (per D-11). This sentence about empirical advantage MUST be included.

3. **Lambert-W common-offset pricing** (currently at lines 60-79): Move this to be the THIRD and LAST transform presented. Condense the current content to ~5-7 sentences. Keep the key equations (S_impl, delta_impl via W_0, clipping) but reduce the surrounding explanatory prose. Remove the paragraph-level prominence -- it is now just the third of three rules. Keep the disclaimer at line 79 ("implemented common-offset pricing heuristic, not as the exact optimizer") but condense it to one sentence. Add a forward reference to the appendix for the full derivation: "The full derivation is provided in Appendix~\ref{app:lambertw}."

**After all three transforms**, keep the closing comparison paragraph (lines 87-89, "These baselines deliberately remove the Lambert-W transform...") but update it to be a general comparison note for all three transforms. The sentence "The comparison is therefore about how much the pricing transform itself contributes once filtering and menu composition are held fixed" is good and should remain.

**Important constraints:**
- Do NOT add an explicit "In response to the reviewer's concern about Lambert-W..." paragraph anywhere. Per D-12, structural demotion IS the response.
- The Lambert-W equations (Eq. lambertw and Eq. price) must remain in the method section -- they are just no longer the featured/default rule.
- All equation labels must be preserved for cross-reference integrity.
- The MNL choice model equations and text (lines 19-58) are NOT modified in this task.
  </action>
  <verify>
    grep -c "Reference common-offset pricing rule" ooh_code/manuscript/sections/method.tex  # must return 0
    grep -c "Pricing transforms evaluated" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "cost-plus" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "flat.markdown" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "outperform" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "Appendix.*lambertw" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "eq:lambertw" ooh_code/manuscript/sections/method.tex  # must return >= 1
    grep -c "eq:price" ooh_code/manuscript/sections/method.tex  # must return >= 1
  </verify>
  <acceptance_criteria>
    - method.tex contains zero instances of the old paragraph title "Reference common-offset pricing rule"
    - method.tex contains "Pricing transforms evaluated" as the new paragraph title
    - Cost-plus pricing is presented FIRST among the three transforms
    - Flat-markdown pricing is presented SECOND, with an explicit sentence noting it can outperform Lambert-W in some calibrated regimes
    - Lambert-W pricing is presented THIRD and LAST (not first or featured)
    - Equation labels eq:lambertw and eq:price are preserved (not broken)
    - A forward reference to Appendix app:lambertw exists in the Lambert-W paragraph
    - No "response to reviewer" or "Critical 3" paragraph exists anywhere in the file
    - The MNL choice model subsection (equations, Table reference) is unchanged from its current state
    - The file remains valid LaTeX (balanced braces, no broken commands)
  </acceptance_criteria>
  <done>Method section presents three pricing transforms (cost-plus, flat markdown, Lambert-W) as equal alternatives under "Pricing transforms evaluated". Lambert-W is last and references the appendix. Flat markdown's empirical advantage is noted. No explicit reviewer-response language.</done>
</task>

<task type="auto">
  <name>Task 2: Rename appendix Lambert-W section and verify cross-references</name>
  <files>ooh_code/manuscript/sections/appendix.tex</files>
  <read_first>
    ooh_code/manuscript/sections/appendix.tex
    ooh_code/manuscript/sections/method.tex
    .planning/phases/01-claim-and-theory-reset/01-CONTEXT.md
  </read_first>
  <action>
Rename the Lambert-W appendix section per CONTEXT.md decision D-10:

**Change the section title** (currently line 28): `\section{Lambert-W Common-Offset Pricing Reference}` becomes `\section{Lambert-W Reference Transform (Supplementary)}`

**Keep the label** `\label{app:lambertw}` unchanged (cross-references from method.tex point to this label).

**Keep all derivation content** (equations, conditions, setup) unchanged. The full derivation stays in the appendix.

**Review the existing disclaimers** in the appendix (lines 31, 72) -- they already say things like "not a proof of optimality for the full simulator objective" and "heuristic reference transform rather than an exact optimality certificate." These are good and should remain as-is. No changes to disclaimer language needed.

**After renaming, verify that the cross-reference from method.tex** (`Appendix~\ref{app:lambertw}`) still resolves correctly. Since the label is unchanged, it will.

**Also check**: The appendix's Evidence Hierarchy section was modified by Plan 01 (Task 2). Since this plan depends on Plan 01, that change is already in place. Do NOT touch the Evidence Hierarchy section in this task -- only modify the Lambert-W section (lines 28-82).
  </action>
  <verify>
    grep -c "Lambert-W Common-Offset Pricing Reference" ooh_code/manuscript/sections/appendix.tex  # must return 0
    grep -c "Lambert-W Reference Transform (Supplementary)" ooh_code/manuscript/sections/appendix.tex  # must return >= 1
    grep -c "app:lambertw" ooh_code/manuscript/sections/appendix.tex  # must return >= 1 (label preserved)
    grep -c "app:lambertw" ooh_code/manuscript/sections/method.tex  # must return >= 1 (cross-ref from method)
  </verify>
  <acceptance_criteria>
    - appendix.tex contains zero instances of old title "Lambert-W Common-Offset Pricing Reference"
    - appendix.tex contains "Lambert-W Reference Transform (Supplementary)" as the section title
    - Label app:lambertw is preserved in appendix.tex
    - method.tex references Appendix app:lambertw (cross-reference integrity)
    - All derivation equations and disclaimer text within the appendix section are unchanged
    - The Evidence Hierarchy section in appendix.tex is NOT modified by this task
  </acceptance_criteria>
  <done>Appendix Lambert-W section renamed to "Lambert-W Reference Transform (Supplementary)". Label preserved. Cross-references from method.tex resolve correctly. Derivation content unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Method pricing → Reader | Pricing presentation must not imply Lambert-W is the recommended/default rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | Lambert-W prominence | mitigate | Lambert-W presented last of three equal transforms; flat markdown advantage noted |
| T-01-05 | Information Disclosure | Flat markdown performance | mitigate | Explicit sentence in method noting flat markdown outperforms Lambert-W in some regimes |
| T-01-06 | Repudiation | Structural demotion | accept | No explicit reviewer-response paragraph; demotion speaks for itself |
</threat_model>

<verification>
1. Grep method.tex for "Pricing transforms evaluated" (new title present)
2. Grep method.tex for zero instances of "Reference common-offset pricing rule" (old title gone)
3. Verify Lambert-W is the LAST pricing transform presented (read the paragraph order)
4. Grep method.tex for flat markdown outperforming language
5. Grep appendix.tex for "Lambert-W Reference Transform (Supplementary)" (new title)
6. Grep appendix.tex for zero instances of "Lambert-W Common-Offset Pricing Reference" (old title gone)
7. Verify app:lambertw label exists in appendix and is referenced from method
</verification>

<success_criteria>
- Method subsection presents three equal pricing transforms with Lambert-W last
- Flat markdown's empirical advantage over Lambert-W is noted in method text
- Appendix section renamed to "Supplementary" framing
- Cross-references between method and appendix are intact
- No explicit reviewer-response language anywhere
- Reviewer Critical 3 is structurally addressed by demotion itself
</success_criteria>

<output>
After completion, create `.planning/phases/01-claim-and-theory-reset/01-02-SUMMARY.md`
</output>
