---
phase: 02-medium-evidence-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ooh_code/outputs/phase28/filter_validity.json
  - ooh_code/scripts/build_artifacts.py
  - ooh_code/artifacts/tables/filter_validity_summary.tex
autonomous: true
requirements: [EVID-02]

must_haves:
  truths:
    - "Filter-validity table includes a Bias column with mean signed error for displayed ETA and IVT"
    - "Bias is computed from existing episode-level data in filter_validity.json"
    - "The existing FN pruning rate column is preserved without changes"
  artifacts:
    - path: "ooh_code/outputs/phase28/filter_validity.json"
      provides: "Source data with bias fields added to each row"
      contains: "eta_bias"
    - path: "ooh_code/scripts/build_artifacts.py"
      provides: "Updated build_filter_validity_artifacts function rendering Bias column"
      contains: "displayed_eta_bias"
    - path: "ooh_code/artifacts/tables/filter_validity_summary.tex"
      provides: "Filter-validity table with Bias column"
      contains: "Bias"
  key_links:
    - from: "ooh_code/outputs/phase28/filter_validity.json"
      to: "ooh_code/scripts/build_artifacts.py"
      via: "JSON data read by build_artifacts.py"
      pattern: "filter_validity"
    - from: "ooh_code/scripts/build_artifacts.py"
      to: "ooh_code/artifacts/tables/filter_validity_summary.tex"
      via: "write_tex_table"
      pattern: "filter_validity_summary"
---

<objective>
Add bias (mean signed error) diagnostics to the existing filter-validity table and rebuild the artifact.

Purpose: Addresses reviewer Major 2 by providing directional error information alongside the existing MAE metrics. Bias reveals whether ETA/IVT predictions systematically over- or under-estimate, which is more informative than MAE alone.
Output: Updated filter_validity_summary.tex table with Bias column, updated filter_validity.json with computed bias fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-medium-evidence-expansion/02-CONTEXT.md

<interfaces>
<!-- Current filter_validity.json structure -->
From ooh_code/outputs/phase28/filter_validity.json:
  schema_version: 1
  source: "outputs/phase22/eta_compare.json"
  rows: [
    {eta_variant, label, fn_pruning_rate, displayed_eta_mae, selected_eta_mae,
     displayed_ivt_mae, selected_ivt_mae, acceptance_rate, non_home_acceptance_rate,
     mean_net_profit_gap}
  ]
  4 rows: deployed, heuristic, stronger, oracle

<!-- Current table columns -->
From ooh_code/artifacts/tables/filter_validity_summary.tex:
  Headers: ETA/filter signal, FN pruning, Displayed ETA MAE, Selected ETA MAE,
           Displayed IVT MAE, Selected IVT MAE, Acceptance, Gap vs full

<!-- Key: bias is NOT yet tracked in research_pipeline.py SUMMARY_NUMERIC_KEYS -->
<!-- Per user decision: compute bias from existing episode-level data in filter_validity.json -->

From ooh_code/scripts/build_artifacts.py:
  write_tex_table(path, caption, label, headers, rows) -- standard table renderer
  format_metric(value, digits=3) -- formats float or None to "--"

Manuscript pattern for supplementary appendix tables:
  Tables in appendix use same format as main-text tables but go to appendix sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute bias and update filter_validity.json with bias fields</name>
  <files>
    ooh_code/outputs/phase28/filter_validity.json
  </files>
  <read_first>
    ooh_code/outputs/phase28/filter_validity.json
    ooh_code/outputs/phase22/eta_compare.json
  </read_first>
  <action>
Compute mean signed error (bias) for displayed ETA and displayed IVT and add these fields to filter_validity.json.

First, investigate the source data in `ooh_code/outputs/phase22/eta_compare.json` to see if raw episode-level prediction errors are available. If they are:
- For each ETA variant (deployed, heuristic, stronger, oracle), compute:
  - `displayed_eta_bias` = mean(predicted_eta - actual_eta) across displayed non-home offers
  - `displayed_ivt_bias` = mean(predicted_ivt - actual_ivt) across displayed non-home offers
- Positive bias means over-prediction (predicted > actual), negative means under-prediction.

If raw episode-level data is NOT available in eta_compare.json, compute bias from the existing aggregate data:
- `displayed_eta_bias` cannot be computed from MAE alone (MAE = mean|error| loses sign).
- In this case, look for the raw episode-level output files in `ooh_code/outputs/` directories that the original filter_validity data was generated from. Check `ooh_code/outputs/phase22/` or `ooh_code/outputs/phase5/` for episode-level prediction records.
- As a last resort, compute bias by re-running the filter-validity extraction script (if one exists) with a bias computation added.

Update `ooh_code/outputs/phase28/filter_validity.json`:
- Add `displayed_eta_bias` and `displayed_ivt_bias` fields to each of the 4 rows
- Preserve all existing fields unchanged
- Update schema_version to 2 to reflect the new fields
- Add an `interpretation` note: "displayed_eta_bias is mean signed error (predicted - actual); positive means over-prediction"

Per user decision D-FilterDepth: only compute bias (mean signed error). No P50/P90/P95 percentile errors (deferred). Keep FN pruning rate only (no confusion matrix).
  </action>
  <acceptance_criteria>
    - `ooh_code/outputs/phase28/filter_validity.json` contains `displayed_eta_bias` field in at least one row
    - `ooh_code/outputs/phase28/filter_validity.json` contains `displayed_ivt_bias` field in at least one row
    - All 4 original rows (deployed, heuristic, stronger, oracle) still exist in the file
    - All original fields (fn_pruning_rate, displayed_eta_mae, etc.) are preserved
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/2.paper_2_menu optimization-6分（需降ai）/ooh_code" && python -c "
import json
d = json.loads(open('outputs/phase28/filter_validity.json').read())
rows = d['rows']
print(f'Row count: {len(rows)}')
assert len(rows) >= 4, 'Need 4 rows'
has_eta_bias = all('displayed_eta_bias' in r for r in rows)
has_ivt_bias = all('displayed_ivt_bias' in r for r in rows)
print(f'ETA bias present in all rows: {has_eta_bias}')
print(f'IVT bias present in all rows: {has_ivt_bias}')
assert has_eta_bias, 'Missing displayed_eta_bias'
assert has_ivt_bias, 'Missing displayed_ivt_bias'
print('PASS')" </automated>
  </verify>
  <done>filter_validity.json updated with displayed_eta_bias and displayed_ivt_bias fields on all 4 rows, all original fields preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update build_artifacts.py and rebuild filter_validity_summary.tex with Bias column</name>
  <files>
    ooh_code/scripts/build_artifacts.py
    ooh_code/artifacts/tables/filter_validity_summary.tex
  </files>
  <read_first>
    ooh_code/scripts/build_artifacts.py
    ooh_code/outputs/phase28/filter_validity.json
    ooh_code/artifacts/tables/filter_validity_summary.tex
  </read_first>
  <action>
Add a filter-validity table builder to build_artifacts.py and rebuild the table with a Bias column.

Find how the filter_validity_summary.tex table is currently generated. It may be generated by a standalone script or hand-authored. Regardless, the approach is:

1. Add a new function `build_filter_validity_artifacts()` to build_artifacts.py that:
   - Reads `ooh_code/outputs/phase28/filter_validity.json`
   - Extracts the 4 rows with their fields
   - Builds table rows with columns: ETA/filter signal, FN pruning, Displayed ETA Bias, Displayed ETA MAE, Displayed IVT Bias, Displayed IVT MAE, Acceptance, Gap vs full
   - The "Displayed ETA Bias" and "Displayed IVT Bias" columns are inserted BEFORE the corresponding MAE columns
   - Uses `write_tex_table()` with appropriate caption and label
   - Caption: "Blended-filter validity diagnostics on the RC mechanism benchmark. Bias is mean signed error (predicted - actual); positive values indicate over-prediction. Displayed-offer errors are computed over all displayed non-home offers."
   - Label: `tab:filter_validity_summary`

2. Ensure the function is called from the appropriate place in the build pipeline (in `main()` or from the relevant build path).

3. Rebuild the table:
   ```
   python scripts/build_artifacts.py --study phase27_mnl_sensitivity
   ```
   (or whichever study triggers the filter validity build, or call the function directly if needed)

If the filter_validity_summary.tex was hand-authored, replace it with the generated version but preserve the same caption style and table format.

Per user decision: the FN pruning rate column is kept. No percentile errors. No confusion matrix.
  </action>
  <acceptance_criteria>
    - `ooh_code/scripts/build_artifacts.py` contains a function that reads filter_validity.json and produces the table
    - `ooh_code/artifacts/tables/filter_validity_summary.tex` contains a "Bias" column header
    - The rebuilt table has at least 8 columns (original 8 columns minus 2 old + 2 new bias = same or more)
    - The table retains all 4 data rows (deployed, heuristic, stronger, oracle)
    - `displayed_eta_bias` values appear in the table (not just "--" placeholders)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/2.paper_2_menu optimization-6分（需降ai）/ooh_code" && python -c "
t = open('artifacts/tables/filter_validity_summary.tex').read()
has_bias = 'Bias' in t
rows = [l for l in t.split(chr(10)) if '&' in l and 'hline' not in l and 'tabular' not in l and 'filter' not in l.lower()]
print(f'Has Bias column: {has_bias}')
print(f'Data rows: {len([r for r in rows if not r.strip().startswith(\"ETA\")])}')
assert has_bias, 'Missing Bias column header'
print('PASS')" </automated>
  </verify>
  <done>build_artifacts.py updated with filter-validity builder function, filter_validity_summary.tex rebuilt with Bias column containing displayed ETA and IVT bias values for all 4 ETA variants.</done>
</task>

</tasks>

<verification>
1. filter_validity.json has displayed_eta_bias and displayed_ivt_bias on all 4 rows
2. build_artifacts.py has a filter-validity table builder
3. filter_validity_summary.tex contains Bias column with numeric values
4. All original columns and data rows preserved
</verification>

<success_criteria>
- Filter-validity table includes directional error (bias) alongside absolute error (MAE)
- Bias values are computed from existing data, not placeholders
- build_artifacts.py can regenerate the table reproducibly
- Table structure is consistent with manuscript formatting conventions
</success_criteria>

<output>
After completion, create `.planning/phases/02-medium-evidence-expansion/02-02-SUMMARY.md`
</output>
